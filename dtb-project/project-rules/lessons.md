# Lektionen (lessons.md)

> Append-only Sammlung nicht-offensichtlicher, wiederverwendbarer Regeln.
> Erfasst mit `/dtb:lesson`, gelesen als Prior von impl-plan, debug-plan, plan-review, code-review, implement.
> **Nicht** manuell editieren/loeschen (append-only); Kuratierung geschieht bewusst.
> Kompakt halten — Soft-Limit ~150 Zeilen (danach aeltere, ueberholte Lektionen aussortieren).

| # | Datum | Context | Problem | Rule | Applies-to |
|---|-------|---------|---------|------|------------|
| 1 | 2026-07-15 | Gate-/Flip-Bedingungen, die mechanische Kriterien pro Phase pruefen (`dtb:implement`, §2) | Ein Kriterium, das erst ein spaeterer Schritt derselben Phase erfuellt, wird als „verletzt" gewertet und blockiert das Abhaken frueherer Schritte — das Gate blockiert sich selbst | Bei Phasen-Kriterien „verletzt" (pruefbar rot bzgl. bereits umgesetzter Schritte) von „ausstehend" (liefert ein spaeterer Schritt) trennen — nur Verletzung blockiert, Ausstehendes nicht | impl-plan, code-review |
| 2 | 2026-07-15 | Automated-Verifikationskriterien als Grep formulieren (Checkpoint-Kriterien, Konsistenz-Sweeps) | Ein Grep auf bloße Stichwoerter trifft auch eigene Inventar-/Doku-Zitate desselben Begriffs → False Positives | Verifikations-Greps praezise auf den Zielort ankern (Pfad-Glob, `after:`/`next:`-Kontext, Sektion) statt auf bloße Stichwoerter | impl-plan, code-review |
| 3 | 2026-07-15 | Einen bestehenden Skill umhaengen, umbenennen oder entkernen (Pipeline-Aenderung) | Uebersichts-Skills (workflow-status, pipeline-graph, project-health, workflow-next) referenzieren Pipeline-Schritte außerhalb der Frontmatter — Discovery ueber `consumes`/`produces` findet sie nicht | Vor Skill-Umbau repo-weit nach dem Skill-Namen greppen, nicht nur Frontmatter-Referenzen betrachten | impl-plan, plan-review |
| 4 | 2026-07-18 | Treiber-/Umsetzungs-Skill (`dtb:implement`) — gehoert er zum Lektionen-Prior-Kreis? | `implement` bekam eine „Lektionen als Prior"-Sektion (`consumes: lessons.md`), die kein Plan-Schritt auffuehrte → ein Feature-End-Review liest das als ungeplanten Drift, obwohl es Design-Absicht ist | `implement` liest `lessons.md` bewusst als Prior (wie impl-plan/plan-review/code-review) — Sektion + `consumes`-Eintrag sind Absicht, kein Drift; der Treiber soll bei der Umsetzung bereits erfasste Fehler nicht wiederholen | implement, impl-plan |
| 5 | 2026-07-19 | `impl-plan` Codebase-Research — Stichproben-Verifikation der `## Betroffene Module`-Liste aus `discovery.md` | Eine Modul-Zeile, die auf eine noch zu bauende Neubau-Datei zeigt, fehlt beim Glob → die ganze Liste gilt als „veraltet" → unnoetiger Full-Scan bei jedem Greenfield-Skill-Feature | Bei der Modul-Listen-Verifikation Neubau-Ziele (noch nicht existente Deliverable-Dateien) von echter Staleness (verschobene/geloeschte Bestandsdateien) trennen — ein fehlender Neubau-Pfad ist kein Scan-Ausloeser | impl-plan |
| 6 | 2026-07-19 | Phasen-Commit waehrend `dtb:implement`, aber ueber `commit-and-push` statt des Phasen-Ende-Rituals ausgefuehrt | `commit-and-push` schreibt bewusst keine SHA in `## Progress` zurueck → die geflippte Zeile bleibt SHA-los, `impl-review` flaggt das als Traceability-Luecke | Wird ein Phasen-Commit ausserhalb des `implement`-Rituals gemacht (z.B. `commit-and-push`), die Commit-SHA manuell in die zugehoerige `## Progress`-Zeile nachtragen | implement, impl-review |
| 8 | 2026-07-30 | Automated-Checkpoint-Kriterium als Negativ-Grep (`grep -c "X" datei` = 0), wenn ein Wert/Begriff abgeschafft wird (Feature plan-status-feld, Wertemenge `In Umsetzung`/`Abgeschlossen`) | Der Skill muss den Begriff weiterhin nennen — in der Abschaffungs-Notiz (Phase 1) und in der Lese-/Toleranz-Matrix fuer Rueckwaertskompatibilitaet (Phase 3). Das Kriterium meldet rot, obwohl sein Zweck erfuellt ist; in Phase 1 half Umformulieren, in Phase 3 waere sie Funktionsverlust gewesen (der Leser muss den Altwert erkennen koennen) | Negativ-Kriterien auf die **Wirkstelle** ankern (Gate-Zeile, Ableitungszeile, Template-Block) statt auf die ganze Datei — „Begriff kommt nirgends mehr vor" ist unerfuellbar, sobald Toleranz-, Migrations- oder Abschaffungstexte ihn nennen muessen | impl-plan |
| 7 | 2026-07-30 | Verifikations-/Vergleichslaeufe auf wechselnden Windows-Maschinen (kit-sync Drei-Punkte-Vergleich; `grep -P`-Locale-Ausfall, Heredoc-Backslash-Verlust, PowerShell-Here-String im Bash-Tool) | Ein Vergleich, dessen eine Seite durch Werkzeug-Ausfall leer blieb, meldete 41 Schein-Abweichungen gegen den Leer-Hash — Werkzeug-Fehlschlag sah aus wie ein Datenbefund; Ursache war Improvisation an der verbindlichen Sequenz vorbei | Werkzeug-Fehlschlag nie als Datenbefund werten: vor jedem Mehr-Seiten-Vergleich per hartem Gate belegen, dass alle Seiten gefuellt sind (sonst Abbruch); verbindliche Kommando-Sequenzen nie improvisieren; Shell-Syntax nie ueber Werkzeuggrenzen mischen | alle |
| 9 | 2026-08-06 | Ausloeser fuer einen Skill entwerfen, der von selbst anspringen soll (Feature `no-loss-check`, Vorabprobe in Schritt 1.0) | `disable-model-invocation: true` wurde als „dieser Skill ist nur menschlich startbar, also kann er auch keinen anderen anstossen" gelesen — daraus folgte faelschlich, dass ein gesperrter Skill als Ausloeser ausscheidet | Die Sperre wirkt nur auf die Modell-**Initiative**, nicht auf die Anweisungskette: ein gesperrter Skill darf in seinem Ablauf einen ungesperrten anstossen, und der Aufrufer braucht dafuer kein Skill-Tool in `allowed-tools`. Wer einen verlaesslichen Ausloeser fuer einen ungesperrten Skill sucht, verankert ihn im Ablauf eines gesperrten (belegt 2026-08-06: `dtb:implement` stiess `dtb:workflow-next` an) | impl-plan, plan-review |
| 10 | 2026-08-06 | Unscharfe Vergleichsschwelle in einem Prompt-Skill festlegen (`no-loss-check` Stufe 2: „steht dieser Fund schon irgendwo?") | Eine Prozent-Quote („ab 60 % Stichwort-Ueberlappung") suggeriert eine Messbarkeit, die kein Lauf einloest — zwei Laeufe zaehlen Inhaltswoerter unterschiedlich, und niemand rechnet die Zahl nach | Unscharfe Schwellen als **Ersetzungsprobe** formulieren („koennte der vorhandene Eintrag den Kandidaten ohne Informationsverlust ersetzen?") statt als Quote, plus asymmetrische Tie-break-Regel fuer den Zweifelsfall — das ist bei jedem Lauf gleich stellbar und vom Menschen nachvollziehbar | impl-plan, plan-review |
| 11 | 2026-08-06 | Pruefen, ob eine Datei versioniert ist (Report-Kennzeichnung in `no-loss-check`) | Der Ignorier-Status sollte per Pfad-Grep in `.gitignore` bestimmt werden; die Datei traegt aber Glob-Muster mit Last-Match-wins und Negationen — im Kit selbst wird `lessons.md` ueber `dtb-project/project-rules/*` ignoriert, waehrend der Klartext dort nur in einem Kommentar steht. Der Grep lieferte das richtige Ergebnis aus dem falschen Grund | Ignorier-Status nie per Pfad-Vergleich in `.gitignore` ermitteln — nur `git check-ignore` beantwortet die Frage. Steht das Kommando nicht zur Verfuegung (read-only Skill), eine feste Zuordnung dokumentieren und den Abweichungsfall dem Menschen zuweisen, statt zu raten | alle |
| 17 | 2026-08-07 | Bestaende aus Markdown-Tabellen zaehlen und die Zahl weitermelden (Archiv-Kandidaten, offene Ideen, aktive Features) | Zwei Fehlzaehlungen in Folge an denselben Tabellen: `grep -c '^| [a-zA-Z]'` traf die **Kopfzeile** mit (13 statt 11 Archiv-Kandidaten), und offene wurden mit ausgearbeiteten Eintraegen verrechnet (21 statt 25 offene Ideen — archivierte `Ausgearbeitet`-Zeilen mindern den `Offen`-Bestand nicht). Beide Zahlen standen bereits im Statusdokument, bevor das Nachzaehlen sie widerlegte | Vor jeder gemeldeten Bestandszahl den Zaehl-Ausdruck gegen eine **Stichprobe der getroffenen Zeilen** pruefen (nicht nur die Zahl ansehen) und Statuskategorien nie verrechnen — eine einmal ausgesprochene Zahl wird ungeprueft weitergereicht, auch in Statusdateien und Handoffs | alle |
| 16 | 2026-08-07 | Repo-weite Gegenprobe nach einer Skill-Aenderung („wer beschreibt die alte Wirkung noch?"), Feature `feature-start-statusfeld` Schritt 2.2 | 30 Treffer wurden durchgegangen und einer als Nachzuegler erkannt — eine Zeile (`plan-review:47`) galt als „bleibt gueltig", weil sie gegen die **naheliegende** Frage geprueft wurde („schreibt der Skill noch `plan.md`?" — ja, Progress-Nachruestung) statt gegen die **eigentliche** („traegt die Aussage noch?" — nein, die Zeile ist ein Redirect fuer ein FEHLENDES `plan.md`, und genau dann verweigert der Skill selbst). Erst der impl-review fand es | Vor einer Gegenprobe die Pruefragefrage ausdruecklich ausformulieren und **jeden** Treffer gegen genau diese Frage bewerten. Ohne formulierte Frage beantwortet der Durchgang die naheliegende (steht der Name noch drin? stimmt der Nebensatz?) statt der richtigen (traegt die Aussage nach der Aenderung noch?) | impl-review, implement, plan-review |
| 15 | 2026-08-07 | Ein Feature bauen, dessen Zweck die Abschaffung einer Fehlerklasse ist (hier: Zustandsaussagen ohne Pfleger, Feature `feature-start-statusfeld`) | Der Umbau entfernte eine solche Aussage und setzte im **selben Commit** eine neue derselben Klasse ein („einziger Pfleger ist workflow-checkpoint" — fuer `bug.md` nachweislich falsch). Alle sieben Automated-Kriterien der Phase waren gruen: sie pruefen, dass die ALTEN Aussagen weg sind, nie ob die NEUEN tragen. Gefunden erst vom Craft-Reviewer als blocking | Wer ein Feature baut, das eine Fehlerklasse beseitigt, muss den **eigenen neuen Text** ausdruecklich gegen genau diese Klasse pruefen — als eigener Schritt, nicht als Nebeneffekt. Mechanische Kriterien sind hier blind: ein Negativ-Kriterium bezeugt die Abwesenheit des alten Wortlauts, nicht die Richtigkeit des neuen | impl-plan, impl-review, implement |
| 14 | 2026-08-07 | Ein Prompt-/Stil-Artefakt abnehmen, dessen Wirkung an einem Fall-Set kalibriert wurde (Feature `output-style-gezielt`, Schritt 3.3 + anschliessendes impl-review) | Das Kalibrier-Fall-Set lief 5/5 durch — der Craft-Reviewer fand im selben Regeltext danach eine ungeloeste Praezedenz zwischen zwei Regeln (Orientierungspflicht vs. Redundanz-Verbot). Die bestandene Verhaltensprobe hatte den Widerspruch nicht sichtbar gemacht, weil kein Fall ihn traf | Verhaltens-Kalibrierung an Faellen und Text-Review des Regeldokuments finden verschiedene Fehlerklassen: das Fall-Set zeigt, ob der Text im geprueften Fall wirkt, das Review, ob seine Regeln untereinander widerspruchsfrei sind. Bei Prompt-/Stil-/Regel-Artefakten beides ansetzen — ein 5/5-Fall-Set ist kein Ersatz fuer den Text-Review | impl-plan, impl-review |
| 13 | 2026-08-07 | Mehrere `impl-review`-Findings in einer Triage fixen, nachdem die zugehoerigen Phasen laengst abgeschlossen und committet sind (Feature `output-style-gezielt`) | Ein Triage-Fix formulierte eine Textstelle um, auf die ein Automated-Checkpoint-Kriterium einer bereits abgeschlossenen Phase ankerte (`grep -c "output-styles/dtb-\*" >= 2` fiel auf 1) — das Gate der Phase war damit nachtraeglich rot, ohne dass irgendein Lauf es erneut geprueft haette. Lektion 12 deckt den Fall nicht ab: sie verlangt das Gegenlesen benachbarter **Anweisungen im selben Abschnitt**, hier lag das Opfer in einer **anderen Datei** (`plan.md`) | Nach jeder abgeschlossenen Review-Triage die Automated-Checkpoint-Kriterien **aller** von Fixes beruehrten Phasen erneut ausfuehren, nicht nur die Nachbarschaft der Fundstelle gegenlesen. Kriterien ankern auf Textstellen — wer den Text aendert, kann ein gruenes Gate nachtraeglich rot machen | impl-review, implement |
| 12 | 2026-08-06 | Mehrere Findings aus einem Review nacheinander fixen (`impl-review`-Triage, Feature `no-loss-check`) | Zwei Fixes wurden unabhaengig voneinander gebaut: der eine schob die Rechenschaft ueber eine Fund-Klasse an eine Textstelle ab, die der andere kurz darauf inhaltsfrei machte — die Klasse fiel dadurch aus jeder Ausweisung. Gefunden erst vom Verifikations-Lauf danach | Nach jedem angewendeten Review-Fix die benachbarten Anweisungen im selben Abschnitt gegenlesen: die Nachbarschafts-Regel aus `skills/CLAUDE.md` gilt auch fuer Triage-Fixes, nicht nur fuer Code-Aenderungen. Bei inhaltlich gekoppelten Findings die Naht zwischen ihnen ausdruecklich pruefen, bevor die Triage weiterlaeuft | impl-review |
| 18 | 2026-08-09 | Autonome Worker-Laeufe (`dtb:worker`), die einen frisch per `/dtb:task` angelegten Change-Ordner abarbeiten sollen (Task `pipeline-kanten-reziprok`) | Der Worktree entsteht mit `git worktree add <pfad> HEAD` und traegt damit nur committete Dateien — die eben angelegte, untrackte `task.md` fehlt dort, der Worker findet weder seine `## Schritte` noch die Datei zum Abhaken. Die Clean-Vorbedingung des Skills schlaegt nicht an, weil sie nur die vom Task beruehrten Zielpfade prueft, nicht die Auftragsdatei selbst | Vor jedem Worker-Start pruefen, dass die **Auftragsdatei** committet ist — nicht nur, dass die Zielpfade clean sind. Wer einen Arbeitsbaum aus einem Commit ableitet, gibt alles preis, was noch nicht committet ist; die Datei, die den Auftrag traegt, gehoert immer dazu | alle |
| 19 | 2026-08-09 | INBOX-Eintrag per `dtb:task` nur teilweise routen — ein Teilaspekt wird als Aufgabe ausgeschnitten, der Rest bleibt zu entscheiden (Fall #46, 2026-08-08) | Der uebliche Status `Ausgearbeitet` haette den ungerouteten Rest aus jeder Leseansicht fallen lassen — `idea-review` filtert auf `Offen`, `workflow-next` liest nur `In Arbeit` (#44-Muster: Zustand ohne Leser) | Beim Teil-Routing den INBOX-Status auf `Offen` belassen und den gerouteten Teil per Vermerk + Task-Link kennzeichnen; `Ausgearbeitet` erst setzen, wenn der letzte offene Teil geroutet oder entschieden ist | alle |
| 20 | 2026-08-09 | Plan-Schritt, dessen einziges Ergebnis ein Pruefprotokoll ist — `**Dateien:** keine (Pruefschritt)` (Feature `autonome-schiene` Schritt 3.3, Selbstpruefung gegen die eigene Fehlerklasse) | Der Schritt wurde abgehakt und mit SHA belegt, aber sein Output landete nirgends: nicht im Session-Log, nicht in `review.md`, nicht in der Spec. Erst das Manual-Kriterium der Restabnahme („3.3-Pruefprotokoll gegengelesen") stiess einen Tag spaeter ins Leere — der SHA belegte den Commit der Phase, nicht die Durchfuehrung der Pruefung | Jeder Plan-Schritt ohne Datei-Deliverable braucht einen im Plan benannten **Ablageort** fuer sein Ergebnis (Session-Log-Abschnitt, `review.md`, Spec-Sektion) — sonst ist er nicht nachpruefbar und ein spaeteres Abnahme-Kriterium, das ihn gegenliest, hat kein Objekt. `**Dateien:** keine` ist bei Pruef-/Analyse-Schritten ein Warnzeichen, kein Normalfall | impl-plan, plan-review, implement |
| 21 | 2026-08-09 | Verifikationsschritt, der laut Task/Plan „auf der anderen Maschine" stattfinden soll (Task `gitattributes-eol` Schritt 6, offen seit 2026-08-05) | Der Schritt blieb vier Tage liegen, obwohl der groesste Teil seiner Frage ortsunabhaengig war: das Konfigurations-Risiko liess sich lokal mit drei frischen Clones (`core.autocrlf` = true, false, input) in Minuten ausschliessen — nur der maschinenspezifische Rest (lokal modifizierte oder ungepushte Dateien) brauchte wirklich den anderen Rechner | Ortsgebundene Verifikationsschritte vor dem Vertagen zerlegen: den ortsunabhaengigen Teil sofort lokal belegen (bei Umgebungs-/Konfigurationsfragen ein frischer Clone je Variante) und als Vorab-Beleg im Task festhalten, OHNE den Schritt abzuhaken; nur der echte Rest wartet auf den anderen Rechner | impl-plan, plan-review |
| 35 | 2026-09-07 | Verteilungs-/Kopier-Laeufe, deren Erfolg im selben Lauf per Hash-Vergleich "nachverifiziert" und dann ins Session-Log geschrieben wird (`dtb:kit-sync sync`) | Der Lauf vom 2026-09-07 protokollierte "4 Updates eingespielt, je Hash nach dem Kopieren verifiziert, Lock 47 @ `153506e`, 47/47 synchron" — auf der Platte stand einen Monat spaeter unveraendert Lock `d43d5a2` vom 16.08. und Zielkopien vom 14.08./06.08. Es wurde nie geschrieben. Ein gruener Hash-Vergleich belegt nur, dass die beiden verglichenen Seiten gleich sind, nicht dass eine davon die Zielkopie auf der Platte war; das falsche Gruen ueberlebte 31 Tage und einen Handoff, weil die Log-Zeile als Beleg galt | Jede "synchron"/"verteilt"-Meldung an einen **unabhaengigen Zeugen** binden, der den Schreibvorgang bezeugt statt den Vergleich: mtime der Zielkopie nach dem Kopieren, `updatedAt`/`sourceCommit` frisch aus der neu geschriebenen Lock-Datei zurueckgelesen. Zeigt der Zeuge ein Datum vor diesem Lauf, ist die Meldung falsch — unabhaengig davon, was der Hash-Vergleich sagt. Lektion 7 verlangt, gefuellte Vergleichsseiten zu belegen; diese hier verlangt zusaetzlich, die **Identitaet** der geprueften Seite mit dem Schreibziel zu belegen | alle |
| 36 | 2026-09-07 | Steuerzeichen (CR, Tab) in einer Datei per `grep` pruefen, wenn der Aufruf in einer Kommandosubstitution innerhalb doppelter Anfuehrungszeichen steckt — `printf '%s' "$(grep -c $'\r' datei)"` | Freistehend liefert `grep -c $'\r' datei` korrekt 0; in der verschachtelten Form meldete derselbe Ausdruck 46 von 46 Zeilen. Das ANSI-C-Quoting `$'\r'` geht dabei verloren, `grep` bekommt den Literal-String `\r` und matcht in BRE schlicht den Buchstaben `r` — den enthaelt praktisch jede Zeile. Der Fehlalarm sah exakt aus wie ein CRLF-Befund und haette (wie am 2026-09-07 die echte CRLF-Fahndung) eine Stunde Ursachensuche am falschen Objekt ausgeloest | CR-/Steuerzeichen-Pruefungen nie mit `$'...'` in verschachtelter Kommandosubstitution formulieren. Verbindlich: `awk '/\r/{n++} END{print n+0}' datei` (quoting-fest) oder `od -c datei | tail`. Gegenprobe-Pflicht: liefert eine Steuerzeichen-Pruefung "alle Zeilen betroffen", ist zuerst das Pruefkommando verdaechtig, nicht die Datei. Verwandt: Python `open(p,'w')` ohne `newline='\n'` schreibt auf Windows tatsaechlich CRLF — beide Faelle koexistieren, der eine ist ein echter Befund, der andere ein Werkzeug-Artefakt | alle |
| 37 | 2026-09-07 | Eine bisher per .gitignore ignorierte Datei wird versioniert (Negation ergaenzt, Datei committet) — hier `lessons.md` am 2026-09-07 (#34/#64) | Auf anderen Maschinen liegt die Datei noch als ignorierte Kopie; Git ueberschreibt ignorierte Dateien beim Pull/Checkout ohne Warnung — lokal erfasste Inhalte gehen still verloren | Vor dem ersten Pull nach einer .gitignore-Negation die lokale Kopie sichern (`cp`), dann pullen und per Diff zusammenfuehren — gilt fuer jede Negation auf eine Datei, die lokal schon existiert | alle |
| 38 | 2026-09-08 | Bash-Tool unter Windows (Git Bash) — lange Heredocs mit Unicode, Backticks und gemischten Anfuehrungszeichen (Skill-Texte, Python-Skripte, Commit-Messages) | Der Heredoc bricht mit „unexpected EOF while looking for matching `''", auch mit quoted Delimiter — am 2026-09-08 dreimal, jedes Mal erst nach dem Fehlschlag umgestellt | Lange Inhalte nie inline per Heredoc — Skript oder Message per Write-Tool in den Scratchpad schreiben und per `python <datei>` bzw. `git commit -F <datei>` ausfuehren | alle |
| 39 | 2026-09-08 | `dtb:kit-sync sync` im Kit-Repo, waehrend ein Phasen-Commit lokal committet, aber noch nicht gepusht ist (Phase 2 `checkpoint-verlustfunde`) (via Checkpoint 2026-09-08) | Die Quelle des Sync ist der Clone von GitHub (`source`), nicht der lokale HEAD — bei `ahead 1` haette der Sync die Checkpoint-Kopie OHNE die gerade gebaute Phase installiert, der Wirklauf haette die alte Fassung getestet; Lock und Hash-Vergleich waeren trotzdem gruen gewesen | Vor jedem `kit-sync sync` im Kit-Repo `git status -sb` pruefen — steht der Branch auf `ahead`, erst pushen, dann syncen; die Quelle ist der gepushte Stand, nie HEAD | alle |
| 40 | 2026-09-08 | Bash-Tool unter Windows (Git Bash) — Pfad mit `~` als Argument an `python -c` durchgereicht (`L=~/.claude/dtb-lock.json`) (via Checkpoint 2026-09-08) | Bash expandiert `~` zur MSYS-Form `/c/Users/...`; das Windows-Python kann diesen Pfad nicht oeffnen → `FileNotFoundError`, obwohl die Datei existiert | Pfade mit `~` nie aus Bash an Python durchreichen — im Skript `os.path.expanduser('~/...')` aufloesen (liefert `C:\Users\...`) | alle |
| 41 | 2026-09-08 | Sammelvorlage von `dtb:workflow-checkpoint` Schritt 0 — eine Zeile mit `streiche N` abgewaehlt, danach weiterer Checkpoint in derselben Sitzung (Wirklauf 2→3, `checkpoint-verlustfunde`) (via Checkpoint 2026-09-08) | Die gestrichene Zeile gilt fuer `dtb:no-loss-check` als bewusst verworfener Fund (Randfall 3) und erscheint im naechsten Report derselben Sitzung nicht mehr — Streichen wirkt wie Verwerfen, nicht wie Vertagen | Wer einen Vorlage-Fund nur vertagen will, den `→ /dtb:…`-Befehl aus dem Report absetzen oder den Fund in der naechsten Sitzung neu finden lassen — nie streichen in der Erwartung, er komme wieder | alle |
| 42 | 2026-09-09 | Ein Waechter/Deckel prueft eine definierte Eingangsmenge; die Menge wird spaeter erweitert (via Checkpoint 2026-09-09) | Der Waechter behaelt seine alte Bedingung und schuetzt still nur noch die alte Teilmenge — belegt 2026-09-08 zweimal in einem Feature: der Struktur-Check loeste nur Quellen der dringenden Gruppe auf, die Kappung zaehlte nur dringende Funde, nachdem der Eingang auf Kann-warten-Ideen erweitert worden war | Wer den Eingang eines abgesicherten Blocks erweitert, muss Bedingung UND Zaehlgroesse der Absicherung im selben Zug mitziehen | impl-plan, implement, impl-review |
| 43 | 2026-09-09 | Einen Eintrag zwischen zwei Dateien verschieben (Befoerderung, Archivierung, Migration) (via Checkpoint 2026-09-09) | Wird zuerst an der Quelle geloescht, ist der Eintrag bei einem Abbruch dazwischen ersatzlos weg — und eine Append-only-Zusage macht den Verlust endgueltig | Immer erst am ZIEL schreiben und pruefen, dann an der QUELLE loeschen — ein Abbruch hinterlaesst dann eine erkennbare Dublette statt eines Lochs | alle |
| 44 | 2026-09-09 | Den Datei-Umgang eines Skills aendern (neue Zieldatei, neue Lesequelle) (via Checkpoint 2026-09-09) | Der Fliesstext wird umgestellt, das pipeline-Frontmatter bleibt stehen — belegt 2026-09-08: vier consumes/produces-Bloecke waren stumm zur wichtigsten neuen Datei, obwohl workflow-status, pipeline-graph und der project-health-Lint genau daraus lesen | consumes/produces im selben Zug mit dem Datei-Umgang nachziehen — das Frontmatter ist die maschinell gelesene Wahrheit, der Fliesstext nicht | impl-plan, implement, impl-review |
| 45 | 2026-09-09 | Ein Zaehlbegriff wird von mehreren Skills verwendet (z.B. 'ungesichtet', 'offen', 'faellig') (via Checkpoint 2026-09-09) | Jeder Konsument definiert ihn selbst; die Definitionen driften auseinander und der Widerspruch faellt erst im Ernstfall auf — belegt 2026-09-08: derselbe Begriff war in drei Skills verschieden gefasst, ein Zaehler haette bei maximalem Rueckstand null gemeldet | Einen von mehreren Skills geteilten Zaehlbegriff einmal an der Datenquelle festschreiben (Datei-Kopf), nicht je Konsument | alle |
| 46 | 2026-09-09 | Ein mtime-Waechter prueft vor JEDER Statusentscheidung eines Laufs auf Fremdaenderung (Lesestand-Pruefung, `dtb:idea-review`/`dtb:idea-triage`/`dtb:workflow-resume`) (via Checkpoint 2026-09-09) | Die Referenz ist die mtime vom Einlesen — nach dem ersten eigenen Schreibvorgang weicht sie ab, und der Waechter meldet ab dem zweiten Eintrag jedes Mal eine Fremdaenderung. Belegt 2026-09-09 im idea-review ueber fuenf Ideen: ab Entscheidung 2 haette die Warnung bei jeder Zeile gestanden, obwohl nur der Lauf selbst geschrieben hatte | Die Referenz-mtime nach jedem EIGENEN Schreibvorgang nachfuehren, nicht die vom Einlesen halten — sonst wird die Warnung zur Tapete und ein echter Fremdzugriff geht darin unter | alle |
| 47 | 2026-09-09 | Eine neue Zaehlgroesse in ein Dokument einfuehren, das die alte an mehreren Stellen nennt (via Checkpoint 2026-09-09) | Die alte Groesse behaelt an den nicht angefassten Stellen ihre alte Bedeutung und traegt danach zwei. Belegt 2026-09-09: der Kappungs-Fix fuehrte `{V}` (Vorlagenzeilen) ein; `{D}` steht seither an einer Stelle als Obermenge (open-question zaehlt mit, ohne in der Vorlage zu stehen) und an der anderen als Teilmenge (`{D}` = davon dringend) — die Kopfzeile las sich als „3 Eintraege (5 dringend)" | Wer eine neue Zaehlgroesse einfuehrt, definiert die alte im selben Zug an ALLEN Fundstellen neu — eine Variable mit zwei Bedeutungen faellt erst im absurden Zahlenpaar auf | impl-plan, implement, impl-review |
| 48 | 2026-09-10 | Python-HTTPS (pip, certifi-basierte Libs wie edge-tts) auf einem Rechner mit AVG „Web/Mail Shield" oder Firmen-TLS-Proxy (via Checkpoint 2026-09-10) | Der Scanner signiert HTTPS mit eigenem Stammzertifikat, dem Windows vertraut, Python aber nicht → `CERTIFICATE_VERIFY_FAILED`; `SSL_CERT_FILE` hilft nicht, weil Libs wie edge-tts `certifi.where()` fest verdrahten | Zertifikatsliste aus certifi + Windows-Stammzertifikaten bauen und per `pip --cert` bzw. Patch von `certifi.where()` vor dem Import nutzen — nie `--trusted-host`, nie Verifikation abschalten | impl-plan, debug-plan |
| 49 | 2026-09-10 | Skripte und Testdaten mit Windows PowerShell 5.1 lesen/schreiben (via Checkpoint 2026-09-10) | PS 5.1 liest BOM-lose Dateien als ANSI: `Get-Content -Raw` ohne `-Encoding UTF8` verstuemmelte „—" in einer Testfixture, der Vergleich „Einstellungen unveraendert" wurde rot, obwohl der getestete Installer korrekt war | Immer mit `-Encoding UTF8` bzw. UTF8-Encoding lesen; `.ps1` mit deutschen Meldungen als UTF-8 MIT BOM speichern; bei rotem Vergleich zuerst die Fixture pruefen (Vorher-Sicherung gegen Nachher) | impl-plan, debug-plan, impl-review |
| 50 | 2026-09-10 | Audio (MP3) aus einem PowerShell-Skript abspielen, z.B. in einem Hook-Worker (via Checkpoint 2026-09-10) | `WMPlayer.OCX` (COM) meldete Dauer 0 und spielte nicht verlaesslich — sieht aus wie ein Erfolg ohne Fehlermeldung | `System.Windows.Media.MediaPlayer` (PresentationCore) mit Position-Polling bis zur `NaturalDuration` statt WMPlayer-COM verwenden | impl-plan |
| 51 | 2026-09-10 | Testeingaben mit Backslashes (Windows-Pfade in JSON) fuer ein Skript erzeugen (via Checkpoint 2026-09-10) | Inline per bash-`printf` oder `python -c` erzeugt, gingen die Escapes je nach Shell-Schicht zweimal verloren — das ungueltige JSON erschien als Scheinfehler im getesteten Skript | Testdaten mit Backslashes als Datei per Write-Tool anlegen, nie inline durch mehrere Quoting-Schichten reichen | impl-plan, debug-plan |
| 52 | 2026-09-11 | Render-Check einer lokalen HTML-Datei per Playwright (`dtb:pipeline-graph`-Ausgabe) mit einem `python -m http.server` im Hintergrund, weil Playwright `file:` blockiert (via Checkpoint 2026-09-11) | Das Aufraeumen per `taskkill //IM python.exe` beendete ALLE Python-Prozesse des Nutzers, nicht nur den eigenen Server — der Befehl sieht nach gezieltem Stopp aus, trifft aber per Image-Name | Hintergrund-Prozesse per PID beenden (`$!` beim Start merken, dann `kill {PID}` bzw. `taskkill //PID {PID}`), nie per Image-Name oder Muster ueber alle Prozesse | alle |
| 53 | 2026-09-11 | Drei-Punkte-Vergleich von `dtb:kit-sync` mit selbst geschriebenem Hash-Skript (Repo ↔ Lock ↔ Kopie) (via Checkpoint 2026-09-11) | Beim Auflisten der Klasse-A-Dateien wurde `agents/CLAUDE.md` ausgelassen (Ausnahme aus `dtb:pipeline-graph` uebertragen) — die Datei erschien als „verwaist" und waere im sync zum Entfernen angeboten worden; zweites Auftreten nach 2026-09-08 (#82) | Die Klassen-Muster aus `dtb:kit-sync` woertlich uebernehmen (`agents/*.md` schliesst `agents/CLAUDE.md` ein), keine Ausnahmen aus anderen Skills einbauen; ein „verwaist" gegen eine Datei, die im Lock steht, zuerst als Werkzeugfehler pruefen | alle |
| 54 | 2026-09-11 | Installer/Deinstaller mit Zielverzeichnis-Parameter (`claude-code-tts`, `-ClaudeDir`) in einem Wegwerf-Ordner testen (via Checkpoint 2026-09-11) | Das Zielverzeichnis gilt nur fuer Dateien — die Startmenue-Verknuepfung ist global; die Deinstallation der Test-Installation loeschte den Stopp-Hotkey der echten Einrichtung | Globale Artefakte (Startmenue, Registry, PATH) nur anfassen, wenn sie nachweislich zu dieser Installation gehoeren (Ziel zeigt auf deren Dateien); Tests in Wegwerf-Ordnern zusaetzlich mit Schaltern gegen globale Effekte fahren (z.B. `-NoHotkey`) | impl-plan, impl-review |
| 55 | 2026-09-11 | PowerShell-Skript, das per Einzeiler direkt aus dem Netz laeuft (`claude-code-tts/install.ps1`) (via Checkpoint 2026-09-11) | `exit` im Skript schliesst das Fenster des Nutzers; `irm \| iex` laeuft im Aufrufer-Scope und veraendert dessen Variablen (z.B. `$ErrorActionPreference`) und kann keine Parameter uebergeben; ein BOM kann das Parsen des heruntergeladenen Texts stoeren | `return` statt `exit`, als `& ([scriptblock]::Create((irm URL))) -Parameter` dokumentieren statt `irm \| iex`, Datei ASCII-only bauen und im Build auf Nicht-ASCII + Parser-Fehler pruefen | impl-plan, impl-review |
| 56 | 2026-09-12 | Wirklauf, der ein Schreibverhalten eines Skills belegen soll (Sofort-Schreiben in `dtb:idea-review`, Abnahme `idea-review-sofortschreiben`) (via Checkpoint 2026-09-12) | Die Testanleitung empfahl „Behalten" und eine nicht existierende Option „Prioritaet aendern" — Behalten schreibt nichts, der Lauf endete ohne Diff und belegte das Schreibverhalten nicht; ein zweiter Lauf war noetig | Vor jeder Wirklauf-Anleitung die Aktionen des Skills nachlesen (nie aus dem Gedaechtnis) und mindestens eine Entscheidung vorgeben, die tatsaechlich schreibt; der Beleg ist erst der Diff danach | impl-review |
