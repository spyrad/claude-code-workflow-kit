# Session-Log 2026-09-11

## Session 1 - 06:05

### Implementiert
- **Archivierung (`/dtb:archive`):** `ideen-becken` (abgenommen 2026-09-09, 14/14) und `inbox-becken-migration` (8/8) per `git mv` nach `archive/`; INBOX #76, #74, #73 entfernt; 5 Zeilen im `ARCHIVE_LOG.md`. Drei Entscheidungen einzeln: #71 bleibt in der INBOX (Task `idea-review-sofortschreiben` 0/6, dessen Schritt 6 die Zeile braucht); die vier „Fertig zum Testen"-Features nicht ohne Abnahme archiviert
- **Abnahme `feature-fast`:** E2E-Probelauf #28/#25/#35 (`8c095a0`) bestanden; danach zwei echte Einsaetze — `meeting-agenda` und `feature-start-statusfeld` via Fast-Track geplant und umgesetzt
- **Abnahme `meeting-agenda`:** Probelauf mit echten Fragen (`59b3e4e`) korrekt gruppiert, Negativ-Test gegen die `[Fach]`-Doku-Zeilen aus `fachfragen-erfassung/discovery.md` bestanden, Leer-Fall im Skill definiert
- **Abnahme `output-style-gezielt`:** Formulierungen freigegeben, Kalibrierung bestanden; Stil seit 2026-08 global aktiv (`~/.claude/settings.json` `outputStyle: Gezielt`), im Kit-Repo lokal durch `i-have-adhd` (`.claude/settings.local.json`) ueberschrieben
- **`/dtb:pipeline-graph`:** `dtb-project/claude-code-workflow-kit.html` neu erzeugt (42 Skills, 10 Stages, 37 Artefakt-Typen, 34 Kanten, 4 Agenten; gitignored). Stages `capture`/`execution` und die Pfade Fast-Track/Task/Becken/Greenfield manuell ergaenzt, `flowchart.useMaxWidth: false` gesetzt → #88
- Verlustpruefung: 3 dringend → L52, #86, #87 erfasst · 2 kann warten (#88, #89 ueber die Vorlage erfasst) · 3 gefiltert

### Dateien
- `dtb-project/project-workflows/archive/ideen-becken/`, `archive/inbox-becken-migration/` - aus `features/` verschoben; `ideen-becken/spec.md` Status → Abgeschlossen
- `dtb-project/project-workflows/archive/ARCHIVE_LOG.md` - 5 Eintraege (#76, #74, #73, ideen-becken, inbox-becken-migration)
- `dtb-project/project-workflows/INBOX.md` - #76/#74/#73 entfernt (8 offen)
- `dtb-project/project-workflows/features/{feature-fast,meeting-agenda,output-style-gezielt}/spec.md` - Status → Abgenommen
- `dtb-project/project-workflows/BACKLOG.md` - Archivierte Zeilen entfernt, drei Features auf Abgenommen
- `dtb-project/project-rules/lessons.md` - L52 (Hintergrund-Prozesse per PID beenden, nie per Image-Name)
- `dtb-project/project-workflows/INBOX-BEFUNDE.md` - #86-#89

### Kontext
Aufraeumsession nach dem Resume: Der Handoff-Befehl `/dtb:commit-and-push` war bereits erledigt (`102641e` gepusht), also direkt archiviert und die seit August wartenden Features abgenommen. Nebenbei geklaert, wie die Schienen zusammenhaengen: `idea-review` schlaegt Task / Fast-Track / Voll-Schiene vor, der Mensch entscheidet; `dtb:worker` arbeitet nur `task.md` ab, nie `feature-fast`. Vorfall: Das Aufraeumen des Playwright-Testservers per `taskkill //IM python.exe` beendete alle Python-Prozesse des Nutzers → L52.

### Naechste Schritte
- [ ] `/dtb:commit-and-push` — Archivierung, Abnahmen, L52, #86-#89, Session-Log
- [ ] `/dtb:archive` — `feature-fast`, `meeting-agenda`, `output-style-gezielt` sind abgenommen
- [ ] Abnahme `feature-start-statusfeld` (5/5, Review triagiert) — Beleg noch offen
- [ ] `/dtb:implement idea-review-sofortschreiben` (0/6), danach #71 schliessen und mitarchivieren
- [ ] `/dtb:idea-triage` — 38 ungesichtet im Becken

---

## Session 2 - 08:49

### Implementiert
- **Archivierung:** `feature-fast`, `meeting-agenda`, `output-style-gezielt` nach `archive/` (`735d362`); ARCHIVE_LOG mit Abnahme-Beleg und Phasen-SHAs
- **`idea-review-sofortschreiben` erledigt (6/6):** `skills/dtb-idea-review/SKILL.md` Schritt 3 verlangt jetzt Sofort-Schreiben jeder Statusaenderung (entscheiden → schreiben → naechste Idee, #71) und beim Zusammenlegen Ziel vor Quelle (L43); Schritt 4 zeigt die Zusammenfassung auch nach Abbruch, mit Abbruch-Zeile. Eigene knappe Fassung, 0 woertliche Saetze aus `dtb:idea-triage`. Commits `e4e436d`, `7987282`; `/dtb:kit-sync sync` 1 Update, Lock 48 @ `e4e436d`; INBOX #71 mit Umsetzungsvermerk. Umgesetzt ueber die Escape-Hatch von `/dtb:implement` (`## Schritte` als Plan) → #90
- **Stopp-Hotkey fuer die Sprachausgabe:** Startmenue-Verknuepfung mit **Strg+Alt+S** ruft `speak-last-answer.ps1 -Stop` (PID-basiert) — stoppt das Vorlesen in jedem Fenster
- **Neues oeffentliches Repo `github.com/spyrad/claude-code-tts`** (separat vom Kit, `Projekte/claude-code-tts`): Einzeiler-Installer `& ([scriptblock]::Create((irm …/install.ps1)))` + `install.cmd`, Englisch, Stimme nach Windows-Sprache (de → Katja/Hedda, sonst Aria/Zira) ueber `tts/voice.json`, `-Voice`/`-FallbackVoice`/`-OfflineOnly`/`-StopHotkey`/`-NoHotkey`/`-Uninstall`, Migration der alten Verknuepfung, README mit Datenschutz-Hinweis, MIT. Commits `46fc936`, `bcdbf2e`. Getestet: Wegwerf-Ordner (Install 2x idempotent, fremder Hook bleibt, Uninstall restlos), Worker liest `voice.json`, Einzeiler direkt von GitHub
- **Fehler beim Test und Fix (`bcdbf2e`):** Deinstallation einer Test-Installation loeschte die globale Verknuepfung der echten Einrichtung → Verknuepfungen werden nur noch entfernt, wenn sie auf das Hook-Skript derselben Installation zeigen; beide Faelle getestet, echte Verknuepfung wiederhergestellt
- Verlustpruefung: 5 dringend → L53, L54, L55, #90, #91 erfasst · 0 kann warten · 6 gefiltert (erster Checkpoint-Anlauf dieser Session abgebrochen, Funde dort nicht geschrieben)

### Dateien
- `skills/dtb-idea-review/SKILL.md` - Schritt 3 Sofort-Schreiben, Schritt 4 Abbruch-Zeile
- `dtb-project/project-workflows/features/idea-review-sofortschreiben/task.md` - 6/6 mit SHA, Status Erledigt
- `dtb-project/project-workflows/INBOX.md` - #71 Umsetzungsvermerk
- `dtb-project/project-workflows/archive/{feature-fast,meeting-agenda,output-style-gezielt}/` - archiviert
- `dtb-project/project-rules/lessons.md` - L53-L55
- `dtb-project/project-workflows/INBOX-BEFUNDE.md` - #90, #91
- ausserhalb des Kit-Repos: `~/.claude/hooks/speak-last-answer.ps1` (englische Fassung, liest `voice.json`), `~/.claude/tts/voice.json`, Startmenue `Claude stop reading.lnk`; `~/.claude/settings.json` unveraendert

### Kontext
Zweite Tageshaelfte: letzte offene Aufgabe umgesetzt und verteilt, danach die Sprachausgabe (#41) fuer andere nutzbar gemacht. Entscheidung: eigenes oeffentliches Repo auf Englisch statt Kit-Baustein (#91), weil Hooks/tts keine Klasse-A-Artefakte sind und das Tool unabhaengig vom Kit nuetzt. Beim Checkpoint zeigte sich zweimal eine Ableitungsluecke fuer Aufgaben (`/dtb:implement` fuer `task.md`) → #90.

### Naechste Schritte
- [ ] Wirklauf `idea-review-sofortschreiben`: `/dtb:idea-review` starten, nach der 2. Entscheidung abbrechen, beide in `INBOX.md` pruefen → Beleg fuer die Abnahme
- [ ] Abnahme `feature-start-statusfeld` (5/5, Review triagiert)
- [ ] `/dtb:idea-triage` — 40 ungesichtet, darunter #85 (mit #91 verwerfen), #81
- [ ] Veraltete Dateien loeschen: `Desktop\install-claude-tts.ps1`, `~/.claude/tts/install-template.ps1`, `~/.claude/tts/build-installer.ps1`
- [ ] Sprachausgabe auf dem Arbeitsrechner per Einzeiler aus `claude-code-tts` installieren (ggf. `-OfflineOnly`)
