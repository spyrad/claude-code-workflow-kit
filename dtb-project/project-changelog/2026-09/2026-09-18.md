# Session-Log 2026-09-18

## Session 1 - 15:46

### Implementiert
- **Resume:** kein aktives Feature (`features/` leer), Git sauber und synchron mit `origin/master`
- **Idee #97 erfasst:** `/loop` im DTB-Workflow als getakteter Lese-Lauf fuer die Herdr-/Pane-Ueberwachung — Beleglage: `dtb:pane-start` haelt fest „Es laeuft kein blockierender Warteprozess", das Nachfragen ist Handarbeit
- **`dtb:worker` durchgesprochen:** Modus-Weiche (Sicht ohne Argument / Ausfuehrung mit `{slug}` oder `liste` — ein Oder, kein Zweiwort), Traeger-Weiche `subagent` (Default, keine Pane) vs. `pane`, Herkunft beider Pane-IDs (`$HERDR_PANE_ID` in den Auftrag, `pane split`-Antwort als Ziel), Quellen (INBOX + `features/*/task.md` mit `## Schritte`)
- **`/dtb:worker` Modus 1:** 7 Eintraege geprueft — 0 ja / 0 teilweise / 7 nein; alle offenen Ideen sind Grundsatz-/Designfragen, `features/` ist leer, es existiert kein `task.md`
- **Autopilot-Analyse fuer die Voll-Schiene:** harte Wand ist `dtb:implement` („Kein autonomer Durchlauf: Das manuelle Gate wird IMMER gestellt", `SKILL.md:215`), nicht Herdr — der Transport funktioniert bereits. Zwei getrennte Sperren herausgearbeitet: Zustellungs-Sperre (Slash-Befehl per `herdr agent prompt` kommt als Chat-Text an) und Frontmatter-Sperre (36 von 43 Kit-Skills `disable-model-invocation: true`, modell-aufrufbar nur die 7 read-only-Sichten)
- **Harness-Pruefung (Subagent `claude-code-guide`):** kein settings.json-Key, kein CLI-Flag, keine Umgebungsvariable und kein Modus hebt `disable-model-invocation` auf; Gegenstueck `user-invocable: false` (nur Modell, nicht Mensch) existiert; Anthropic sieht fuer zwei Varianten desselben Skills nichts vor
- **`skillOverrides`-Test durchgefuehrt und negativ:** `"on"` fuer `dtb:pipeline-graph` (Frontmatter-Name) und `dtb-project-health` (Verzeichnisname) in `.claude/settings.local.json`, geprueft in einer frischen Pane — unveraenderte 7er-Liste, beide Schreibweisen wirkungslos. Testkonfiguration danach vollstaendig zurueckgebaut
- **Zwischenfall:** `settings.local.json` beim Setzen der Testkonfiguration per Heredoc ueberschrieben, Backslashes wurden halbiert, JSON ungueltig — Claude Code verwarf still `additionalDirectories` und `outputStyle`; aus dem Backup wiederhergestellt, zweiter Anlauf per `node`-Parse/Serialisierung erfolgreich
- **Idee #98 erfasst:** Auto-Mode/Autopilot fuer die Voll-Schiene mit der kompletten Befundlage als Vorarbeit (7 Befunde, 3 Stufen, 7 offene Punkte a-g) — Stufe 3 (ab Idee ohne Rueckfrage) bewusst nicht das Ziel
- Verlustpruefung: 2 dringend → L66, L67 erfasst · 1 kann warten (Befehl im Report) · 3 gefiltert

### Dateien
- `dtb-project/project-workflows/INBOX.md` - Ideen #97 (`/loop` im Workflow) und #98 (Auto-Mode/Autopilot) ergaenzt, jetzt 8 offene Ideen
- `dtb-project/project-rules/lessons.md` - L66 (JSON nie per Heredoc schreiben) und L67 (Settings-Aenderung nie mit `/clear` pruefen)
- `.claude/settings.local.json` - Testkonfiguration `skillOverrides` gesetzt und wieder entfernt; netto unveraendert (ungetrackt)

### Kontext
Die Sitzung hat den Autopilot-Gedanken von der Idee bis zur belastbaren Machbarkeitsaussage getrieben. Kernergebnis: Der Engpass ist nicht die Technik — Pane-Erzeugung, Auftragszustellung und Rueckkanal sind seit 2026-08-16 belegt — sondern die Governance und das fehlende Antwortgeber-Muster an den Interview-Stellen der Kette. Die Harness-Pruefung schliesst den bequemsten Weg aus: es gibt keinen Schalter fuer `disable-model-invocation`, weder dokumentiert noch im eigenen Test. Damit bleibt nur ein Bau im Repo (Verweis-Huelle als eigenes Klasse-A-Artefakt) oder der stille Weg ueber den Skill-Koerper als Prompt-Text — und Letzterer wuerde eine Regel stehen lassen, die nichts mehr bedeutet. Beide Ideen (#97 Taktgeber, #98 Rahmen) sind erfasst und verweisen aufeinander; zusammen mit #57 bilden sie eine Entscheidungsgruppe.

### Naechste Schritte
- [ ] `/dtb:idea-review` fortsetzen — pausiert bei Schritt 2 von 4, inzwischen 8 offene Ideen
- [ ] #97, #57 und #98 zusammen entscheiden — getrennt entschieden entstehen drei widerspruechliche Autonomie-Begriffe
- [ ] `/dtb:idea-triage` — 46 ungesichtet
- [ ] Lektion selbst erfassen (kann warten): Slash-Befehl = Text-Injektion des Skill-Koerpers, `disable-model-invocation` bremst nur den Selbstaufruf
