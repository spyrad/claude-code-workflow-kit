# Session-Log 2026-09-10

## Session 1 - 21:09

### Implementiert
- **INBOX #41 (Sprachausgabe) lokal umgesetzt** — ausserhalb des Kit-Repos als User-Config unter `~/.claude/`:
  - Stop-Hook liest die letzte Antwort vor (ganze Antwort, Codebloecke raus, Tabellenzeilen als Saetze, Cap `$MaxChars = 3000`); UserPromptSubmit-Hook bricht die Ausgabe beim Abschicken ab (`-Stop`)
  - Stimme **Katja** (`de-DE-KatjaNeural` via edge-tts 7.2.8) mit Stueck-Pipeline (erstes Stueck 200 Zeichen, dann 450; naechstes Stueck wird erzeugt, waehrend das vorige laeuft) — gemessen: Start 2 s nach Antwortende, keine Luecke zwischen Stuecken
  - **Hedda** (Windows SAPI) springt automatisch ein, wenn Katja nicht verfuegbar ist (kein Python, Synthese-Timeout 10 s, Wiedergabefehler); getestet mit absichtlich kaputter Stimme
  - AVG-TLS-Scanning geloest ohne AVG-Ausnahme: Zertifikatsliste certifi + Windows-Stammzertifikate, `certifi.where()` im Wrapper vor dem Import gepatcht
  - Aus-Schalter: Datei `~/.claude/tts-off`; Protokoll: `%TEMP%\claude-tts\speak.log`
- **Installer fuer weitere Rechner** `install-claude-tts.ps1` (Desktop): prueft Stimme, schreibt Skripte, sucht Python (ueberspringt Store-Stub), baut CA-Bundle, installiert edge-tts, traegt Hooks idempotent in `settings.json` ein (Backup, fremde Hooks bleiben); getestet in 4+3 Szenarien, eingebettete Skripte byte-gleich (normalisiert) mit den Quellen
- Verlustpruefung: 5 dringend → L48, L49, L50, L51, #85 erfasst · 0 kann warten (Befehle im Report) · 0 gefiltert

### Dateien
- `~/.claude/hooks/speak-last-answer.ps1` - neu: Hook-Skript (Modi Hook/-Stop/-DryRun/-Worker, Engine edge/sapi)
- `~/.claude/tts/edge_say.py` - neu: edge-tts-Wrapper mit CA-Bundle-Patch
- `~/.claude/tts/ca-bundle.pem` - neu: certifi + AVG-Root (lokal)
- `~/.claude/tts/install-template.ps1`, `~/.claude/tts/build-installer.ps1` - neu: Installer-Vorlage + Bau-Skript
- `~/.claude/settings.json` - Stop-Hook ("Task completed") ersetzt, UserPromptSubmit-Hook neu
- `dtb-project/project-rules/lessons.md` - L48-L51 (AVG/certifi, PS-5.1-BOM, MediaPlayer, Testdaten mit Backslashes)
- `dtb-project/project-workflows/INBOX-BEFUNDE.md` - #85 (#41 lokal umgesetzt, Kit-Uebernahme offen)

### Kontext
Nutzer wollte Claude-Ausgaben vorgelesen bekommen (ADHS-Hilfe). Entscheidung: zuerst lokale User-Config statt Kit-Baustein — Hooks und `~/.claude/tts/` sind kein Klasse-A-Artefakt von `dtb:kit-sync`, der Verteilungsweg gehoert zu #45. Nebenbefund: `INBOX.md` wurde heute 18:18 ausserhalb der Session von einem Editor umformatiert (Spalten-Padding + LF, Inhalt/Status unveraendert) — nicht angefasst.

### Naechste Schritte
- [ ] `/dtb:commit-and-push` — lessons.md, INBOX-BEFUNDE.md, Session-Log, WORKFLOW_STATUS.md; INBOX.md-Umformatierung bewusst mitnehmen oder verwerfen
- [ ] Installer auf dem Arbeitsrechner ausfuehren und `/hooks` oeffnen
- [ ] #41-Status nachziehen und Kit-Uebernahme via #45 klaeren (`/dtb:idea-triage` fuer #85)
