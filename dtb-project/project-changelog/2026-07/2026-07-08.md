# Session-Log: 2026-07-08

## Session 1 - 09:47 (aktualisiert 10:12)

### Implementiert
- Alle 28 dtb-Skills nach `~/.claude/skills/` synchronisiert (14 waren veraltet, byteweise verifiziert) — offene Aufgabe aus Session 2026-07-06 erledigt
- Praxiserprobung Derived State mit den NEUEN Skill-Kopien: `/dtb:workflow-resume` und `/dtb:workflow-next Derived State` haben den Status korrekt aus den `## Progress`-Checkboxen abgeleitet (16/16 → "Fertig zum Testen"), Regel-Datei wurde gelesen, Konfliktpruefung lief (Ergebnis: konsistent)
- **Abnahme des Features DERIVED_STATE erklaert** (explizite Nutzer-Entscheidung nach Regel-Datei §1.2): Status in `FEATURE_DERIVED_STATE.md` auf "Abgenommen (2026-07-08)" gesetzt

### Dateien
- `~/.claude/skills/dtb-*/SKILL.md` - 28 installierte Skill-Kopien mit Repo synchronisiert (ausserhalb des Repos)
- `dtb-project/project-workflows/features/FEATURE_DERIVED_STATE.md` - Status → Abgenommen
- `dtb-project/project-workflows/BACKLOG.md` - Status-Spalte synchronisiert (Abgenommen)
- `dtb-project/project-workflows/WORKFLOW_STATUS.md` - Dashboard aktualisiert

### Kontext
Wichtige Erkenntnis beim Sync: Die Praxiserprobung vom Resume zu Session-Beginn lief noch mit
den ALTEN installierten Skill-Kopien — die Ableitung funktionierte nur dank CLAUDE.md-Vorwissen.
Erst nach dem Sync wurde der Mechanismus mit der echten Skill-Version verifiziert (workflow-next
liest DERIVED_STATE_RULES.md, zaehlt Checkboxen, prueft Konflikte). Das bestaetigt den
Idee-#4-Schmerz (Drift zwischen Repo und installierten Kopien) erneut.

### Entscheidungen
- Abnahme DERIVED_STATE explizit erklaert nach Praxistest mit den neuen Skill-Kopien
- Archivierung bewusst verschoben: DERIVED_STATE bleibt als "Abgenommen" im BACKLOG stehen
  und wird im Batch zusammen mit dem naechsten fertigen Feature archiviert (kein Blocker,
  archive sammelt Kandidaten bei jedem Aufruf neu)

### Naechste Schritte
- [ ] Ideen #2-#7 in INBOX triagieren (/dtb:idea-review) — Idee #4 (skills-lock.json) durch heutigen Sync-Fall erneut motiviert; #7 durch Abnahme von #1 formal freigeschaltet
- [ ] Naechstes Feature bearbeiten, danach `/dtb:archive` im Batch (DERIVED_STATE + neues Feature)

---

## Session 2 - 13:33

### Implementiert
- Feature **KIT_SYNC** komplett umgesetzt (10/10 Schritte, 3 Phasen, Commits `6ec445b`..`ec0d167`): Distributionsmodell light — Drift zwischen Kit-Repo und installierten Kopien wird erkannt und kontrolliert abgeglichen
- Pipeline-Durchlauf: idea-review (#4 ausgewaehlt) → feature-discover → feature-plan → impl-plan → plan-review (3 Agenten, 3 Entscheidungen, 6 Empfehlungen) → feature-start → Umsetzung im 3x3-Rhythmus
- Neuer Skill `skills/dtb-kit-sync/SKILL.md`: Lock-Datei `~/.claude/dtb-lock.json` (Lock+Manifest kombiniert), Drei-Punkte-Vergleich GitHub ↔ Lock ↔ Kopie mit 8 Zustaenden, Modi check/sync/install, 10 Randfaelle, feste Bash-Sequenzen als Determinismus-Anker
- Hash-Spike: `git hash-object` allein ist config-abhaengig (autocrlf) — verbindliche Methode `tr -d '\r' | git -c core.autocrlf=false hash-object --stdin`
- project-init: Sentinel-Marker `<!-- BEGIN dtb -->…<!-- END dtb -->` fuer idempotente CLAUDE.md-Bloecke + Ebenen-Trennung (Maschinen- vs. Projekt-Ebene); project-health: Check 11 Kit-Drift (netzwerkfrei, read-only)
- Doku: README-Installation auf kit-sync umgestellt (mit Bootstrap-Schritt), Distributionsmodell in CLAUDE.md + skills/CLAUDE.md, repo-sync-Abgrenzung; pipeline-graph regeneriert (29 Skills, dtb-lock.json-Spalte automatisch)

### Testprotokoll (unabhaengiger Agent ohne Session-Kenntnis, 4 Szenarien)
| Szenario | Erwartet | Beobachtet | Ergebnis |
|---|---|---|---|
| A: check auf Fixture-Set (7 Artefakte) | alpha=Update, beta=lokal geaendert, gamma=Konflikt, delta=synchron (CRLF), testcmd=neu, orphan=verwaist, tester=synchron | exakt so, Report im fixen Format | ✅ SC2-SC6 |
| B: korruptes Lock | wie fehlend, install anbieten, kein Absturz | exakt so (+ .bak) | ✅ |
| C: Quelle offline, kein localPath | Meldung im Wortlaut, kein Abbruch | exakt so | ✅ SC7 |
| D: Sentinel-Idempotenz | nur Block zwischen Markern ersetzt | Nutzertext byte-identisch | ✅ SC9 |

Agent fand 5 Mehrdeutigkeiten (neu/fehlt-Praezedenz, Verwaist-Regel ohne dtb-Praefix, local-path-Quelle, N undefiniert, read-only vs. Backup) — alle in der Anleitung nachgeschaerft (`2a0eddf`).

### Realtest (Success Criterion 1)
`install` auf dem echten Bestand: 34 Artefakte (29 Skills, 4 Agents, 1 Command) — 1 kopiert (kit-sync selbst), 30 adoptiert, 3 mit Abweichung. `sync`: 2 Updates uebernommen, `dtb-pitch-coach.md` bewusst uebersprungen (installierte Version referenziert oma-personas-Repo — INBOX #8). Finaler check: 33 synchron, 1 bewusster Skip. Dogfooding: repo-sync-Doku-Aenderung wurde direkt per kit-sync-Abgleich nachgezogen (Lock auf `ec0d167`).

### Dateien
- `skills/dtb-kit-sync/SKILL.md` - neu (Kern des Features)
- `skills/dtb-{project-init,project-health,repo-sync}/SKILL.md` - Sentinel/Ebenen-Trennung, Check 11, Abgrenzung
- `dtb-project/project-workflows/features/{DISCOVERY,FEATURE,PLAN}_KIT_SYNC.md` - neu (10/10 SC abgehakt)
- `README.md`, `CLAUDE.md`, `skills/CLAUDE.md` - Distributionsmodell dokumentiert
- `~/.claude/dtb-lock.json` - real erzeugt (34 Artefakte, ausserhalb des Repos)
- `dtb-project/project-workflows/INBOX.md` - #4 Ausgearbeitet; NEU: #8 (Agents-Pfad/Persona-Quellen), #9 (Frontmatter-Hygiene)

### Kontext
Ausloeser: Idee #4 (10x-Analyse), Schmerz zweifach belegt (Sync-Faelle 2026-07-03 und 2026-07-08 — heute Morgen lief workflow-resume unbemerkt mit veralteter Skill-Kopie). Kern: Lock-Datei als dritter Fixpunkt macht die Drift-RICHTUNG diagnostizierbar (Update vs. lokale Aenderung vs. Konflikt) — ein Zwei-Wege-cmp kann das nicht.

### Entscheidungen
- Klasse A v1 = skills+agents+commands (alle global unter `~/.claude/`, verifiziert); Personas raus (externe oma-personas-Referenz, Quellen-Klaerung = INBOX #8); DERIVED_STATE_RULES.md bleibt Seed
- Konflikt-Ast schlank (melden + einfache Wahl statt Diff-Dialog) — Nutzer editiert installierte Kopien nie lokal
- Hashing per Spike entschieden statt Annahme geglaubt (Review-Befund Senior Dev bestaetigt)
- `ref` dynamisch beim install erfassen statt hartkodiert; GitHub = kanonische Quelle, localPath = Offline-Fallback
- Fixture-Abnahme verbindlich durch unabhaengigen Agenten (Muster DERIVED_STATE)
- Feature-Zustand: **Fertig zum Testen** — Abnahme nach Praxiserprobung (naechstes echtes Kit-Update via check → sync)

### Naechste Schritte
- [ ] KIT_SYNC in der Praxis erproben (naechstes Kit-Update ueber /dtb:kit-sync check → sync), dann Abnahme
- [ ] Nach Abnahme: /dtb:archive im Batch (DERIVED_STATE + KIT_SYNC), raeumt auch INBOX #1/#4 auf
- [ ] Ideen #2, #3, #5-#9 triagieren (/dtb:idea-review) — Empfehlung Reihenfolge: #2 → #3 → #5/#6 → #7 zuletzt
- [ ] `cognitive/`-Overlays der dtb-stakeholder-pitch-coach-Persona befuellen (vorher Brainstorming)
- [ ] Optional: Description-Optimierungs-Loop auf macOS/Linux/WSL nachholen
- [ ] `cognitive/`-Overlays der dtb-stakeholder-pitch-coach-Persona befuellen (vorher Brainstorming)
- [ ] Optional: Description-Optimierungs-Loop auf macOS/Linux/WSL nachholen

---

## Session 3 - 13:59

### Implementiert
- **KIT_SYNC praxiserprobt (Abnahme-Grundlage):** vollstaendiger Zyklus `/dtb:kit-sync check` → `sync` gegen die kanonische GitHub-Quelle. Clone lieferte `master`@`5ca0c53` (Lock stand auf `ec0d167`) — Drei-Punkte-Vergleich korrekt: 33 Artefakte synchron trotz weitergezogenem Repo-Stand (Checkpoint-Commit `5ca0c53` beruehrte nur `dtb-project/`), 1 Divergenz (`commands/dtb-pitch-coach.md`) praezise als "Update verfuegbar" (R≠L, K=L) diagnostiziert
- **sync-Lauf:** pitch-coach bewusst uebersprungen (installierte oma-personas-Kopie, INBOX #8) → 0 uebernommen, Lock unberuehrt gelassen (wie zugesagt); kein stillschweigendes Ueberschreiben
- **Beide Features abgenommen** (explizite Nutzer-Entscheidung, Regel §1.2): KIT_SYNC + DERIVED_STATE → Status in FEATURE-Specs, BACKLOG, WORKFLOW_STATUS synchronisiert
- **Batch-Archivierung** (`/dtb:archive`): 2 Feature-Sets (je FEATURE+DISCOVERY+PLAN = 6 Dateien) per `git mv` nach `archive/` verschoben, Status → Abgeschlossen; INBOX #1/#4 entfernt (IDs bleiben Luecken); ARCHIVE_LOG.md angelegt; BACKLOG "Aktive Features" geleert + beide unter "Abgeschlossen"; WORKFLOW_STATUS auf "Pipeline leer" gesetzt
- **Idee #10** in INBOX erfasst: kit-sync `pinned/hold`-Zustand (bewusst gehaltene Abweichungen aus dem Sammel-Ja von Buendel 1 heraushalten)

### Dateien
- `dtb-project/project-workflows/archive/{FEATURE,DISCOVERY,PLAN}_{DERIVED_STATE,KIT_SYNC}.md` - 6 Dateien archiviert (git mv, Historie erhalten)
- `dtb-project/project-workflows/archive/ARCHIVE_LOG.md` - neu (append-only)
- `dtb-project/project-workflows/INBOX.md` - #1/#4 archiviert, #10 neu
- `dtb-project/project-workflows/BACKLOG.md` - aktive Features geleert, 2 unter Abgeschlossen
- `dtb-project/project-workflows/WORKFLOW_STATUS.md` - Dashboard bereinigt (Pipeline leer)

### Kontext
Abschluss-Session der zwei parallelen Features. KIT_SYNC-Praxiserprobung bestaetigt: der Drei-Punkte-Vergleich unterscheidet "Repo weitergezogen" sauber von "lokal geaendert", und der weitergezogene Repo-Stand (`5ca0c53`, nur Projektdateien) fuehrt korrekt NICHT zu falschen Klasse-A-Drifts. Einzige Reibung: eine bewusst gehaltene Abweichung ist mechanisch nicht von einem echten Update unterscheidbar → als Idee #10 dokumentiert (kein Blocker).

### Entscheidungen
- KIT_SYNC abgenommen nach Praxiserprobung check → sync (nicht nur Fixture-/Realtest aus Session 2)
- pitch-coach weiterhin nicht ueberschreiben, solange INBOX #8 (Persona-Quellen) offen ist
- Beide Features im Batch archiviert; Pipeline damit leer — naechstes Feature aus Ideen-Triage

### Naechste Schritte
- [ ] Ideen #2, #3, #5-#10 triagieren (/dtb:idea-review) — Reihenfolge: #2 → #3 → #5/#6 → #7 zuletzt; #10 neu
- [ ] `cognitive/`-Overlays der dtb-stakeholder-pitch-coach-Persona befuellen (vorher Brainstorming)
- [ ] Optional: Description-Optimierungs-Loop auf macOS/Linux/WSL nachholen

---

## Session 4 - 16:20

### Implementiert
- **Feature LESSON_LOOP** komplett umgesetzt (15/15, Commits `4b4b080`/`4117563`/`4ee4a9a`/`52f20e9`): neuer Skill `dtb:lesson` (append-only `lessons.md` mit Context/Problem/Rule/Applies-to, zwei Eingangskanäle manuell + Agent-Vorschlag, immer mit Bestätigung) + Prior-Verdrahtung in impl-plan/debug-plan/code-review/plan-review; löst das tote `pitfalls.md`-Konzept ab
- Pipeline-Durchlauf: idea-review (#2) → feature-discover → feature-plan → impl-plan → plan-review → feature-start → 3x3-Umsetzung; 4 Review-Empfehlungen eingearbeitet (Globber-Audit, kit-sync-Schritt, Abnahme-Isolation, Konflikt-Definition)
- **code-review-Abgrenzung:** `lessons.md` aus dem `project-rules/*.md`-Rules-Glob ausgeschlossen + separater Prior-Schritt; project-health Check 10 nimmt `lessons.md` + `DERIVED_STATE_RULES.md` aus
- **Isolierte Abnahme** durch unabhängigen Agenten (8/8 Szenarien bestanden), 4 Findings behoben (Pipe-Escaping, leer-Definition, Konflikt-Klausel, Edit-Tool statt Write)
- **kit-sync** check → sync: `dtb:lesson` + `betriebs-waechter` global installiert, 10 Skills/Agents aktualisiert, Lock auf `4ee4a9a`; `dtb-pitch-coach` bewusst übersprungen (INBOX #8)
- **Betriebs-Wächter-Feature** (parallel, Commit `4ee4a9a`): bedingter 4. plan-review-Agent (Ops/Security, Schritt 2c) + Rollen-Schärfung (Architekt Makro / Senior Dev Mikro / Pragmatiker Spec-Deckung)
- **WORKLOG** aktualisiert: 6 neue Prinzipien (`2b077d5`) + Altbeiträge auf echte Umlaute normalisiert (`044d97e`)

### Dateien
- `skills/dtb-lesson/SKILL.md` (neu) + impl-plan/debug-plan/code-review/plan-review/project-health/pipeline-graph — Prior-Wiring & Abgrenzung
- `agents/betriebs-waechter.md` (neu) + architekt/pragmatiker/senior-dev/CLAUDE.md — Rollen-Schärfung
- `frameworks/.../pitfalls.md` — Ablösungs-Hinweis; `CLAUDE.md`, `README.md` — Skill-Doku
- `dtb-project/.../features/{DISCOVERY,FEATURE,PLAN}_LESSON_LOOP.md` (neu)
- `~/.claude/` (global): dtb-lesson + betriebs-waechter installiert, `dtb-lock.json` auf `4ee4a9a`
- `WORKLOG.md` — Prinzipien + Umlaut-Normalisierung

### Kontext
LESSON_LOOP verdrahtet das seit ~4 Monaten tote `pitfalls.md`-Konzept zu einem echten Lern-Feedback-Loop (Erfassen + maschinell gelesener Prior). Parallel lief ein zweites Feature (Betriebs-Wächter) an teils denselben Dateien (`plan-review`) — konsequent in getrennte, thematisch saubere Commits gehalten. Alles committet, gepusht (`044d97e`) und global synchronisiert.

### Entscheidungen
- LESSON_LOOP **abgenommen** nach isolierter Fixture-Abnahme (§1.2)
- Betriebs-Wächter als eigenes Feature erfasst — entspricht NICHT dem ursprünglichen INBOX #3 (Challenger-Pass, Dimensions-Verdikte, code-review-Achsen bleiben offen)
- pitch-coach weiter nicht überschreiben (INBOX #8 offen)

### Nächste Schritte
- [ ] `/dtb:archive` im Batch: LESSON_LOOP (abgenommen) — räumt auch INBOX #2 auf
- [ ] Ideen triagieren: #3 (Rest-Scope nach Betriebs-Wächter), #5, #6, #7, #8, #9, #10
- [ ] `cognitive/`-Overlays der dtb-stakeholder-pitch-coach-Persona (vorher Brainstorming)

---

## Session 5 - 20:23

### Implementiert
- **LESSON_LOOP archiviert** (`/dtb:archive`): FEATURE + DISCOVERY + PLAN per `git mv` nach `archive/` verschoben (Historie erhalten), `**Status:** Abgeschlossen` in der Feature-Spec gesetzt — die Archivierung ist der explizite Abschluss-Akt (Regel §1.2)
- **INBOX #2** (Ausgearbeitet → FEATURE_LESSON_LOOP.md) entfernt; IDs #3/#5/#6/#7/#8/#9/#10 unverändert (Nummern sind IDs, keine Neuvergabe)
- **BACKLOG** synchronisiert: „Lesson Loop" aus „Aktive Features" → „Abgeschlossen" (Pipeline damit leer)
- **WORKFLOW_STATUS** bereinigt (Pipeline leer, Archiv-Aufgabe aus offenen Aufgaben entfernt); **ARCHIVE_LOG** um 2 Zeilen ergänzt (Feature LESSON_LOOP + Idee #2)

### Dateien
- `dtb-project/project-workflows/archive/{FEATURE,DISCOVERY,PLAN}_LESSON_LOOP.md` - 3 Dateien archiviert (git mv)
- `dtb-project/project-workflows/archive/ARCHIVE_LOG.md` - LESSON_LOOP + Idee #2 angehängt
- `dtb-project/project-workflows/{INBOX,BACKLOG,WORKFLOW_STATUS}.md` - bereinigt/synchronisiert

### Kontext
Abschluss von LESSON_LOOP (in Session 4 abgenommen). Pipeline ist danach leer — nächstes Feature kommt aus der Ideen-Triage. Reibung: Der Sicherheits-Klassifikator für Shell-Befehle war zeitweise ausgefallen; alle Datei-Inhalte wurden per Write/Edit vorbereitet, das `git mv` musste der Nutzer via `!`-Prefix ausführen. Kein Datenverlust, alles konsistent.

### Nächste Schritte
- [ ] Archivierung committen (4 geänderte + 3 verschobene Dateien)
- [ ] Ideen triagieren: #3 (Rest-Scope nach Betriebs-Wächter), #5, #6, #7, #8, #9, #10 — `/dtb:idea-review`
- [ ] `cognitive/`-Overlays der dtb-stakeholder-pitch-coach-Persona (vorher Brainstorming)
