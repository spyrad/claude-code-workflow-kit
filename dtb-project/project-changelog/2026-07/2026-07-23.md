# 2026-07-23

## Session 1 - 16:17

### Implementiert
- **Personas + `dtb-pitch-coach`-Command aus dem Kit entfernt** (`7c3272a`, 324 Zeilen weniger).
  Anlass war ein echter Halbzustand: der global installierte Command referenziert
  `@personas/…`, aber `~/.claude/personas/` existiert nicht und stand in keinem Lock-Eintrag —
  ausserhalb des Kit-Repos lief er ins Leere. Verlustpruefung vorab: die beiden Dateien sind
  **byte-identisch** (EOL-normalisiert) mit `oma-personas/persona-stakeholder-pitch-coach/`,
  dem dedizierten Persona-Repo (6 Personas) — die Kit-Kopie war reine Duplikation.
- **Konzept-Klaerung als Grundlage der Entscheidung:** Drei Konstrukte sauber getrennt —
  **Agent-Rollen** (`agents/*.md`, eigene Datei, eigene Stimme, diskutieren im `plan-review`),
  **Linsen** (`principled`/`torvalds`, inline im `impl-review`-Craft-Prompt, klassifizieren nur,
  kein Dialog) und **Personas** (`COGNITIVE.md` mit Innenwelt, arbeiten mit dem Menschen).
  Merksatz: Rollen diskutieren, Linsen klassifizieren. Das Kit **destilliert** Personas
  (die Linsen sind Destillate von `persona-principled-reviewer`/`persona-torvalds-reviewer`
  aus `oma-personas`), es hostet sie nicht — die Entfernung macht das Muster konsistent.
- **Doku bereinigt:** `CLAUDE.md` + `README.md` (Personas-Abschnitte ersetzt, `commands/`-Konzept
  inkl. Klasse-A-Muster bewusst erhalten — aktuell ohne ausgelieferten Command). Zwei **tote
  INBOX-#8-Verweise** entfernt (`skills/CLAUDE.md`, `dtb-kit-sync/SKILL.md` — beide begruendeten
  den Persona-Ausschluss mit einer Klaerung, die seit 2026-07-13 abgeschlossen ist).
- **`plan.md`-Status open-question korrigiert** (`0913fc0`): stand auf `Entwurf`, obwohl das
  plan-review am 2026-07-19 durchlief (REVISE, 3 WARNs behoben). Folge des Fehlstands:
  `workflow-next`/`workflow-status` meldeten ein offenes Plan-Review fuer ein 9/9-Feature.
- **Nachgetragener Checkpoint S2** (`22b962f`): der am 2026-07-22 geschriebene Session-Log +
  Status/Backlog-Sync war nie committet worden.
- **`kit-sync check` + `sync` durchgefuehrt:** 4 uebernommen (`dtb-kit-sync`, `dtb-open-question`,
  `dtb-project-init` als Updates; **`dtb-meeting-dump` erstmals global installiert** — es fehlte
  komplett, obwohl der Commit vom 2026-07-21 „global verteilt" heisst), 1 verwaiste Kopie entfernt
  (`commands/dtb-pitch-coach.md`). Lock: `sourceCommit 7c3272a`, `updatedAt 2026-07-23`,
  41 Klasse-A-Artefakte, Verifikations-check 0 Abweichungen.
- **Idee #10 verworfen + archiviert:** Faktencheck zeigte, dass der Ausloeser entfallen ist —
  die pitch-coach-Divergenz existiert nicht mehr (installierte Kopie zeigte laengst auf
  `@personas/…`, der Rest war ein reines CRLF/LF-Artefakt), und das Lock fuehrt 41x `synced`
  ohne einen einzigen bewusst abweichenden Kandidaten.
- **Zwei neue Ideen erfasst:** **#30** (`plan.md`-Kopf-Statusfeld ohne schreibenden Pfleger +
  fehlender Checkbox-Guard in `workflow-next:53`), **#31** (`kit-sync` hat keinen definierten
  Zustand fuer Lock-Eintraege ausserhalb der Klassen-Tabelle — `skills/CLAUDE.md` faellt durch
  jedes Raster und wurde im heutigen sync nicht aktualisiert, obwohl die Repo-Version sich aenderte).

### Dateien
- `personas/dtb-stakeholder-pitch-coach/{COGNITIVE,README}.md` - geloescht (Quelle in `oma-personas`)
- `commands/dtb-pitch-coach.md` - geloescht; `commands/` jetzt leer, Konzept bleibt dokumentiert
- `CLAUDE.md` / `README.md` - Personas-Abschnitte ersetzt, Projektstruktur + Kopier-Anleitung angepasst
- `skills/CLAUDE.md` / `skills/dtb-kit-sync/SKILL.md` - tote #8-Verweise durch die tatsaechliche Lage ersetzt
- `features/open-question/plan.md` - Kopf-Status `Entwurf` → `Reviewed` (mit Review-Beleg)
- `INBOX.md` - #30 + #31 neu, #10 verworfen und entfernt
- `archive/ARCHIVE_LOG.md` - +1 Zeile (#10, Verworfen)
- `~/.claude/` (extern) - 4 Skills aktualisiert/installiert, pitch-coach-Command entfernt, Lock neu

### Kontext
Als Routine-Resume gestartet; die Kette begann mit einer Verstaendnisfrage zu den Personas im
Review-Workflow. Deren Beantwortung legte den Halbzustand offen (Command global installiert,
Inhalt nicht), was zur Entfernung fuehrte — und die wiederum machte Idee #10 gegenstandslos.
Zwei Befunde entstanden als Nebenprodukt der Pruefungen (#30, #31): beide sind Faelle desselben
Musters — **ein Feld/Eintrag wird gelesen, aber von keinem Skill gepflegt**.

### Naechste Schritte
- [ ] Diesen Checkpoint + INBOX/ARCHIVE_LOG committen (`/dtb:commit-and-push`)
- [ ] `/dtb:idea-review` fortsetzen — 11 offene Ideen, Kandidat #25 (Fach-Agenda, schliesst die Kette)
- [ ] Reale Abnahmen der 4 „Fertig zum Testen"-Features (haengt am Zielprojekt pkp)
- [ ] Offen aus #31: entscheiden, ob `skills/CLAUDE.md` Klasse A wird oder aus dem Lock fliegt —
      bis dahin traegt die installierte Kopie den alten Text
- [ ] pkp-interne Commits (`UI.md` + `project-design/`-Tokens) — separates Repo
