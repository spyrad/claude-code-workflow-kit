# 2026-07-30

## Session 1 - 14:07

*(Erste Session auf dem Rechner „zuhause" seit S1 2026-07-29 — die Maschine, deren Lock auf
`7c3272a`/2026-07-23 stand. Die Sessions S2-S5 des 2026-07-29 liefen auf der anderen Maschine.)*

### Implementiert

- **Resume identifizierte die Maschine als den „zweiten Rechner" aus der offenen Liste** — belegt,
  nicht vermutet: die installierte `workflow-resume`-Kopie enthielt keinen Remote-Divergenz-Check
  (`rev-list`: 0 Treffer), war also aelter als `0130f2b`. Zweiter Befund: der S5-Handoff-Befehl
  `commit-and-push` war durch `0b08608` bereits erledigt — zum zweiten Mal in Folge beschrieb ein
  Handoff einen ueberholten Stand; die Artefakt-Ableitung gewann.
- **`/dtb:kit-sync sync` (Lauf 1): Rechner „zuhause" auf Kit-Stand gebracht.** Genau 3 Abweichungen,
  alle „Update verfuegbar" (`dtb-lesson`, `dtb-project-init`, `dtb-workflow-resume`) — exakt die
  seit `7c3272a` geaenderten Skills. Buendel 1 ausgefuehrt; die neuen Kopien-Hashes
  (`c6c60eba…`, `444ec5f9…`, `2dcfa04f…`) stimmen **byte-genau mit den in S3/S4/S5 auf der anderen
  Maschine dokumentierten Werten** ueberein — unabhaengige Bestaetigung ueber die Maschinengrenze.
- **Drei Lock-Pflegeaktionen im selben Lauf (je einzeln bestaetigt):** (1) `skills/CLAUDE.md`-Eintrag
  gestrichen UND die veraltete Kopie `~/.claude/skills/CLAUDE.md` (Stand 2026-07-18) entfernt —
  der konkrete #31-Fall ist damit auf **beiden** Maschinen vollzogen, Lock- und Repo-Menge
  deckungsgleich; (2) `localPath` nachgetragen (offener S1-Punkt — beide project-init-Seeds finden
  jetzt ihre Quelle); (3) `sourceCommit` `7c3272a` → `0b08608`. Verifikation: frischer check
  **41/41 synchron**, 0 verwaist.
- **Werkzeug-Absicherung uebernommen:** check-Sequenz als `kitcheck.sh` in Datei-Form (S5-Muster),
  hartes Gate meldete `GATE OK: Lock=42→41, Repo=41` **vor** jedem Vergleich. Nebenbefund: das
  Skript meldete den Zustand „unklassifiziert" fuer `skills/CLAUDE.md` ad hoc — frischer
  Praxisbeleg fuer den generischen Teil von Idee #31.
- **Lektion L7 erfasst** (`lessons.md`, rechnerlokal): „Werkzeug-Fehlschlag nie als Datenbefund
  werten: vor jedem Mehr-Seiten-Vergleich per hartem Gate belegen, dass alle Seiten gefuellt sind;
  verbindliche Kommando-Sequenzen nie improvisieren; Shell-Syntax nie ueber Werkzeuggrenzen
  mischen." `Applies-to: alle`. Der Hebe-Hinweis (versionierte Regel-Datei) wurde ausgegeben, das
  Hochziehen nach `skills/CLAUDE.md` steht aus. Beleg der Klon-Lokalitaet: dieselbe Lektion waere
  auf der anderen Maschine L12 — hier ist sie L7 (Bestand dort L1-L11, hier L1-L6).
- **Beide project-init-Bugs archiviert** (`4641956`): Kopfzeile `project-init-status-pfad/bug.md`
  VOR der Archivierung korrigiert (behauptete „Fix erst nach kit-sync" — seit heute auf beiden
  Maschinen widerlegt), dann beide Ordner per `git mv` nach `archive/`, ARCHIVE_LOG mit Fix-SHAs,
  BACKLOG (Abgeschlossen-Tabelle) und WORKFLOW_STATUS bereinigt. Der offene zweite blinde Lauf
  haengt am Skill, nicht am Bug-Ordner — bleibt als Aufgabe.
- **Idee #16 (mom-test, Verworfen) archiviert** — Log-Eintrag mit komprimiertem Grund +
  Wiedereinstiegspunkt (Muster #10). **#13/#21/#24/#26 (Ausgearbeitet) bewusst belassen:**
  Entscheidung, sie erst zusammen mit der Abnahme/Archivierung ihres jeweiligen Features
  wegzuraeumen (bisherige Log-Praxis, Design-Punkte bleiben bis zur Abnahme griffbereit).
- **Ideen-Triage statt Einzelreview:** zweimal angesetztes `/dtb:idea-review` auf Nutzerwunsch in
  eine Aufwand×Nutzen-Triage aller 10 aktiven Ideen umgelenkt (INBOX unveraendert, keine
  Statusentscheidung). Ergebnis: Quick Wins **#30, #32, #28**; #32 zwingend VOR #27 (grill-me
  waere die naechste Discovery mit externem Vorbild — exakt das #32-Risiko); #31 erst reduzieren;
  #22 strategisch wertvollster Mittel-Aufwand-Kandidat; #25/#18 warten auf Anlass; #29 braucht
  eigenen Discovery-Fokus.
- **Abnahme-Befund am Feature commit-and-push, Option A umgesetzt (`eda0ed1`):** Die
  Freitext-Bestaetigung („passt") in Schritt 4.3 wurde in der Praxis uebersehen — der Lauf
  versandete still, Arbeit blieb uncommittet/ungepusht, genau das Risiko, das der Skill beseitigen
  soll. Nach Pro/Contra-Abwaegung (Alternativen: ersatzlos entfernen / Auto-Argument) entschieden:
  Bestaetigung bleibt, Medium gehaertet — **blockierende Auswahlfrage** (AskUserQuestion-Tool;
  Committen & pushen / Message anpassen / Abbrechen), Begruendung in der Zeile;
  `AskUserQuestion` in `allowed-tools` ergaenzt; `spec.md` konsistent nachgezogen.
  **`dtb:implement:157` (gleiche Freitext-Bestaetigung im Phasen-Ende-Ritual) bewusst nicht
  angefasst** — anderes Risiko-Profil mitten im Loop; zweiter Kandidat, wenn sich A bewaehrt.
- **Erster Praxistest von Option A im selben Lauf:** die beiden Commits dieser Session liefen ueber
  den geaenderten Ablauf — Red-Flag-Scan sauber (0 Treffer, keine Datei > 1 MB), Bestaetigung als
  Auswahlfrage, zwei getrennte Commits, ein Push (`0b08608..eda0ed1`).
- **`/dtb:kit-sync sync` (Lauf 2) nach dem Push:** genau eine Abweichung
  (`dtb-commit-and-push` „Update verfuegbar"), Kopie nachgezogen (`023cc5df…`, hashgleich,
  inhaltlich kontrolliert: 2 AskUserQuestion-Treffer), Lock-Diff exakt 2 Zeilen + `sourceCommit`,
  Verifikation **41/41 synchron**.

### Dateien

- `skills/dtb-commit-and-push/SKILL.md` (+9/−4) — Schritt 4.3 Auswahlfrage, `allowed-tools`
- `dtb-project/project-workflows/features/commit-and-push/spec.md` — Bestaetigungs-Muster nachgezogen
- `dtb-project/project-workflows/archive/{project-init-status-pfad,project-init-settings-seed}/` —
  beide Bug-Ordner (per `git mv`, Analysen vollstaendig erhalten; status-pfad-Kopfzeile korrigiert)
- `dtb-project/project-workflows/archive/ARCHIVE_LOG.md` — 3 Eintraege (2 Bugs, Idee #16)
- `dtb-project/project-workflows/{BACKLOG,INBOX,WORKFLOW_STATUS}.md` — Register bereinigt
- `dtb-project/project-rules/lessons.md` — L7 angehaengt (unversioniert, `.gitignore`)
- `~/.claude/` (ausserhalb des Repos): 4 Skill-Kopien nachgezogen, `dtb-lock.json`
  (localPath neu, `skills/CLAUDE.md`-Eintrag raus, 42→41 Artefakte, `sourceCommit` `eda0ed1`;
  Sicherungen `.bak.0b08608`, `.bak.eda0ed1`), `~/.claude/skills/CLAUDE.md` entfernt

**Zwei Commits, beide gepusht** (`0b08608..eda0ed1`): `4641956` Archivierung, `eda0ed1`
commit-and-push-Haertung. Arbeitsbaum vor dem Checkpoint clean.

### Kontext

Distributions- und Aufraeum-Session mit einem echten Feature-Ertrag am Rand. Der Vormittag
gehoerte der Maschinen-Synchronisation: der Rechner „zuhause" hinkte sieben Tage hinterher und ist
jetzt beweisbar auf dem Stand der anderen Maschine — inklusive der Lock-Sonderfaelle, die dort
schon im Juli bereinigt waren. Der Nachmittag brachte den ersten **Abnahme-Befund aus echter
Nutzung** gegen ein „Fertig zum Testen"-Feature: nicht die Mechanik von commit-and-push war falsch,
sondern ein Interaktionsdetail (uebersehbare Freitext-Bestaetigung) konterkarierte den Skill-Zweck.
Die Korrektur wurde vor der Abnahme eingepflegt und im selben Lauf real getestet — der Zyklus
Befund → Abwaegung → Fix → Praxistest schloss sich innerhalb einer Session.

### Naechste Schritte

- [ ] **`/dtb:kit-sync sync` auf der anderen Maschine** — deren `commit-and-push`-Kopie traegt
      noch das Freitext-Muster (Lock dort auf `07d5107`; Distribution ist rechnerlokal)
- [ ] `/dtb:idea-review` entscheiden — Triage liegt vor (Quick Wins #30 → #32 → #28; #32 vor #27)
- [ ] L7-Regel in `skills/CLAUDE.md` heben (Hebe-Hinweis ausgegeben, Konvention, `Applies-to: alle`)
- [ ] `dtb:implement:157` auf Auswahlfrage umstellen, wenn sich Option A bewaehrt
- [ ] Entscheiden: `workflow.config.yaml`/`ROADMAP.md` im Kit-Repo ausfuellen oder Vorlage belassen?
- [ ] Zweiter blinder Lauf gegen die `project-init`-Fassung nach Schritt 8 — bewusst offen
- [ ] #28 um den CRLF-Beleg ergaenzen; #31 auf den generischen Teil reduzieren (heute zweiter Praxisbeleg)
- [ ] `project-health`-Check „Artefakt am falschen Ort" erwaegen (aus S3 2026-07-28)
- [ ] B4/B8/B9 zusammenfassen? — nur bei einem dritten Fall (dokumentiert in `archive/project-init-settings-seed/bug.md`)
- [ ] Reale Abnahmen der 4 „Fertig zum Testen"-Features (haengt am Zielprojekt pkp)
- [ ] pkp intern committen: `UI.md` + `project-design/`-Tokens (separates Repo)

---

## Session 3 - 21:35

*(Nachbereitung von Session 2 — kein neuer Feature-Inhalt, reine Sicherung + Distribution.)*

### Implementiert

- **`/dtb:commit-and-push` (`8f3c3da`, gepusht `789495f..8f3c3da`):** Ein Root (Monorepo-Logik
  greift nicht — genau ein `.git`), Red-Flag-Scan ohne Treffer (keine Secret-Muster, keine Datei
  > 1 MB), 12 Dateien. Die Heterogenitaet des Diffs (Triage-Fixes + Checkpoint) wurde als
  Routine-Hinweis geflaggt, bewusst nicht aufgeteilt. `lessons.md` blieb korrekt ungestaged
  (`.gitignore`).
- **`/dtb:kit-sync sync` (Lauf 2 des Tages):** genau 5 Abweichungen, alle „Update verfuegbar",
  alle mit `K = L` — die Kopien waren seit Lauf 1 unangetastet, die Lock-Kette ist lueckenlos.
  Kopien-Hashes byte-identisch zu den Repo-Hashes; Lock `sourceCommit` `789495f` → `8f3c3da`,
  Sicherung `.bak.8f3c3da`. Verifikation **41/41 synchron, 0 verwaist**.
- **Inhaltskontrolle der installierten Kopien statt blossem Hash-Vergleich:** je ein gezielter
  Grep pro Fix, inklusive **Negativ-Probe** — die in F2 getilgte Klausel „ohne je gelaufenes
  Review" kommt in der installierten `dtb-workflow-next`-Kopie **0-mal** vor. Damit ist belegt,
  dass die beiden Blocker nicht nur im Repo, sondern auch in den tatsaechlich benutzten Skills
  verschwunden sind.

### Dateien

- `~/.claude/skills/dtb-{backlog-status,plan-review,project-health,workflow-next,workflow-status}/SKILL.md`
  — 5 Kopien nachgezogen (ausserhalb des Repos)
- `~/.claude/dtb-lock.json` — 5 Hashes, 3 `installedAt`, `sourceCommit` `8f3c3da`
- `dtb-project/project-changelog/2026-07/2026-07-30.md`, `WORKFLOW_STATUS.md` — dieser Nachtrag

### Kontext

Session 2 endete mit uncommitteten Triage-Fixes und installierten Skills, die noch beide Blocker
enthielten. Dieser Nachtrag schliesst genau diese Luecke: erst sichern, dann verteilen, dann
inhaltlich gegenpruefen. Der Zwischenschritt lohnte sich — ein reiner Hash-Vergleich haette zwar
„synchron" gemeldet, aber nicht belegt, dass der fehlerhafte Satz wirklich weg ist.

### Naechste Schritte

- [ ] **Auf der anderen Maschine `/dtb:kit-sync sync`** — deren Lock steht auf `07d5107`,
      ihr fehlen inzwischen 7 Skill-Updates
- [ ] Optional: frisches `/dtb:impl-review plan-status-feld` — das Verdikt REJECTED im
      Snapshot ist der Stand VOR der Triage; ein neuer Lauf wuerde es aufloesen
- [ ] `/dtb:idea-review` fuer die 12 offenen Ideen — Triage liegt vor (#32 zwingend vor #27)
- [ ] Reale Abnahme der 5 „Fertig zum Testen"-Features — haengt am Zielprojekt pkp
- [ ] L7 und L8 in `skills/CLAUDE.md` heben
- [ ] #28 (`.gitattributes`) entscheiden — heute dreifach belegt

---

## Session 2 - 20:05

### Implementiert

- **Idee #30 in einem Zug durch die gesamte Pipeline gefahren** (idea-review → feature-discover →
  feature-plan → impl-plan → plan-review → feature-start → implement → kit-sync → impl-review):
  Feature `plan-status-feld`, 12/12 Schritte, 4 Phasen-Commits `bdb412e`, `4dd8671`, `789495f`,
  `bf87fd2` (alle gepusht).
- **Kern der Loesung — `DERIVED_STATE_RULES.md` §7 (neu, rein additiv analog §6):** Das
  `plan.md`-Kopf-Statusfeld ist ab jetzt kanonisiert als **reiner Review-Nachweis**:
  Definitionsfenster (`**Status:**`-Zeile in den ersten 10 Zeilen), Parse-Regel (nur das erste
  Wort zaehlt, Klammer-Vermerk und HTML-Kommentar sind Annotation), Wertemenge auf
  `Entwurf`/`Reviewed` gestrafft, Wertematrix mit vier Verdikt-Ausgaengen, Lese-Toleranz fuer
  Altwerte/fehlende Felder, manueller Flip nur mit Urheber-Vermerk, Status-Neutralitaet gegen §1/§2.
- **`dtb:plan-review` ist der eine Pfleger** (neuer Schritt 6): schreibt das Feld bei JEDEM
  Verdikt — auch bei negativem —, still ohne Bestaetigungsfrage, mit Report-Zeile
  `📝 Kopf-Statusfeld → {Wert}`. Harte binaere „behoben"-Bedingung: `Reviewed` nur, wenn ALLE
  WARN-Anpassungen wirklich geschrieben wurden (asymmetrisches Risiko — `Reviewed` schaltet
  `feature-start` frei). `workflow-checkpoint` bleibt ausdruecklich **kein** zweiter Schreiber.
- **Beide Leser gehaertet:** `workflow-next` bekam den fehlenden Checkbox-Guard (Review-Vorschlag
  nur noch bei 0/Y — das war der Kernbefund der Idee), `workflow-status` das Gate ohne den nie
  gesetzten Altwert; beide zusaetzlich Toleranz-Matrix und Feld-Konfliktzeile
  `⚠ plan.md-Kopf sagt "…", ## Progress zeigt "…"` im Bestandsformat.
- **Verifikation zweistufig:** Trockenlauf mit 5 konstruierten Testkoepfen (fehlend, Altwert,
  unbekannt, echter Konflikt, Feld ausserhalb des Fensters) — 5/5 wie spezifiziert; danach
  Smoke-Test am echten Bestand (5 reale Plaene): 4 fertige Features ohne Review-Vorschlag,
  0 Konfliktzeilen. **Eine Erwartung wurde umgedeutet:** Der Test sollte den 0/Y-Fall am eigenen
  Plan pruefen, doch der stand durch die eigene Umsetzung laengst auf 9/12 — Selbstbezug, der
  Fall ist ueber den Trockenlauf abgedeckt.
- **`/dtb:kit-sync sync`:** Vor dem Lauf fiel auf, dass die 3 Feature-Commits **ungepusht** waren
  — der Sync klont von GitHub und haette „alles synchron" gemeldet, ohne irgendetwas zu verteilen
  (Werkzeug-Ergebnis ≠ Datenbefund, L7). Erst gepusht, dann Sync: genau 4 Abweichungen, alle
  „Update verfuegbar", Kopien-Hashes byte-identisch zu den Repo-Hashes, Lock auf `789495f`,
  Verifikation **41/41 synchron, 0 verwaist**.
- **`/dtb:impl-review` mit Verdikt REJECTED (2 blocking) — beide Blocker stammten aus den
  Edits derselben Session:** (F1) §7 hatte **keine Parse-Regel**, waehrend das im selben Commit
  geaenderte impl-plan-Template `**Status:** Entwurf <!-- … -->` erzeugt — woertlich gelesen
  waere jeder frisch erzeugte Plan ein „unbekannter Wert" gewesen, dazu 5 archivierte Plaene mit
  derselben Kommentarform; (F2) die Konfliktregel in `workflow-next` nannte einen zweiten
  Ausloeser („`Reviewed` bei 0/Y ohne je gelaufenes Review"), der dem Kanon UND der eigenen
  Tabellenzeile widersprach und read-only gar nicht entscheidbar ist.
- **Triage: 8 Fixed · 0 Lesson · 2 Skipped, 0 PENDING.** Neben den Blockern behoben: Vorrang der
  Still-Regel (F4), `project-health` prueft den plan.md-Kopf nicht mehr gegen den abgeleiteten
  Status (F3), `backlog-status` liest das Feld gar nicht mehr (F6), reziproke
  Wartungs-Hinweise an allen fuenf Spiegelstellen (F5), Platzhalter vereinheitlicht (F8),
  Beispiel-Output auf `{…}`-Konvention (F9). Bewusst offen: F7/F10 (Umbauten am frisch
  gesetzten §7, die drei Matrix-Kopien bzw. Verweise in vier Dateien nachziehen muessten —
  Nutzen rein aesthetisch).
- **Lektion L8 erfasst:** Negativ-Kriterien (`grep -c "X" = 0`) auf die **Wirkstelle** ankern
  statt auf die ganze Datei — zweimal in dieser Session rot geworden, weil der Skill den
  abgeschafften Begriff in der Abschaffungs-Notiz bzw. der Toleranz-Matrix nennen MUSS. In
  Phase 1 half Umformulieren, in Phase 3 waere es Funktionsverlust gewesen.
- **Zwei neue Ideen erfasst:** **#33** (Aufwand×Nutzen-Triage-Sicht als eigener read-only Skill
  bzw. idea-review-Modus — dreimal an einem Tag wurde `idea-review` genau dahin umgelenkt) und
  **#34** (Entscheidung „`lessons.md` bleibt ungetrackt" bewusst neu aufmachen; neues Faktum:
  belegte Zwei-Maschinen-Divergenz L1–L11 vs. L1–L7 mit kollidierender Nummerierung).

### Dateien

- `dtb-project/project-rules/DERIVED_STATE_RULES.md` — **§7 neu** (~95 Zeilen: 7.1 Fenster/
  Wertemenge/Vermerkform/Parse-Regel, 7.2 Pfleger+Wertematrix, 7.3 Lese-Regeln+Toleranz,
  7.4 Status-Neutralitaet) + 2 Kopplungs-Hinweise + Changelog-Fussnote
- `skills/dtb-plan-review/SKILL.md` — Schritt 6 (Pfleger), Report-Zeile, `allowed-tools: +Edit`,
  `produces: [] → [features/*/plan.md]`, Kopplungs-Hinweis
- `skills/dtb-workflow-next/SKILL.md` — Checkbox-Guard, Toleranz-Matrix, Parse-Regel,
  Feld-Konfliktzeile, Beispiel-Output, Kopplungs-Hinweis
- `skills/dtb-workflow-status/SKILL.md` — Gate ohne Altwert, Ableitungszeilen, Konfliktzeile,
  Queue-Zeile „(Entwurf, 0/Y)", Kopplungs-Hinweis
- `skills/dtb-impl-plan/SKILL.md` — Template-Kopf `Entwurf` + Kommentar, Autoring-Anweisung
- `skills/dtb-project-health/SKILL.md` — Anzeige-Drift-Check auf `spec.md`/`task.md` eingeschraenkt
- `skills/dtb-backlog-status/SKILL.md` — vom Feldwert entkoppelt
- `skills/CLAUDE.md` — plan.md-Konventionszeile (Zweck, Wertemenge, Fenster, Pfleger, §7)
- `dtb-project/project-workflows/features/plan-status-feld/` — `discovery.md`, `spec.md`,
  `plan.md` (12/12 mit SHA-Belegen), `review.md` (10 Findings, 0 PENDING)
- `dtb-project/project-workflows/{INBOX,BACKLOG,WORKFLOW_STATUS}.md` — #33/#34 neu, #30
  `Ausgearbeitet`, #22 um den Skew-Beleg ergaenzt, Feature-Zeile
- `dtb-project/project-rules/lessons.md` — L8 (unversioniert)
- `~/.claude/` — 4 Skill-Kopien, `dtb-lock.json` (`sourceCommit` `789495f`, Sicherung
  `.bak.789495f`)

### Kontext

Die Session hat eine belegte Drift-Quelle im Kern des Statussystems geschlossen: Ein Feld, das
zwei Skills lasen und keiner schrieb. Bemerkenswert ist weniger das Ergebnis als der Weg — das
Kit hat sich an mehreren Stellen selbst korrigiert. Der `plan-review` fand drei WARNs im eigenen
Plan (u.a. ein Prueferkriterium, das die eigene Spec verfehlte), der `impl-review` fand
anschliessend zwei echte Blocker, die erst in der Umsetzung entstanden waren — beide in Text, den
dieselbe Session geschrieben hatte. Ohne den Review waeren sie unentdeckt distribuiert worden.
Zweiter Beleg fuer den Wert der Gates: Vor dem kit-sync verhinderte die Push-Pruefung einen Lauf,
der „alles synchron" gemeldet und nichts verteilt haette.

### Naechste Schritte

- [ ] **`/dtb:commit-and-push`** — die Triage-Fixes (5 Skills + Regel-Datei), `review.md`,
      Register und dieser Log sind uncommittet
- [ ] **Danach erneut `/dtb:kit-sync sync`** — die Triage hat 5 Skill-Dateien angefasst,
      darunter `project-health` und `backlog-status` (vorher nicht im Scope); die installierten
      Kopien weichen wieder ab
- [ ] **Auf der anderen Maschine `/dtb:kit-sync sync`** — deren Lock steht auf `07d5107`,
      ihr fehlen inzwischen 6+ Skill-Updates
- [ ] Optional: frisches `/dtb:impl-review plan-status-feld` nach dem Commit — das Verdikt
      REJECTED im Snapshot ist der Stand VOR der Triage
- [ ] Reale Abnahme der 5 „Fertig zum Testen"-Features — haengt am Zielprojekt pkp
- [ ] L7 und L8 in `skills/CLAUDE.md` heben (beide `Applies-to`-relevant, Hebe-Hinweis offen)
- [ ] #28 ist jetzt dreifach belegt (Staging-Warnungen, Lock-Diff) — `.gitattributes` entscheiden
- [ ] `/dtb:idea-review` fuer die restlichen 12 offenen Ideen (Triage liegt vor: #32 vor #27)
- [ ] `workflow.config.yaml`/`ROADMAP.md` im Kit-Repo ausfuellen oder als Vorlage belassen?
- [ ] Zweiter blinder Lauf gegen die `project-init`-Fassung nach Schritt 8 — bewusst offen
- [ ] `project-health`-Check „Artefakt am falschen Ort" erwaegen (aus S3 2026-07-28)
- [ ] B4/B8/B9 zusammenfassen? — nur bei einem dritten Fall
- [ ] pkp intern committen: `UI.md` + `project-design/`-Tokens (separates Repo)
