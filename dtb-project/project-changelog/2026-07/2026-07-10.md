# Session-Log: 2026-07-10

## Session 1 - Eligibility-Gates (Idee #12 → 11/13)

### Implementiert
- **Feature Eligibility-Gates** über die volle Pipeline gestartet und bis Progress **11/13** umgesetzt
  (Commits `a4ee4ad`/`5772553`/`c6e8fca`/`6efe843`): schreibende Skills verweigern am Eingang, wenn
  ihr kritisches Eingabe-Artefakt fehlt, mit Redirect auf den erzeugenden Skill.
- **Pipeline-Durchlauf:** idea-review (#12 → In Arbeit) → feature-discover → feature-plan → impl-plan
  → plan-review (4 Agenten inkl. Betriebs-Wächter, Verdikt **REVISE** → alle Punkte eingearbeitet)
  → feature-start → 3x3-Umsetzung (Phase 1 + 2 komplett, Phase 3 bis 3.2).
- **Scope-Entscheidung (A):** nur **6 Hard-Gates** in v1; Git-Gates + Soft-Gates als Folge-Feature.
- **Phase 1** (`a4ee4ad`): neuer Abschnitt „## Eligibility-Gates" in `skills/CLAUDE.md` —
  Einteilungsregel „braucht zwingende Eingabe" (nicht `produces`≠leer, nicht positional-`consumes`),
  Fit-Check + Redirect-Mechanik (Selbst-Ausschluss → `after`-Match → verbleibende), Meldeblock mit
  geprüftem Pfad + Escape-Hatch (Bestätigungs-Rückfrage), Hard-Gate-Zuordnungstabelle.
- **Phase 2** (`5772553`/`c6e8fca`): Gates in `impl-plan` (spec.md→feature-plan), `plan-review`
  (plan.md→impl-plan voran, feature-start alt.), `feature-start` (plan.md→impl-plan), `debug-plan`
  (bug.md→bug-report). `archive`/`migrate` hatten Gate **bereits nativ** → nur Konventions-Querverweis.
- **Phase 3.1/3.2** (`6efe843`): Fixture-Abnahme durch unabhängigen Agenten **PASS 10/10**;
  5 Nachschärfungen eingearbeitet (u.a. Glob-`*`→slug-spezifisch, impl-plan-Dublette entfernt).

### Frontmatter-/Skill-Verifikation (Prior L1/L2 + neue Funde)
- **Fund A:** „erstes `consumes` = kritisch" unzuverlässig (`feature-start` führt `BACKLOG.md` zuerst)
  → kritisches Artefakt explizit pro Skill.
- **Fund B:** `produces: []` ≠ „kein Gate" (`plan-review`/`code-review`/`build-check`).
- **Fund C:** `feature-plan` braucht `discovery.md` nicht hart → Soft-Gate → Folge-Feature.
- **Fund D:** `docs-extract` hat `consumes: []` → Sonderfall → Folge-Feature.
- **Fund E:** `archive` ist Scanner (kein Argument); `archive`/`migrate` haben native Gates.
- **Lektion L3** erfasst: `consumes`-Reihenfolge ist kein Indikator für die kritische Vorbedingung.

### Dateien
- `skills/CLAUDE.md` - neuer Abschnitt „Eligibility-Gates" (Konvention, nicht Klasse-A → nicht distribuiert)
- `skills/dtb-{impl-plan,plan-review,feature-start,debug-plan}/SKILL.md` - Hard-Gates (Klasse-A)
- `skills/dtb-{archive,migrate-change-folders}/SKILL.md` - Konventions-Querverweis (Gate nativ)
- `dtb-project/.../features/eligibility-gates/{discovery,spec,plan}.md` - neu (Plan Reviewed, 11/13)
- `dtb-project/project-rules/lessons.md` - L3
- `INBOX.md` (#12 Ausgearbeitet), `BACKLOG.md`, `WORKFLOW_STATUS.md` - synchronisiert

### Entscheidungen
- A Scope = Hard-Gates-only; B Redirect = beide Erzeuger, `after`-Match voran; C Soft-Gate-Verhalten
  = entfällt (Folge-Feature); D = Escape-Hatch als Bestätigungs-Rückfrage; E = Testbett-Vorlauf `dtb-assistant`.
- Ehrliche Bilanz: 2 der 6 „Hard-Gates" existierten schon nativ — echter Neu-Wert bei den 4 planenden Skills.

### Offene Punkte / Nächste Schritte
- [ ] **3.4 globale Verteilung**: erfordert **Push nach `origin/master`** (4 Commits, ausgehend, Freigabe offen),
  danach `/dtb:kit-sync sync` (6 Klasse-A-Updates). Reihenfolge getauscht (Entscheidung a): erst global, dann Testbett.
- [ ] **3.3 Testbett-Vorlauf `dtb-assistant`**: kein Git + flaches Layout → ggf. erst `/dtb:migrate-change-folders`.
- [ ] Optional: `skills/CLAUDE.md`-Verweis in den Gates entschärfen (Pfad zeigt in installierten Kopien ins Leere; funktional unkritisch, Gates self-contained).
- [ ] Folge-Feature: Git-Gates (`code-review`/`build-check`) + Soft-Gates (6 Skills) nach Praxiserprobung.

---

## Session 2 - Eligibility-Gates 3.4 (global) + Testbett pkp + Kit-Bug

### Implementiert
- **3.4 Globale Verteilung** (`7e76c74`): Push `origin/master` (`ebe3dfd..8c1c906`), dann `kit-sync sync` —
  6 Klasse-A-Gate-Skills global übernommen (archive/debug-plan/feature-start/impl-plan/
  migrate-change-folders/plan-review), Lock `8f508b8 → 8c1c906`, `updatedAt 2026-07-10`.
  `pitch-coach` als bewusste Divergenz übersprungen (INBOX #10). Progress **12/13**.
- **Testbett gewechselt auf `pkp`** (statt `dtb-assistant`): frisches, git-gestütztes Projekt.
  Post-Init-Analyse + Angleichung an aktuelles Kit (2 Commits in pkp: `c47ef50`, `41e427a`).
- **Realer Pipeline-Lauf in pkp** (Nutzer): Feature `simulationsmaske-grunddaten` über
  idea → discovery → feature-plan → impl-plan erzeugt → Change-Ordner mit
  `discovery.md`/`spec.md`/`plan.md` (feste Namen). **Happy-Path der Gates + Change-Folder-Modell
  am echten Projekt bestätigt.**
- **Kit-Bug erfasst** (`ca9dcb4`): `project-init-stale-rules-seed` (Severity Hoch) + BACKLOG-Eintrag.
- **Idee #13** (`ca9dcb4`): Fachfragen-/Stakeholder-Agenda (derived aus `[Fach]`-Tags).

### Testbett-Befund (Kit-Bug via pkp)
`project-init` seedete eine **veraltete** `DERIVED_STATE_RULES.md` (86 Z., Flach-Modell) statt der
aktuellen Kit-Version (162 Z., Change-Folder). Ursache: Seed-Quelle in einer Zielprojekt-Session
nicht auflösbar → Rekonstruktion statt Kopie. Fix in pkp: aktuelle Kit-Regel-Datei kopiert (identisch).

### Dateien
- `~/.claude/skills/dtb-{archive,debug-plan,feature-start,impl-plan,migrate-change-folders,plan-review}/SKILL.md` - global aktualisiert
- `~/.claude/dtb-lock.json` - 6 Hashes + sourceCommit `8c1c906`
- `dtb-project/.../features/project-init-stale-rules-seed/bug.md` - neu (Kit-Bug, Offen)
- `dtb-project/project-workflows/{INBOX,BACKLOG}.md` - #13 + Bug-Eintrag
- (pkp) `DERIVED_STATE_RULES.md` ersetzt, Root-Waisen `BACKLOG.md`/`WORKFLOW_STATUS.md` entfernt

### Kontext
Der pkp-Testbett-Aufsatz hat den Kit-Bug aufgedeckt, bevor die Gates selbst getestet wurden —
das Testbett zahlt sich also doppelt aus. Der reale Pipeline-Lauf belegt Happy-Path + Change-Folder-Modell.

### Entscheidungen
- Testbett `pkp` statt `dtb-assistant` (git-gestützt, sauber, `features/` leer → keine Migration nötig).
- Reihenfolge 3.4 vor 3.3 (global-shared Skills → Sync muss vor der Verprobung laufen).

### Offene Punkte / Nächste Schritte
- [ ] **3.3 abschliessen**: expliziten **Block-Test** in pkp belegen (`/dtb:impl-plan test-gate` ohne Spec → ⛔ + Redirect) → dann 12/13 → **13/13**.
- [ ] 2 Kit-Commits (`7e76c74`, `ca9dcb4`) nach `origin/master` pushen.
- [ ] `/dtb:debug-plan project-init-stale-rules-seed` — Fix des Kit-Bugs planen (Lösungsrichtung: Seed-Quelle über `lock.localPath`).
- [ ] Optional: `skills/CLAUDE.md`-Gate-Verweis entschärfen.

---

## Session 3 - Eligibility-Gates abgeschlossen (Abgenommen) + Push

### Implementiert
- **3.3 Testbett-Verprobung belegt** (`59057e6`): Evidenzkette (a) Gates global aktiv,
  (b) echter Happy-Path in pkp (`simulationsmaske-grunddaten` mit `spec.md`+`plan.md`/26 Checkboxen
  = echter Pass, kein False-Pass), (c) Block-Pfad deterministisch (keine `test-gate/spec.md`).
  Zusammen mit Fixture-Abnahme 10/10 → **Progress 13/13, „Fertig zum Testen"**.
- **Eligibility-Gates → Abgenommen** (`f0d068d`, expliziter Zustand, §1.2): nach realer
  Verprobung freigegeben. spec/BACKLOG/WORKFLOW_STATUS synchronisiert.
- **Alle Commits gepusht** (`8c1c906..f0d068d` → `origin/master`, jetzt 0/0).

### Dateien
- `features/eligibility-gates/{plan,spec}.md` - 3.3 abgehakt (13/13), Status Abgenommen
- `dtb-project/project-workflows/{BACKLOG,WORKFLOW_STATUS}.md` - synchronisiert

### Kontext
Abschluss des Eligibility-Gates-Features (Idee #12) über die volle Pipeline in einer Session,
inkl. echtem Dogfooding der neuen Gates am Testbett pkp. Kein Re-Sync nötig — die Commits nach
dem kit-sync (`8c1c906`) berührten keine Klasse-A-Skills.

### Entscheidungen
- Status **Abgenommen** (nicht nur „Fertig zum Testen") — Fixture 10/10 + realer pkp-Lauf als Beleg.
- Archivierung aufgeschoben bis zum gemeinsamen Abnahme-/Archiv-Durchgang mit den 5 Altfeatures.

### Naechste Schritte
- [ ] `/dtb:debug-plan project-init-stale-rules-seed` — Hoch-Bug (betrifft jede Projekt-Init).
- [ ] Gemeinsamer Abnahme-/Archiv-Durchgang: 6 abnahmereife Features → `/dtb:archive`.
- [ ] Idee-Triatge #11/#13/#10 → `/dtb:idea-review`.
