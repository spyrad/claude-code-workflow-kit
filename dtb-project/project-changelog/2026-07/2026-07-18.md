# 2026-07-18

## Session 1 - 07:58

### Implementiert
- **impl-review `opportunity-map`** durchgefuehrt (Feature-End-Review, 3 Dimensionen):
  Drift alle 7 Schritte MATCH (kein EXTRA), Craft 5 Findings (0 blocking), Rules uebersprungen
  (keine Coding-Rules ausser DERIVED_STATE_RULES/lessons). Gesamt-Verdikt: NEEDS ATTENTION.
  Scope aus Progress-SHAs (`772ae4f`, `a2fd691`, `e69446e`, `91bedac`, `c0b0c0f`).
- **Triage: 4 Fixed · 0 Lesson · 2 Skipped.** Alle Fixes im Working Tree (kein Commit).
  - **F1** (`SKILL.md:231`): Selbst-Review-Struktur-Check prueft jetzt den exakten Template-Header
    `## Naechste Richtung, falls wertvoll` (vorher `## Naechste Richtung` — Kontrakt-Check gegen
    nicht-existenten Namen).
  - **F2** (`SKILL.md` Schritt 5): vierter Hand-off-Zweig **„Kein Bau empfohlen"** ergaenzt —
    der kandidatenlose Ausgang hatte kein definiertes Hand-off-Verhalten.
  - **F3** (`SKILL.md:69`): Quellmaterial-Glob schliesst jetzt die eigene Ausgabe
    `OPPORTUNITY-MAP.md` aus (verhindert Re-Ingestion/Anchoring beim Zweitlauf) — analog
    greenfield-prds PRD-MVP-Ausschluss.
  - **F4** (`plan.md` Progress 3.3): SHA-Beleg `c0b0c0f` nachgetragen (Arbeit war committet,
    nur die Rueckschreibung fehlte, §2 Regel 4).
  - **F5** (Pipeline-Reziprozitaet, nit) + **F6** (Cross-Skill-Drift zu greenfield-prd, nit):
    bewusst SKIPPED — F5 einseitiger Vorfeld-Edge gewollt, F6-Angleichung gehoert bei naechster
    Beruehrung in greenfield-prd.

### Dateien
- `skills/dtb-opportunity-map/SKILL.md` - F1/F2/F3-Fixes (Sektionsname, No-Build-Hand-off, Glob-Ausschluss)
- `dtb-project/project-workflows/features/opportunity-map/plan.md` - F4: SHA `c0b0c0f` an Progress 3.3
- `dtb-project/project-workflows/features/opportunity-map/review.md` - NEU (Review-Snapshot, alle Findings entschieden)

### Kontext
Erstes der beiden noch nicht reviewten „Fertig zum Testen"-Features abgearbeitet. Der Review
foerderte drei reale Guard-/Konsistenz-Luecken im Neubau zutage (Selbst-Review prueft falschen
Header, No-Build-Ausgang ohne Hand-off, eigene Ausgabe re-ingestierbar) — alle geschlossen, bevor
die Map in einem echten Greenfield-Flow benutzt wird. Verbleibt fuer die reale Abnahme: die Map
in einer tatsaechlichen build/buy-Sichtung anwenden.

### Naechste Schritte
- [ ] Fixes committen (`/dtb:commit-and-push`) — 3 Dateien im Working Tree
- [ ] `greenfield-autoren-skills` reviewen: `/dtb:impl-review Greenfield-Autoren-Skills`
  (zweites „Fertig zum Testen" ohne review.md)
- [ ] Reale Abnahmen abbauen: opportunity-map (Map in echtem Flow nutzen), commit-and-push /
  Verifikations-Gate (pkp) + F2-Follow-up, fachfragen-erfassung

---

## Session 2 - 09:21

### Implementiert
- **opportunity-map committet+gepusht+verteilt:** `773dd73` (fix F1-F4 + Checkpoint S1) gepusht;
  `/dtb:kit-sync sync` verteilte 7 Bündel-1-Aktionen global (1 neu `dtb-commit-and-push`,
  6 Updates inkl. opportunity-map-Fixes), Lock → `sourceCommit 773dd73`, 41 Artefakte synchron.
- **impl-review `greenfield-autoren-skills`** (Feature-End-Review, 3 Dimensionen): Drift 11/11 MATCH
  (kein EXTRA), Craft 6 Findings (1 als blocking gemeldet), Rules übersprungen. Gesamt: NEEDS ATTENTION.
  Scope aus 8 Commits (`948421e`…`9ade135`).
- **Triage: 6 Fixed · 0 Lesson · 1 Skipped** (alle Fixes im Working Tree):
  - **F1** (S:Hoch): bidirektionale Format-Kopplung war defekt — PRD-Hinweis listete `## Zielgruppen`
    (nie geparst), beide Hinweise verschwiegen real geparste `## Risiken`/`## Abhaengigkeiten`.
    Beide Gegen-Hinweise auf den echten Parse-Satz vereinheitlicht (der L5-Schutz schützte falsch).
  - **F2**: Doc-Status-Zuweisungsregel (ready/blocked/proposed) in roadmap Schritt 6 ergänzt.
  - **F3**: Eligibility-Gate blockte den Report-Modus (Gate vor Modus-Weiche) → Vorab-Ausnahme
    für vorhandene ROADMAP.md.
  - **F4**: falsche SHA-Zuordnung in plan.md korrigiert (4.4 zeigte `6bb2a9b` = 4.3-Commit → `9ade135`).
  - **F5** (Readiness-Signal 1 mechanisch präzisiert) + **F6** (Resume-Endzustand K=f) — Fixed.
  - **F7** (consumes-Mischformat) — Skipped (kosmetisch).

### Dateien
- `skills/dtb-greenfield-prd/SKILL.md` - F1 (Kopplungs-Hinweis) + F6 (Resume-Endzustand)
- `skills/dtb-greenfield-roadmap/SKILL.md` - F1 (Kopplungs-Hinweis) + F2 (Doc-Status) + F3 (Gate) + F5 (Readiness)
- `dtb-project/project-workflows/features/greenfield-autoren-skills/plan.md` - F4 (SHA-Zuordnung 4.3/4.4)
- `dtb-project/project-workflows/features/greenfield-autoren-skills/review.md` - NEU (Snapshot, 6 Fixed/1 Skipped)
- `~/.claude/` (via kit-sync) - dtb-commit-and-push neu + 6 Updates; `dtb-lock.json` → 773dd73

### Kontext
Zweites (letztes) noch nicht reviewtes „Fertig zum Testen"-Feature abgearbeitet — die Review-Front ist
jetzt leer. Der schwerste Fund (F1) war ein realer Defekt genau im L5-Schutz, den das Feature selbst
als Deliverable einführte: die Format-Kopplung sicherte gegen den falschen Sektionssatz ab. Verdikt
NEEDS ATTENTION statt REJECTED, weil die Laufzeit intakt war (roadmap liest am Nutzungsort korrekt).

### Naechste Schritte
- [ ] greenfield-autoren-skills-Fixes committen (`/dtb:commit-and-push`) — 4 Dateien im Working Tree
- [ ] Danach `/dtb:kit-sync sync` — greenfield-prd + -roadmap global aktualisieren
- [ ] Reale Abnahmen abbauen (via `/dtb:workflow-checkpoint` mit Beleg): opportunity-map (echter Flow),
  greenfield-autoren-skills (erster realer Greenfield-Lauf, zweistufige Abnahme), commit-and-push /
  Verifikations-Gate (pkp) + F2-Follow-up, fachfragen-erfassung

---

## Session 3 - 13:16

### Implementiert
- **greenfield-autoren-skills-Fixes committet + verteilt** (Abschluss der S2-Folgeschritte):
  `e157cdf` (fix F1-F6 + Checkpoint S2) nach `origin/master` gepusht (7 Dateien).
- **`/dtb:kit-sync sync`:** 2 Bündel-1-Updates global übernommen (`dtb-greenfield-prd`,
  `dtb-greenfield-roadmap`), Lock → `sourceCommit e157cdf`, Verifikation **41 synchron, 0 Abweichung**.
- **`/dtb:workflow-next`** (read-only) bestätigt: kein Feature „In Entwicklung", Review-Front leer —
  alle 5 Features stehen auf „Fertig zum Testen", offen nur die realen Abnahmen.

### Dateien
- (keine neuen Repo-Änderungen in dieser Session — nur Commit/Push des S2-Stands + Distribution)
- `~/.claude/skills/dtb-greenfield-{prd,roadmap}/SKILL.md` + `~/.claude/dtb-lock.json` (via kit-sync)

### Kontext
Reine Sicherungs-/Verteilungs-Session: die in S2 gefixten Greenfield-Autoren-Skills sind jetzt
gepusht und global aktuell (die F1-Kopplungskorrektur wirkt ab sofort in allen Projekten). Damit ist
der Testau-Abbau der Review-Seite vollständig — es bleiben ausschließlich reale Abnahmen im Zielprojekt.

### Naechste Schritte
- [ ] Reale Abnahmen belegen (via `/dtb:workflow-checkpoint`, Option „Abgenommen"): Verifikations-Gate +
  commit-and-push an pkp durchspielen; greenfield-autoren-skills/opportunity-map bei echtem Greenfield-/
  build-buy-Lauf; fachfragen-erfassung bei nächster feature-discovery
- [ ] Follow-up F2 (§2 Regel 3 „verletzt ≠ ausstehend") — `verifikations-gate/review.md` PENDING
- [ ] Alternativ ohne Zielprojekt: `[Fach]`-Strang #26 → #25 → #24 oder Ideen-Triage (#27, #23, #22, #18, #16, #15, #10)

---

## Session 4 - 14:53

### Implementiert
- **verifikations-gate-Review vollstaendig entschieden:** die drei bis dahin offenen Findings
  (F1/F2/F3) abgearbeitet, Gesamt-Verdikt NEEDS ATTENTION → alle Findings FIXED. Zwei Commits.
  - **F2** (`DERIVED_STATE_RULES.md` §2 Regel 3, `36d6c1a`): den tragenden Halbsatz aus
    `implement` 3.2 **wortgleich** in die Single-Source-Regel ergaenzt — „Kriterien, die erst
    spaetere Schritte der Phase liefern, gelten als ausstehend, nicht verletzt". Vorher trug nur
    der Treiber-Skill die Nuance; ein Mensch, der nur §2 liest, konnte ein ausstehendes
    Automated-Kriterium faelschlich als „rot" fehldeuten.
  - **F3** (`spec.md` SC9-Notiz, `b28451d`): Scope-Halbsatz ergaenzt — `features/`-Laufzeitplaene
    per Scope-Entscheidung ausgenommen; die SC9-Aussage „3x3 = genau eine Quelle" haelt jetzt
    scope-unabhaengig (Laufzeitplan bewusst nicht retroaktiv umgeschrieben, kein Churn).
  - **F1** (Weg b, `lessons.md`, `b28451d`): als Lektion #4 festgehalten, dass `implement`
    `lessons.md` bewusst als Prior liest (Design-Absicht, kein Drift) — verifiziert an
    `dtb-implement/SKILL.md:16` (`consumes` listet `lessons.md`); Reader-Liste im Kopf um
    `implement` korrigiert. `lessons.md` ist im Kit-Repo gitignored → Lektion lebt lokal, der
    getrackte Beleg ist `review.md`.

### Dateien
- `dtb-project/project-rules/DERIVED_STATE_RULES.md` - F2 (§2 Regel 3: ausstehend≠verletzt)
- `dtb-project/project-workflows/features/verifikations-gate/spec.md` - F3 (SC9 Scope-Notiz)
- `dtb-project/project-workflows/features/verifikations-gate/review.md` - F1/F2/F3-Decisions → FIXED + Auflösungszeile
- `dtb-project/project-rules/lessons.md` - F1: Lektion #4 + Reader-Liste (lokal, gitignored — nicht committet)

### Kontext
Die letzte offene Review-Front ist abgearbeitet — alle 5 aktiven Features sind reviewt und ihre
Findings entschieden. F2 war der substanzielle Fund: die „ausstehend vs. verletzt"-Unterscheidung
stand nur im Treiber-Skill, nicht in der deklarierten Single-Source-Regel §2 — jetzt an beiden
Orten wortgleich. Kein `kit-sync` noetig: kein Klasse-A-Artefakt (Skill/Agent/Command) geaendert;
F2 traf `DERIVED_STATE_RULES.md` (Klasse-B-Seed, via `project-init` verteilt, nicht drift-checked
— Bestandsprojekte bekommen die Haertung nicht automatisch). Nebenbefund (nicht geaendert):
`lessons.md` ist im Kit-Repo per `.gitignore` lokal; der „nicht an Nutzer ausliefern"-Grund greift
faktisch nicht, weil Nutzer `dtb-project/` nie erhalten — reine Beobachtung fuer spaeter.

### Naechste Schritte
- [ ] Reale Abnahmen belegen (via `/dtb:workflow-checkpoint`, Option „Abgenommen"): Verifikations-Gate +
  commit-and-push an pkp; greenfield-autoren-skills/opportunity-map bei echtem Greenfield-/build-buy-Lauf;
  fachfragen-erfassung bei naechster feature-discovery
- [ ] Optional kit-intern: `[Fach]`-Strang #26 → #25 → #24 oder Ideen-Triage (#27, #23, #22, #18, #16, #15, #10)
- [ ] Optional: `lessons.md`-gitignore-Beobachtung via `/dtb:idea` festhalten

---

## Session 5 - 17:44

### Implementiert
- **Session-Auftakt:** `workflow-resume` + `idea-review`-Uebersicht (10 offene Ideen gelistet — keine
  Triage-Entscheidung getroffen) + vollstaendige Kartierung aller offenen Punkte (5 reale Abnahmen,
  Ideen-Inbox, 3 kleine Aufraeum-/Erfassungs-Punkte).
- **Entscheidung `lessons.md`-gitignore (offenen Idee-Punkt geschlossen statt vertagt):** Variante
  **„bewusst lokal lassen"** gewaehlt. Der Kern war eine Rollen-Spannung des Kit-Repos —
  Distributionsquelle (ignore korrekt: Nutzer will fremde Lessons nicht) vs. eigenes Arbeitsprojekt
  (ignore verliert Daten ueber Klone/Arbeitsorte). Bewusst zugunsten „klon-lokal/fluechtig" aufgeloest.
  Begruendung als **versionierter `.gitignore`-Kommentar** verankert — sie ueberlebt den Ortswechsel,
  im Gegensatz zu `lessons.md` selbst.
- **Kosmetik:** BACKLOG-Platzhalterzeile `[Feature 1]` aus „Ideen / Backlog" entfernt (Tabelle jetzt
  leer, konsistent mit „Aufgaben").
- WORKFLOW_STATUS: offene Aufgabe „`lessons.md`-Beobachtung festhalten" gestrichen (durch Entscheidung erledigt).

### Dateien
- `.gitignore` - Kommentar bei der project-rules-Regel: `lessons.md` bewusst ungetrackt/klon-lokal (Entscheidung 2026-07-18)
- `dtb-project/project-workflows/BACKLOG.md` - Platzhalterzeile `[Feature 1]` entfernt + Datum synchronisiert
- `dtb-project/project-workflows/WORKFLOW_STATUS.md` - offene Aufgabe gestrichen + dieser Checkpoint

### Kontext
Kurze Housekeeping-Session ohne Feature-Arbeit. Der seit S4 als Nebenbefund offene `lessons.md`-gitignore-
Punkt wurde entschieden statt erneut vertagt; die zugehoerige „Idee festhalten"-Aufgabe entfaellt damit.
Feature-Status unveraendert: alle 5 Features weiterhin „Fertig zum Testen".

### Naechste Schritte
- [ ] Kleiner Rest #3: `[Fach]`-Prio-Frage in `features/fachfragen-erfassung/spec.md:90` abhaken (Prio „Mittel" im BACKLOG faktisch bestaetigt)
- [ ] Ideen-Triage fortsetzen (#27, #23, #22, #18, #16, #15, #10)
- [ ] Reale Abnahmen belegen (via `/dtb:workflow-checkpoint`, Option „Abgenommen"): Verifikations-Gate +
  commit-and-push an pkp; greenfield-autoren-skills/opportunity-map bei echtem Greenfield-/build-buy-Lauf;
  fachfragen-erfassung bei naechster feature-discovery
