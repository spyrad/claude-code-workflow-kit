# 2026-07-31

## Session 1 - 14:04

*(Erste Session auf der Maschine, deren Lock auf `07d5107` stand — dem Rechner, der seit
2026-07-29 (S1) nicht mehr synchronisiert war. Die Sessions des 2026-07-30 liefen auf der
anderen Maschine.)*

### Implementiert

- **`/dtb:kit-sync sync` (Lauf 1): diese Maschine auf Kit-Stand gebracht.** Genau 7 Abweichungen,
  alle „Update verfuegbar" mit `K = L` (Kopien seit dem letzten Sync unangetastet, Lock-Kette
  lueckenlos) — exakt die seit `07d5107` geaenderten Skills. **Unabhaengige Gegenprobe:**
  `git diff --name-only 07d5107..e55978a -- skills agents commands` liefert dieselben 7
  SKILL.md-Dateien. Lock `07d5107` → `e55978a`, Diff exakt 32 Zeilen (7×2 Feldpaare +
  `sourceCommit` + `updatedAt`), Verifikation **41/41 synchron, 0 verwaist**.
- **`#31`-Fall auf dieser Maschine vollzogen:** die veraltete Kopie `~/.claude/skills/CLAUDE.md`
  (Hash `a312b500…` vs. Repo `44a09675…`) entfernt — separat abgefragt, weil Regel §5 sie als
  Nicht-Klasse-A eigentlich ignoriert. Der **konkrete** Teil von Idee #31 ist damit auf beiden
  Maschinen erledigt; offen bleibt nur der generische Zustand „unklassifiziert".
- **`/dtb:impl-review plan-status-feld` (frischer Lauf, Verdikt REJECTED — 1 blocking).**
  Beide **Alt**-Blocker verifiziert behoben (Parse-Regel in `DERIVED_STATE_RULES.md:292`;
  Klausel „ohne je gelaufenes Review" 0-mal in `workflow-next`). Der neue Blocker ist ein anderer:
  **F1** — die `📝 Kopf-Statusfeld`-Zeile stand im selben Output-Fence wie die Anpassungs-Frage,
  deren Antwort sie voraussetzt. Wer das Template ausfuehrt, bestimmt (und schreibt) den Feldwert,
  bevor feststeht, ob die WARNs eingearbeitet werden — genau die harte binaere
  „behoben"-Bedingung, die der Plan mit dem asymmetrischen Risiko begruendet, waere im
  REVISE-Pfad ausgehebelt gewesen.
- **Scope-Entscheidung transparent gemacht:** Die `## Progress`-SHAs nennen 4 Commits, die
  Triage-Nacharbeit liegt aber in `8f3c3da` (regelkonform nicht in Progress nachgetragen). Da sie
  der Anlass des Laufs ist und zwei zusaetzliche Skills anfasst, wurde `8f3c3da` in den
  Datei-Scope aufgenommen — 9 inhaltliche Dateien statt 7.
- **Triage: 10 Fixed · 0 Lesson · 0 Skipped, 0 PENDING.** Neben F1: **F2** Still-Vorrang war nur
  in den Kanon geschrieben, nicht in die beiden Spiegel (der Triage-Fix des Vortags hat seinen
  eigenen Kopplungs-Hinweis verletzt); **F3** Konflikt-Praedikat in §7.3 war undefiniert —
  woertlich gelesen waere `Reviewed` + 7/12 ein Konflikt gewesen, nach §7.4 ist es keiner;
  **F4** das Checkpoint-Kriterium `grep -c "In Umsetzung" … = 0` war rot (1 Treffer) und wurde
  trotzdem geflippt — das Kriterium widersprach dem eigenen Schritt-Output; **F5** der geplante
  Konsistenz-Sweep war unbelegt und unvollstaendig; **F6-F10** `skills/CLAUDE.md`-Frontmatter-
  Konvention, `workflow-status`-Praeambel und Gate-Spalte, Einfuegeregel ohne Fenster-Bindung,
  Spec-Widerspruch.
- **Sweep zu F5 nachgeholt und belegt:** `In Umsetzung`/`Abgeschlossen` kommen ausserhalb von
  Changelog/Archiv **4-mal** vor (alle legitim), das Feld wird von genau **7 Dateien**
  referenziert — die vollstaendige bekannte Menge. **0 neue Befunde**, Nachweis unter Schritt 2.3
  im Plan.
- **`/dtb:commit-and-push` (`5b29f81`, gepusht `e55978a..5b29f81`):** ein Root, Red-Flag-Scan ohne
  Treffer, 8 Dateien. `lessons.md` und `STYLE.md` blieben korrekt ungestaged (`.gitignore`).
- **`/dtb:kit-sync sync` (Lauf 2):** genau 3 Abweichungen (die Klasse-A-Skills aus `5b29f81`),
  Lock → `5b29f81`, **41/41 synchron**. **Inhaltskontrolle statt blossem Hash-Vergleich:**
  F1-Negativ-Probe (`📝` im Output-Fence der installierten `plan-review`-Kopie = **0**, letzte
  Fence-Zeile ist die Anpassungs-Frage), F2/F3 je 1× in beiden Lesern, F8/F9 je 1×.
- **`/dtb:idea-review`:** Idee **#29** (Session-Hygiene-/No-Loss-Gate) auf `In Arbeit` gesetzt,
  die uebrigen 11 unveraendert. Zwei Ideen hatten sich seit der Triage vom 2026-07-30 bewegt und
  wurden vor der Entscheidung gemeldet: #31 zur Haelfte erledigt, #28 mit dem vierten Beleg
  (CRLF-Warnung beim `add` von `review.md`).
- **Lektion L14 erfasst** (`lessons.md`, rechnerlokal): „Bei einer Aenderung an einer Stelle mit
  Kopplungs-/Wartungs-Hinweis die genannten Spiegel im selben Zug mitziehen UND die Spiegelung
  mechanisch verifizieren (Grep ueber alle genannten Dateien, Zielzahl = Anzahl der Spiegel)."
  `Applies-to: alle`. Verallgemeinert L10 auf explizite Verweise ueber Dateigrenzen.
- **L7, L8 und L14 nach `skills/CLAUDE.md` gehoben** — die Mechanik-Regeln haben jetzt sechs
  Abschnitte statt drei: „Kopplungs-Hinweise: Spiegel mitziehen und verifizieren" (L14, bewusst
  direkt hinter den L10-Block gesetzt, dessen Verallgemeinerung es ist),
  „Werkzeug-Fehlschlag ist kein Datenbefund" (L7), „Negativ-Kriterien auf die Wirkstelle ankern"
  (L8). Jede mit Regel + Praxisfall im Bestandsstil. Damit ist der seit 2026-07-29 offene
  Hebe-Punkt erledigt.

### Dateien

- `skills/dtb-plan-review/SKILL.md` — F1 (📝-Zeile aus dem Output-Fence geloest, Begruendung
  umgedreht, 6.3 macht die Reihenfolge verbindlich), F9 (Fenster-Grenze in 6.2 Punkt 2)
- `skills/dtb-workflow-next/SKILL.md`, `skills/dtb-workflow-status/SKILL.md` — F2/F3
  (Konflikt-Praedikat + Vorrang der Still-Regel gespiegelt), F7 (Praeambel eingegrenzt),
  F8 (Spalte „Verifikation" statt „Build")
- `dtb-project/project-rules/DERIVED_STATE_RULES.md` — F3 (Praedikat in §7.3)
- `skills/CLAUDE.md` — F6 (`after`/`next` als Listen) sowie die drei gehobenen Lektionen (+39)
- `dtb-project/project-workflows/features/plan-status-feld/` — `review.md` (neuer Snapshot,
  10 Findings, 0 PENDING), `plan.md` (F4-Kriterium + F5-Sweep-Nachweis), `spec.md` (F10)
- `dtb-project/project-workflows/INBOX.md` — #29 → `In Arbeit`
- `dtb-project/project-rules/lessons.md` — L14 angehaengt (unversioniert, `.gitignore`)
- `~/.claude/` (ausserhalb des Repos): 10 Skill-Kopien nachgezogen (7 + 3), `dtb-lock.json`
  zweimal fortgeschrieben (`07d5107` → `e55978a` → `5b29f81`; Sicherungen `.bak.e55978a`,
  `.bak.5b29f81`), `~/.claude/skills/CLAUDE.md` entfernt

**Ein Commit, gepusht** (`e55978a..5b29f81`). Uncommittet bei Session-Ende: `skills/CLAUDE.md`
(Lektionen-Hebung), `INBOX.md` (#29) und dieser Checkpoint.

### Kontext

Die Session hat den Kreis um Idee #30 geschlossen — aber anders als erwartet. Der frische
`impl-review` sollte nur ein veraltetes REJECTED aufloesen; er fand stattdessen einen neuen,
echten Blocker in Text, den die Vorsession geschrieben hatte. **Zum dritten Mal in Folge entstand
der Defekt nicht in der Mechanik, sondern zwischen zwei frisch geschriebenen Stellen** — hier
Report-Template gegen Schrittfolge. F2 zeigt dieselbe Klasse eine Ebene hoeher: Der Triage-Fix
des Vortags hat einen Wartungs-Hinweis verletzt, der zwei Zeilen ueber der geaenderten Stelle
stand. Genau daraus entstand L14, und weil `Applies-to: alle` gilt, wurde sie zusammen mit L7/L8
sofort versioniert gehoben statt rechnerlokal liegen zu lassen — die Konsequenz aus der
Divergenz-Beobachtung in Idee #34.

Zweiter roter Faden: **Hash-Gleichheit ist kein Inhaltsnachweis.** Beide kit-sync-Laeufe wurden
mit gezielten Greps auf die Wirkstellen gegengeprueft, inklusive Negativ-Probe. Das ist inzwischen
die dritte Session, in der dieser Zwischenschritt etwas belegt, was ein reiner Hash-Vergleich nur
behauptet haette.

### Naechste Schritte

- [ ] **`/dtb:feature-discover` fuer #29** (Session-Hygiene-/No-Loss-Gate) — Idee steht auf
      `In Arbeit`. Zwei Praxisbelege aus dieser Session als Discovery-Material: (a) L14 lebte
      bis zur Erfassung nur im Chat — genau der Fall, den #29 fangen soll; (b) #33/#34 wurden
      mitten im Arbeitslauf direkt erfasst, was die offene Frage „nur melden vs. direkt routen"
      erdet
- [ ] `/dtb:commit-and-push` — `skills/CLAUDE.md`, `INBOX.md` und dieser Checkpoint sind
      uncommittet (`skills/CLAUDE.md` ist **kein** Klasse A → kein kit-sync noetig)
- [ ] **Auf der anderen Maschine `/dtb:kit-sync sync`** — deren Lock steht auf `eda0ed1`,
      ihr fehlen 8 Skill-Updates
- [ ] Optional: frisches `/dtb:impl-review plan-status-feld` — der Snapshot steht auf REJECTED
      (Stand VOR der Triage, im Kopf der Datei vermerkt); ein neuer Lauf wuerde neu vergeben
- [ ] `#31` auf den generischen Teil reduzieren — der konkrete Fall ist seit heute auf beiden
      Maschinen erledigt
- [ ] `#28` (`.gitattributes`) entscheiden — heute vierfach belegt
- [ ] Reale Abnahmen der 5 „Fertig zum Testen"-Features — haengt am Zielprojekt pkp
- [ ] `/dtb:idea-review` fuer die restlichen 11 offenen Ideen (#32 zwingend vor #27)
- [ ] `dtb:implement:157` auf Auswahlfrage umstellen, wenn sich Option A bewaehrt
- [ ] `workflow.config.yaml`/`ROADMAP.md` im Kit-Repo ausfuellen oder als Vorlage belassen?
- [ ] Zweiter blinder Lauf gegen die `project-init`-Fassung nach Schritt 8 — bewusst offen
- [ ] `project-health`-Check „Artefakt am falschen Ort" erwaegen (aus S3 2026-07-28)
- [ ] pkp intern committen: `UI.md` + `project-design/`-Tokens (separates Repo)
