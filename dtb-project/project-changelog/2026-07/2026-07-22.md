# 2026-07-22

## Session 1 - 12:42

### Implementiert
- **DtB-Designsprache aus pkp extrahiert + als Kit-Konvention generalisiert.** Ausgehend von der
  Klärung „DESIGN.md" (technisch vs. UI/visuell) wurde pkps implizite visuelle Designsprache
  faktentreu extrahiert und ein wiederverwendbarer Weg für neue DtB-Projekte etabliert.
- **Quellenanalyse pkp-frontend:** React 19 + Vite + **antd v6.2.3** + CSS Modules (kein Tailwind).
  Design liegt implizit in `src/index.css` (`:root`-Tokens), `App.tsx` (`ConfigProvider`-Theme:
  Primary `#000`, Radius 4, Inter, kompakte Tabellen), `constants/chartColors.ts`+`chartConfig.ts`
  (IBCS-Chart-System) und ~30 `*.module.css` (4/8-Spacing, Radius 4→12, Gewichte 400/500/600).
- **Extrakt-Artefakte (liegen in pkp, intern):** `UI.md` (Design-Guideline, zwei Schichten: neutral
  + antd, inkl. „Known Drift"-Abschnitt) sowie `dtb-design-tokens.css`/`.json` (konsolidierte,
  bereinigte Tokens — löst pkps Hex-Duplikate + px/rem-Mix auf).
- **Leak vermieden:** Artefakte zuerst versehentlich im Kit (`frameworks/`) angelegt → erkannt, dass
  das Kit auf **öffentliches GitHub** pusht → alles nach pkp (internes GitLab `uhbs.ch`) verschoben,
  Kit wieder clean. DtB-Designwerte gehen nie ins öffentliche Kit.
- **Konventions-Verortung in pkp:** Guideline → `project-rules/UI.md` (wird von `dtb:impl-review`
  geprüft, Ziel von `generate-rules ui`); Token-Assets → neuer Bucket `project-design/`
  (Regeln vs. Assets getrennt). Ordner `design/` → `project-design/` umbenannt (project--Präfix-Konvention).
- **Kit-Generalisierung (nur leere Struktur, keine Werte):** `project-init` legt künftig
  `project-design/` inkl. erklärender README an; Output-Locations-Doku in CLAUDE.md ergänzt.

### Dateien
- `CLAUDE.md` - Output-Locations um `project-design/` + `project-rules/UI.md`-Konvention (+2 Zeilen)
- `skills/dtb-project-init/SKILL.md` - `project-design/` im Verzeichnisbaum + README-Vorlage (+19 Zeilen)
- *(extern, pkp — intern)* `dtb-project/project-rules/UI.md`, `dtb-project/project-design/dtb-design-tokens.{css,json}`

### Kontext
Die Fach-Frage „wo passt DESIGN.md in meinen Workflow" führte über eine Konzept-Klärung
(technisches Design-Doc vs. UI-/Visual-Guide) zur eigentlichen Aufgabe: die abteilungsweit
etablierte DtB-Designsprache aus dem Referenz-Produkt pkp portabel machen. Kernentscheidungen:
Designwerte bleiben **intern** (pkp/GitLab), das Kit trägt nur das leere Muster; pkp ist die
**kanonische Design-Referenz**, aus der neue Projekte per Copy&paste starten (Regel-Doc →
`project-rules/UI.md`, Assets → `project-design/`, antd-Theme aus Schicht 2 übernehmen).

### Naechste Schritte
- [ ] Kit-Änderung committen (`docs(project-init): project-design-Bucket + UI.md-Konvention`)
- [ ] pkp committen (intern): `UI.md` + `project-design/` — via `/dtb:commit-and-push`
- [ ] Folge-Idee erwägen: `generate-rules ui` könnte Tokens nach `project-design/` emittieren / referenzieren
- [ ] Bestandsprojekte: `project-design/` bei Bedarf nachrüsten (nicht rückwirkend durch project-init)
- [ ] Unverändert offen: `/dtb:idea-review` fortsetzen (9 Ideen), meeting-dump real abnehmen, commit-and-push-Vollabnahme (pkp), #23

---

## Session 2 - 22:06

### Implementiert
- **Stale-Klon erkannt + auf Remote-Stand synchronisiert.** `/dtb:workflow-resume` deckte auf, dass
  dieser Klon auf 2026-07-19 (Checkpoint `8f16744`) hing, `origin/master` aber **9 Commits voraus**
  war (07-20/21/22: Verifikations-Gate real abgenommen, meeting-dump gebaut+verteilt, Idee #29 neu
  gefasst, project-design-Konvention). Nach Verlustanalyse (**kein** Datenverlust — `lessons.md`
  gitignored, Memory extern, #29 remote in besserer Fassung) via `git reset --hard origin/master`
  synchronisiert; verwaister lokaler S2-Commit `40f6ac4` (07-19-Housekeeping) verworfen (reflog-rettbar).
- **2 Features abgenommen** (Nutzer-Entscheidung 2026-07-22): **Opportunity-Map** und
  **Greenfield-Autoren-Skills** — dessen dokumentierte „Entscheidung G" (Abnahme erst am realen
  Greenfield-Lauf) bewusst übersprungen, transparent im spec-Kommentar. Status in `spec.md` (Quelle)
  + BACKLOG/WORKFLOW_STATUS synchronisiert → Commit `8debb8c`, gepusht.
- **3 abgenommene Features archiviert** (`/dtb:archive`): verifikations-gate, greenfield-autoren-skills,
  opportunity-map. Ganze Change-Ordner via `git mv` nach `archive/<slug>/` (12 Renames); spec-Status →
  Abgeschlossen; INBOX #19/#17/#11 (Ausgearbeitet) entfernt; `ARCHIVE_LOG.md` +6 Zeilen; BACKLOG
  Aktiv→Abgeschlossen; WORKFLOW_STATUS 3 Zeilen raus → Commit `1ad8c6e`, gepusht.
- Read-only zwischendurch: `/dtb:backlog-status` (2×), ein „nichts zu tun"-Lauf von `/dtb:commit-and-push`.

### Dateien
- `archive/{verifikations-gate,greenfield-autoren-skills,opportunity-map}/` - ganze Change-Ordner archiviert
- `archive/ARCHIVE_LOG.md` - 6 Zeilen (3 Features + 3 Ideen #19/#17/#11)
- `BACKLOG.md` / `INBOX.md` / `WORKFLOW_STATUS.md` - Abnahme-Sync + Archiv-Bereinigung

### Kontext
Als Routine-Resume gestartet, aber ein 3 Tage veralteter lokaler Klon kam ans Licht. Nach verlustfrei
belegtem Sync zwei Features abgenommen und die drei Abgenommenen archiviert — der aktive Backlog
schrumpfte von 7 auf **4** Features (alle Fertig zum Testen, reviewt, warten auf reale Abnahme).

### Naechste Schritte
- [ ] `/dtb:idea-review` fortsetzen — 10 offene Ideen (Kandidat #25 Fach-Agenda, schließt die Fach-Fragen-Kette)
- [ ] Reale Abnahmen der 4 verbleibenden „Fertig zum Testen"-Features
- [ ] pkp-interne Commits (`UI.md` + `project-design/`-Tokens) — separates Repo, nicht von hier erreichbar
