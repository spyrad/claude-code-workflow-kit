# 2026-07-29

## Session 1 - 05:40

*(Session am Rechner „zuhause" begonnen am Abend des 2026-07-28 mit `/dtb:workflow-resume`;
die inhaltliche Arbeit faellt vollstaendig auf den 29., daher hier protokolliert.)*

### Implementiert

- **Die drei Folgebefunde aus S3 einzeln bewertet statt gesammelt uebernommen** — zwei davon
  hielten der Pruefung nicht als eigener Vorgang stand:
  - **`settings.json` wird nicht verteilt** → bestaetigt, als Bug erfasst (s.u.)
  - **`CLAUDE.md` ohne Zielort** → **weitgehend erledigt durch den S3-Fix selbst.** `:250-251`
    sagt seit `9e2e9e3` „dort liegen nur `workflow.config.yaml` (Schritt 3) und `CLAUDE.md`" —
    der Root ist damit belegt, die Aussage stand nur an der falschen Stelle. Kein Bug, eine Zeile
  - **`.gitkeep` nur in `input/`** → kein Bug, Hygiene. Der belastbare Teil war ein anderer als
    notiert: `project-design/` bekommt zwei Zeilen spaeter eine README „haelt das leere Verzeichnis
    im Git" — das Problem ist im selben Schritt einmal geloest und dreimal nicht
- **Bug `project-init-settings-seed` erfasst, analysiert, behoben, getestet, gepusht** (`41ebf97`,
  Severity Mittel, 7/7 Fix-Schritte, 5/6 Testschritte gruen).
- **Root-Cause STRONG, zweifach belegt:** `grep -ni "settings"` ueber die komplette
  `skills/dtb-project-init/SKILL.md` → **0 Treffer** (auch nicht in `produces:`), waehrend
  `dtb:kit-sync` Schritt 4 die Klasse-B-Seeds woertlich an project-init delegiert („wird von
  install NICHT kopiert — Seeds sind projektlokal und Sache von `dtb:project-init`"). Beide
  Seiten der Delegation sind dokumentiert, nur die Ausfuehrung fehlte. **Der Widerspruch ist
  damit Skill-vs.-Skill, nicht nur Doku-vs.-Skill** — schaerfer als bei der Erfassung vermutet.
- **Entstehungsursache (H2):** Die Seed-Mechanik war fuer *eine Datei* gebaut, nicht fuer die
  *Klasse* — `:261-296` hiess „Regel-Datei verteilen" mit hartkodiertem `SRC`/`DST`. L10-Muster
  eine Ebene hoeher: nicht die Nachbarzeile wurde uebersehen, sondern das zweite Element derselben
  Klasse. **Fix entsprechend strukturell:** Abschnitt „Seeds verteilen (Klasse B)" mit Seed-Tabelle
  und drei Stufen — A loest die Quelle einmal fuer beide auf, B ist der Regel-Seed, C der neue
  settings.json-Seed.
- **Wichtigste Fix-Entscheidung — Existenz-Gate:** Der Vorlage-Block macht `cp` **ohne
  Existenzpruefung**. 1:1 uebernommen haette das eine projekteigene `.claude/settings.json`
  (permissions, hooks, env) zerstoert. Deshalb `elif [ -f "$DST" ]` → `UEBERSPRUNGEN` + `diff` zur
  Anzeige, ausdruecklich „NICHT automatisch mergen".
- **Dritter Fall in der Quellenaufloesung ergaenzt** (fiel erst beim Testen auf): kein Lock /
  Lock ohne `localPath` / `localPath` zeigt ins Leere — letzteres mit Rechnerwechsel-Hinweis.
  Vorher gab es dafuer eine einzige Sammelmeldung.
- **Tests aus der Quelle statt nachgebaut:** die Bash-Bloecke wurden per `awk` **aus der SKILL.md
  extrahiert** und unveraendert ausgefuehrt — getestet ist der Text, der im Skill steht.
  Happy Path `.claude/settings.json` hashgleich (`67a06f4c…`); Ueberschreib-Schutz byte-identisch
  (`13d7df48…` vorher = nachher); Fehlerpfad gegen den **echten** Lock dieser Maschine;
  Regel-Seed unbeschaedigt; `grep -rn "consumes:.*settings" skills/` → 0 Treffer, also kein
  `produces`-Fehlalarm moeglich.
- **Eine geplante Aenderung nach Pruefung verworfen:** Die kit-sync-Klassen-Tabelle sollte um
  `DERIVED_STATE_RULES.md` ergaenzt werden — `kit-sync:80-81` zeigt, dass sie dort **bewusst**
  als „nicht in v1" ausgeklammert ist. Kein Defekt; stattdessen den Verweis im Fix praezisiert.
  Scope blieb damit auf einen Skill beschraenkt.

### Dateien

- `skills/dtb-project-init/SKILL.md` — der Fix (+99/−17): Abschnitt „Seeds verteilen (Klasse B)"
  mit Tabelle + Schritten A/B/C, `.claude/` im Verzeichnisbaum, `.gitkeep` fuer
  `project-changelog/`/`project-testing/`/`features/`, Zielort an der CLAUDE.md-Anweisung,
  `produces:` um `.claude/settings.json`
- `dtb-project/project-workflows/features/project-init-settings-seed/bug.md` — neu (154 Zeilen):
  Symptom mit Zeilenbelegen, Auswirkung, 3 Hypothesen mit Evidenzstaerke, 7 Fix-Schritte
  (abgehakt mit Belegen), Testplan mit Ergebnissen, 3 Nebenbefunde
- `dtb-project/project-workflows/BACKLOG.md` / `WORKFLOW_STATUS.md` — Bug-Eintrag + Status

Committet und gepusht als `41ebf97` (`4ab0069..41ebf97`, 4 Dateien, 253 +/17 −). Red-Flag-Scan
sauber (keine Secret-Muster, nichts ueber 1 MB); ein Git-Root; 3 Pfade in `.gitignore` unberuehrt.

### Kontext

Die Session begann als Resume und lief den Bug-Zyklus zum **zweiten Mal in Folge** vollstaendig
durch (`bug-report → debug-plan → feature-start → Fix → commit-and-push`). Wieder korrigierte
jede Stufe die vorherige: die Analyse schaerfte den Befund von „Doku widerspricht Skill" auf
„zwei Skills widersprechen sich", und die Umsetzung verwarf eine geplante Aenderung, die sich
beim Nachlesen als Absicht entpuppte.

**Der eigentliche Ertrag war das Sortieren der drei Folgebefunde.** Zwei von dreien waren nach
Pruefung keine Bugs — einer davon war durch den vorherigen Fix schon erledigt, ohne dass es
jemand vermerkt hatte. Der Reflex „alle drei erfassen" haette zwei Vorgaenge erzeugt, die sofort
wieder geschlossen worden waeren.

**Rechnerwechsel mit Folgen — dritter Fall in drei Tagen:** Diese Session lief auf der Maschine,
die den kit-sync-Lauf aus S3 **nicht** gesehen hat. Belege: der Lock steht hier auf `7c3272a` /
2026-07-23 (statt `9e2e9e3`), und `lessons.md` enthaelt nur L1-L6 (statt L10). Der
Lektionen-Prior lief deshalb auf **0 Treffer**, obwohl L10 (`Applies-to: alle`) exakt dieser Fall
war — die „Applies-to-Luecke" aus S3 hat damit eine zweite, mechanische Ursache: die Datei ist per
`.gitignore:15` unversioniert und wandert nicht zwischen den Rechnern. Der Remote-Divergenz-Check
(offener Punkt aus S2) wurde diesmal manuell gefahren: `git fetch` + `rev-list --left-right` → 0/0.

### Naechste Schritte

- [ ] **Blinde Abnahme des Fixes** (Subagent, leeres Verzeichnis, nur die SKILL.md als Quelle) —
      beim Vorgaenger-Bug fand genau dieser Schritt einen Fehler *im Fix selbst*. Braucht Freigabe
- [ ] **`/dtb:kit-sync sync`** — die installierte Kopie ist hier auf Stand `7c3272a` (2026-07-23),
      der Fix also nicht aktiv
- [ ] **`localPath` im Lock nachtragen** — fehlt auf dieser Maschine komplett; ohne ihn laeuft
      `/dtb:project-init` bei **beiden** Seeds in die (neue, korrekte) Fehlermeldung
- [ ] **`lessons.md`-Synchronisation entscheiden** — unversioniert und damit rechnerlokal;
      L7-L10 fehlen hier. Versionieren, oder bewusst lokal lassen und den Prior-Filter darauf einstellen?
- [ ] Erfassen? `feature-start` setzt den `bug.md`-Header auf „In Arbeit", waehrend
      `DERIVED_STATE_RULES §1.5` Header-Felder dieser Dateien nur fuer *Pausiert* gelten laesst —
      die Anzeige ist bis zum ersten Haken falsch (kein Schaden, das Feld wird ignoriert)
- [ ] Beide behobenen Bugs archivieren? (`project-init-status-pfad` 5/5, `project-init-settings-seed`
      7/7) — Gegenargument unveraendert: die `bug.md` tragen die Analysen
- [ ] `project-health`-Check „Artefakt am falschen Ort" erwaegen (aus S3)
- [ ] `/dtb:idea-review` fortsetzen — 10 offene Ideen ohne #15; naechstliegend #27 und #25
- [ ] `workflow-resume` um den Remote-Divergenz-Check erweitern (Lehre aus S2, diesmal manuell ersetzt)
- [ ] Reale Abnahmen der 4 „Fertig zum Testen"-Features (haengt am Zielprojekt pkp)
- [ ] Offen aus #31: `skills/CLAUDE.md` als Klasse A aufnehmen oder aus dem Lock streichen
- [ ] pkp intern committen: `UI.md` + `project-design/`-Tokens (separates Repo)

---

## Session 2 - 11:20

### Implementiert

- **Der Rechnerwechsel-Vorbehalt aus S1 ist auf dieser Maschine gegenstandslos — belegt, nicht
  vermutet.** S1 schrieb in den Handoff, die installierte Kopie stehe auf `7c3272a` und
  `localPath` fehle. Real: Lock auf `7b6ba14` **mit** `localPath`, und
  `~/.claude/skills/dtb-project-init/SKILL.md` ist byte-identisch mit der Repo-Version
  (`143726de…`, 12 `settings`-Treffer). Der Fix `41ebf97` ist hier also wirksam. Damit fielen
  drei Punkte der offenen Liste weg, ohne dass Arbeit noetig war.
- **`0130f2b` gepusht** (`7b6ba14..0130f2b`) — der Remote-Divergenz-Check in
  `dtb:workflow-resume` (offener Punkt seit S2 2026-07-28, +45/−2). Schritt 3 macht jetzt
  `fetch --all --prune` + `rev-list --left-right --count HEAD...@{u}` + `log HEAD..@{u}`,
  mit Read-only-Guardrails (nie `pull`/`merge`/`stash`/`reset`/`checkout`), Auswertung fuer
  „kein Upstream/offline" (1 Hinweiszeile statt Abbruch), **behind > 0** (Commits +
  `diff --stat` + Kollisionspruefung gegen uncommittete Aenderungen) und **ahead > 0**.
  Der Commit entstand parallel zur Session: HEAD wanderte von `7b6ba14` auf `0130f2b`,
  waehrend der Resume-Lauf noch die alte installierte Fassung ausfuehrte.
- **`/dtb:kit-sync sync` gelaufen — genau eine Abweichung:** `skills/dtb-workflow-resume/SKILL.md`
  im Zustand **„Lock veraltet"** (R ≠ L, K ≠ L, K = R). Buendel 1 ausgefuehrt: nur der
  Lock-Eintrag nachgezogen, keine Datei-Aktion. Verifikation ueber eine frische Klon-Quelle:
  **41/41 synchron, keine Abweichungen.**
- **Der Zustand „Lock veraltet" ist hier erstmals real eingetreten** — und die Ursache ist
  belegbar: die installierte Kopie wurde um **10:53** geschrieben, das Lock zuletzt um **08:25**.
  Die Kopie wurde also **am Skill vorbei** aktualisiert (`cp`), genau der Fall, den die
  Klassifikations-Tabelle vorsieht. Kein Datenverlust, aber ohne den sync haette jeder
  kuenftige `check` eine Abweichung gemeldet, die real nicht existiert.
- **Beleg fuer Idee #28 (`.gitattributes`) gefunden:** Repo-Datei 0 CRLF-Zeilen, installierte
  Kopie 221 — die rohen `sha1sum` weichen ab (`e3a4d9c0` vs `22b3310f`), die Git-Blob-Hashes
  sind identisch (`e950e294`). `kit-sync` selbst ist immun (Referenz 2 normalisiert per
  `tr -d '\r'`), aber jeder ad-hoc-Hashvergleich Repo ↔ `~/.claude/` stolpert darueber.
- **Konkreter Fall aus Idee #31 hat sich erledigt:** `skills/CLAUDE.md` steht **nicht mehr** im
  Lock — Lock- und Repo-Menge sind deckungsgleich (41/41). Der generische Teil von #31 (ein
  definierter Zustand „unklassifiziert" fuer Lock-Eintraege ohne Klassen-Zuordnung) bleibt offen.
- **Read-only-Laeufe zur Orientierung:** `workflow-resume` (Handoff-Abweichung aufgedeckt),
  `backlog-status` (6 Items, 0 Statuskonflikte, keine Backlog-Luecke), `idea-review`
  (**ohne Entscheidung abgebrochen** — INBOX unveraendert, 11 Eintraege `Offen`).

### Dateien

- `~/.claude/dtb-lock.json` (ausserhalb des Repos, nicht versioniert) — 3 Zeilen:
  `sourceCommit` `7b6ba14` → `0130f2b`; `skills/dtb-workflow-resume/SKILL.md` `hash`
  `f89a5fc6…` → `e950e294…` und `installedAt` `2026-07-09` → `2026-07-29`. Diff gegen die
  Sicherungskopie belegt: keine weitere Zeile, kein Neuformatieren des JSON
- `skills/dtb-workflow-resume/SKILL.md` — der Remote-Check (Commit `0130f2b`, parallel entstanden)
- Keine weitere Repo-Datei geaendert; Arbeitsbaum vor dem Checkpoint sauber, Divergenz `0 0`

### Kontext

Aufraeum- und Distributions-Session ohne Feature-Fortschritt. Der Ertrag ist ein **korrigiertes
Lagebild**: der S1-Handoff beschrieb einen Maschinenstand, der nicht (mehr) zutraf, und drei
Punkte der offenen Liste waren bereits erledigt. Das ist die Kehrseite des Rechnerwechsel-Themas
der letzten Tage — der Handoff traegt den Stand *einer* Maschine, wird aber auf jeder gelesen.
Konsequenz fuer kuenftige Resumes: der neue Remote-Check aus `0130f2b` deckt die Git-Seite ab,
die **Distributions**-Seite (Lock, installierte Kopien) bleibt Sache eines `kit-sync check`.

Der Vollstaendigkeit wegen: das Idea-Review lief nur bis zur Uebersicht. Die dort vorgeschlagene
Reihenfolge (#30 zuerst statt #27/#25, weil #30 vollstaendig belegt ist und die Ableitungsmechanik
selbst betrifft) ist ein **Vorschlag, keine Entscheidung** — die INBOX wurde nicht angefasst.

### Naechste Schritte

- [ ] **Blinde Abnahme des Fixes `41ebf97`** (Subagent, leeres Verzeichnis, nur die SKILL.md) —
      unveraendert offen; beim Vorgaenger-Bug fand genau dieser Schritt einen Fehler im Fix. Braucht Freigabe
- [ ] **Zwei sachlich ueberholte `bug.md`-Kopfzeilen korrigieren** — beide behaupten „Fix noch
      nicht per `kit-sync` installiert" bzw. „trägt den Fix erst nach `/dtb:kit-sync sync`";
      auf dieser Maschine nachweislich falsch (41/41 synchron). Prosa, die kein Skill synchronisiert
- [ ] `/dtb:idea-review` fortsetzen — 11 Eintraege `Offen`; Vorschlag #30, dann #32, danach #23/#22
- [ ] Beide behobenen Bugs archivieren? (5/5 bzw. 7/7) — Gegenargument unveraendert: die `bug.md`
      tragen die Analysen
- [ ] `lessons.md`-Synchronisation entscheiden — hier vollstaendig (L1-L10), aber per
      `.gitignore:15` unversioniert und damit rechnerlokal
- [ ] #28 um den CRLF-Beleg dieser Session ergaenzen (0 vs. 221 CRLF-Zeilen, gleiche Blob-Hashes)
- [ ] #31 auf den generischen Teil reduzieren — der konkrete `skills/CLAUDE.md`-Fall ist erledigt
- [ ] `project-health`-Check „Artefakt am falschen Ort" erwaegen (aus S3 2026-07-28)
- [ ] Reale Abnahmen der 4 „Fertig zum Testen"-Features (haengt am Zielprojekt pkp)
- [ ] pkp intern committen: `UI.md` + `project-design/`-Tokens (separates Repo)

---

## Session 3 - 13:10

### Implementiert

- **Blinde Abnahme von `41ebf97` durchgefuehrt — NICHT bestanden.** Aufbau: Subagent, leeres
  `git init`-Verzeichnis, einzige Wissensquelle die SKILL.md (Lesen von `features/**`, Changelogs
  und anderen Skills untersagt), Interview-Antworten vorgegeben, harte Schreibgrenze aufs
  Testverzeichnis. Der Lauf **gelang** — aber nur, weil der Subagent improvisierte und die
  Abweichung selbst berichtete. **Zweite blinde Abnahme in Folge, die einen Fehler im Fix fand.**
- **Befund B1 — `$KIT` ueberlebt den Blockwechsel nicht.** Schritt A loest die Kit-Quelle auf,
  B und C benutzen `$KIT` — aber jeder Bash-Aufruf ist eine eigene Shell. Selbst nachgestellt,
  zwei getrennte Aufrufe: `Block A: KIT=C:/…/claude-code-workflow-kit` → `Block B sieht: KIT=[]`
  → `SRC=/dtb-project/…` → FEHLER-Zweig. Woertlich ausgefuehrt melden **beide** Seeds
  „nicht gefunden" bei vorhandenem, korrektem Kit.
- **Es ist eine Regression, belegt per `git show 4ab0069:skills/dtb-project-init/SKILL.md`:** vor
  dem Fix stand die `LOCK`/`KIT`-Aufloesung **im selben Block** wie `SRC`/`cp`. Fix-Schritt 3
  („Quellenaufloesung vor beide Seeds gezogen") hat sie herausgezogen und damit den vorher
  **funktionierenden** Regel-Seed mit gebrochen — genau Punkt 3 der eigenen Regressionsrisiko-Liste,
  ohne dass ein Test ihn abdeckte.
- **Die eigentliche Lehre — warum 5/6 gruene Tests blind waren:** der Testplan extrahierte die
  Bloecke per `awk` und fuehrte sie **gebuendelt** aus. In einer Shell funktioniert es; der Defekt
  sitzt an der **Naht zwischen** den Bloecken. „Getestet ist der Text, der im Skill steht" war
  richtig, aber unvollstaendig — es fehlten die **Ausfuehrungsgrenzen**.
- **Fix-Schritt 8 umgesetzt:** `LOCK`/`KIT`-Aufloesung in jedem der drei Bloecke, plus
  `[ ! -d "$KIT" ]`-Guard mit ehrlicher Meldung statt des irrefuehrenden „`/settings.json` nicht
  gefunden"; Warnblock nach Schritt A kennzeichnet die Wiederholung als Absicht, damit sie niemand
  als Redundanz wieder herauskuerzt. **Mitnahme B3:** aeusserer Fence der Integrations-README auf
  4 Backticks (der innere ```` ``` ````-Fence beendete formal den aeusseren). **Mitnahme B11:**
  `|| true` an den `ls`-Block `:52` (Exit 2 im leeren Projekt — dem Normalfall fuer project-init).
- **Verifikation diesmal an der Naht:** beide Bloecke per `awk` extrahiert und **je einzeln in
  frischer Shell** ausgefuehrt (`$KIT` beweisbar leer beim Start) → beide `Seed OK … hashgleich`,
  `.claude/settings.json` wieder `67a06f4c…`; Ueberschreib-Schutz `8ecec192…` vorher = nachher;
  neuer Guard mit `HOME` ohne Lock → „Kit-Quelle nicht aufloesbar", **nichts angelegt**;
  `ls`-Block `exit=0`. Vorab-Sonde: `git hash-object` laeuft **ausserhalb** eines Repos (exit 0) —
  die Hash-Verifikation traegt also auch in Nicht-Git-Projekten (relevant fuer `dtb-assistant`).
- **`/dtb:commit-and-push` gelaufen** — Ablauf verkuerzt, weil der Commit `88d6087` schon vorlag:
  1 Root (`rev-parse --show-toplevel`), clean & ahead 1 → kein Staging, kein Leer-Commit, nur Push
  (`3f5ad78..88d6087`). Der konditionale Pfad aus Schritt 2 („Push nur, wenn lokal ahead") deckt
  diesen Fall korrekt ab.
- **`/dtb:kit-sync sync` gelaufen** — genau eine Abweichung: `skills/dtb-project-init/SKILL.md`
  als **„Update verfuegbar"** (R ≠ L, K = L). Buendel 1 ausgefuehrt, Buendel 2 leer. Lock-Diff
  gegen die Sicherung: **genau zwei Zeilen** (`sourceCommit` `0130f2b` → `88d6087`, `hash`
  `d4a2f733…` → `6636aad3…`), kein Neuformatieren. Verifikation ueber frischen check-Lauf:
  **41/41 synchron**.
- **Fuenf Nebenbefunde bewusst zurueckgestellt** (in `bug.md` dokumentiert, nicht vergessen):
  **B2** Integrations-README landet in `vendor-x/input/`, obwohl ihr Inhalt `integrations/`
  beschreibt — die Drop-Zone, die `docs-extract` scannt (einziger mit echter Auswirkung);
  **B4** Repo-`name` wird nie erfragt, aber verlangt und ausgegeben; **B8/B9** `{paths.*}`- und
  `[…]`-Platzhalter-Regeln nur fuer Fall 3 definiert; **B5/B6/B7** Reihenfolge, doppelte `mkdir`s,
  `.gitkeep` neben README; **B12** keine `.gitignore`-Behandlung.

### Dateien

- `skills/dtb-project-init/SKILL.md` (+28/−7) — Schritt 8 (B1) + Mitnahmen B3/B11
- `dtb-project/project-workflows/features/project-init-settings-seed/bug.md` (+72/−1) — Kopfzeile
  auf 8/8 und den tatsaechlichen Installationsstand korrigiert (die ueberholte Behauptung „Fix noch
  nicht per `kit-sync` installiert" ist damit weg), Fix-Schritt 8, Abschnitt „Blinde Abnahme
  2026-07-29 — nicht bestanden" mit B1-Beleg, 5 zurueckgestellte Nebenbefunde, 6 neue Testeintraege
- `~/.claude/skills/dtb-project-init/SKILL.md` (ausserhalb des Repos) — installierte Kopie auf
  `6636aad3…` gezogen; `~/.claude/dtb-lock.json` 2 Zeilen, Sicherung als `dtb-lock.json.bak.88d6087`
- `BACKLOG.md` / `WORKFLOW_STATUS.md` — Datum bzw. Statusblock

Committet und gepusht als `88d6087` (`3f5ad78..88d6087`, 2 Dateien, +93/−7).

### Kontext

Die Session bestand aus **einem** Vorgang: der Freigabe der blinden Abnahme und ihren Folgen. Sie
hat sich zum zweiten Mal in Folge bezahlt — und diesmal deckte sie nicht nur einen Fehler im Fix
auf, sondern einen **Fehler in der Testmethode**, die den Fix fuer gruen erklaert hatte. Das ist
der uebertragbare Teil: Bash-Bloecke in Skills teilen sich Variablen ueber Aufrufgrenzen hinweg,
die es zur Laufzeit nicht gibt. Wer sie zum Testen buendelt, testet eine Ausfuehrung, die im
Ernstfall nicht stattfindet.

Nebenbei ein Muster, das sich haelt: von 12 Befunden des blinden Laufs war **einer** ein Bug,
drei waren mitnahmefaehige Textfehler, der Rest Unschaerfe oder Kosmetik. Dieselbe Sortierarbeit
wie in S1 — der Reflex „alle erfassen" haette elf Vorgaenge erzeugt.

### Naechste Schritte

- [x] **Lektion erfasst — L11** (nach dem Checkpoint, `lessons.md` Zeile 20): „Bash-Bloecke eines
      Skills sind eigene Shells: jeden Block selbststaendig halten und beim Test einzeln in
      frischer Shell ausfuehren, nie gebuendelt." `Applies-to: alle`. Duplikat-Check: ein Treffer
      (L8) nur auf dem Wort „Shell" — dort Shell-**Varianten**, hier Shell-**Lebensdauer**,
      andere Achse. Der belastbarste Ertrag dieser Session
- [x] **B2 direkt gefixt statt als Vorgang erfasst** — Zielort der Integrations-README von
      `vendor-x/input/` auf `integrations/` korrigiert (`SKILL.md:150`), mit Begruendung im Text.
      **Abwaegung:** ein eigener `bug.md` haette Ursache, Auswirkung und Fix nur aus dem
      Abnahme-Abschnitt umkopiert — B2 hat keine Analyse-Substanz. Dieselbe Schwelle wie bei
      B4/B8/B9. Der Handoff hatte konservativ auf `/dtb:bug-report` gezeigt; die Entscheidung fiel
      im Gespraech, bevor sie in eine neue Session vererbt wurde
- [ ] Zweiter blinder Lauf gegen die Fassung nach Schritt 8 — B1 ist staerker belegt als ein
      blinder Lauf es koennte, aber weitere Befunde derselben Klasse sind denkbar. Bewusst offen
- [ ] Kopfzeile von `features/project-init-status-pfad/bug.md` korrigieren — behauptet weiter
      „traegt den Fix erst nach `/dtb:kit-sync sync`"; hier widerlegt (41/41). Die Schwester-Datei
      ist in dieser Session erledigt worden
- [ ] `/dtb:idea-review` fortsetzen — 11 Eintraege `Offen`; Vorschlag #30, dann #32
- [ ] Beide behobenen Bugs archivieren? (5/5 bzw. 8/8) — Gegenargument unveraendert: die `bug.md`
      tragen die Analysen, und die des settings-seed-Bugs ist gerade um den Abnahme-Abschnitt gewachsen
- [ ] `lessons.md`-Synchronisation entscheiden — unversioniert (`.gitignore:15`), damit rechnerlokal;
      die Lektion oben waere sofort betroffen
- [ ] #28 um den CRLF-Beleg aus S2 ergaenzen; #31 auf den generischen Teil reduzieren
- [ ] `project-health`-Check „Artefakt am falschen Ort" erwaegen (aus S3 2026-07-28)
- [ ] B4/B8/B9 als Praezisierungs-Vorgang zusammenfassen? (Platzhalter- und Namensregeln in
      `project-init`) — nur wenn ein dritter Fall dazukommt, sonst Ueberverwaltung
- [ ] Reale Abnahmen der 4 „Fertig zum Testen"-Features (haengt am Zielprojekt pkp)
- [ ] pkp intern committen: `UI.md` + `project-design/`-Tokens (separates Repo)
