# 2026-07-28

## Session 1 - 05:58

### Implementiert

- **Idee #16 (`10x-mom-test`-Pendant) verworfen** — mit belegter Begruendung direkt im
  INBOX-Eintrag hinterlegt, damit sie nicht nur im Session-Log lebt.
- **Belegt: die Mom-Test-Substanz ist bereits in `dtb:opportunity-map` eingebaut.** Drei
  Fundstellen — „Gute Signale sind beobachtbar und wiederholen sich" (`SKILL.md:78`, das
  Kernprinzip vergangenes Verhalten statt Meinung); die Nachfrage-Mechanik „je eine kurze Frage,
  die den Schmerz von der vorgeschlagenen Loesung trennt" (`:83`, mit Beispiel „Baut ein
  Dashboard." → *Welchen wiederkehrenden Handgriff ersetzt es?*); und ein Go/No-Go
  „qualifiziert sich keiner, empfiehl keinen Bau" mit 6 Ranking-Kriterien, davon drei
  verhaltensbasiert (`:145-157`). Gegenprobe: `grep -rni 'mom'` ueber `skills/`, `agents/`,
  `commands/`, `CLAUDE.md` → **null Treffer**; auch global unter `~/.claude/skills/` existiert
  kein mom-test-Skill. Es gibt also kein Skill-Pendant, aber die Wirkung ist da.
- **Entscheidender Abgrenzungspunkt festgehalten:** `opportunity-map` befragt den Nutzer ueber
  **seine eigene** wiederkehrende Reibung (Selbstauskunft, Ein-Personen-Setting); der Mom-Test
  adressiert die Verzerrung in Gespraechen mit **Dritten** („Leute sagen dir, was du hoeren
  willst"). Ohne Vorhaben, potenzielle Nutzer zu interviewen, liefen Interview-Leitfaden,
  Umfrage und Screener leer. Nicht abgedeckt bleiben Hypothesen-Extraktion (Fakt vs.
  Wunschdenken als expliziter Trennschritt), Go/No-Go mit numerischen Schwellen und ein eigenes
  Artefakt — als Wiedereinstiegspunkt im INBOX-Eintrag vermerkt, falls echte Nutzer-Interviews
  anstehen.
- **Falsch-Erinnerung korrigiert und belegt:** #16 war *nicht* am 2026-07-27 verworfen worden.
  Der einzige INBOX-Diff von gestern (`4120b8a`, einziger Commit an `INBOX.md` seit dem 25.07.)
  aendert genau eine Zeile — #15 von `Offen` auf `In Arbeit`. In allen 22 Changelog-Treffern zu
  „#16" taucht die Nummer nur in „Ideen-Triage fortsetzen"-Listen auf. Das ablehnende Verdikt
  von gestern betraf **#15** (`10x-health-check`), und auch das war kein Verwerfen, sondern
  „kein 1:1-Pendant" mit Gegenvorschlag.
- **`/dtb:backlog-status` gelaufen:** 4 Features „Fertig zum Testen" (10/10, 10/10, 8/8, 9/9),
  **kein Feature „In Arbeit", kein geplantes, keine Bugs, keine Tasks** — die Umsetzungsseite
  des Backlogs ist leer. Keine Konflikte zwischen BACKLOG-Statusspalte und Ableitung. Genau eine
  offene `[Fach]`-Frage kitweit (`features/fachfragen-erfassung/spec.md:90`, Prioritaet
  bestaetigen — status-neutral, blockiert nichts).
- **Nebenbefund:** `grill-me` und `grill-with-docs` (Idee #27) sind global unter
  `~/.claude/skills/` **bereits installiert**, aber nie gegen den Greenfield-Stack abgegrenzt
  worden — #27 steht weiterhin als „evaluieren" offen.

### Dateien

- `dtb-project/project-workflows/INBOX.md` — #16 Status `Offen` → `Verworfen`, Begruendung mit
  Zeilenbelegen und Wiedereinstiegspunkt im Idee-Text ergaenzt
- `dtb-project/project-workflows/BACKLOG.md` — „Letzte Aktualisierung" nachgezogen (Statusspalten
  unveraendert, Ableitung deckt sich)

### Kontext

Als Ideen-Review gestartet, aber vor der ersten Entscheidung von zwei Zwischenfragen unterbrochen —
und genau die trugen den Ertrag. Die erste („haben wir #16 nicht gestern verworfen?") war eine
Falsch-Erinnerung, deren Aufklaerung zeigt, wie aehnlich sich #15 und #16 anfuehlen: beide sind
10x-Pendant-Ideen, beide bekamen ein ablehnendes Verdikt, nur an verschiedenen Tagen und aus
verschiedenen Gruenden. Die zweite Frage („der mom-test ist doch schon integriert?") war der
eigentliche Hebel: die Vermutung stimmte inhaltlich, obwohl der INBOX-Text seit dem 2026-07-14
das Gegenteil behauptete („Kit hat KEIN Pendant") — richtig auf Skill-Ebene, irrefuehrend auf
Wirkungsebene.

Damit wiederholt sich das Muster von #15 am Vortag: Das Verdikt gegen ein 1:1-Pendant faellt nicht
aus Aufwandsgruenden, sondern weil die Substanz laengst am richtigen Ort sitzt bzw. schlanker
gehoert. Unterschied: bei #16 ist sie tatsaechlich da (`opportunity-map`), bei #15 fehlt sie und
soll nur kleiner ausfallen als das Vorbild. Deshalb #16 `Verworfen`, #15 weiter `In Arbeit`.

**Risiko dieser Session:** keins von der Art des Vortags — die Begruendung steht im INBOX-Eintrag
selbst, nicht nur hier. Der INBOX-Text von #15 traegt dagegen weiterhin sein ueberholtes
„VOR dem Bau pruefen" (Befund vom 2026-07-27, unveraendert offen).

### Naechste Schritte

- [ ] `/dtb:feature-discover 15` — Zuschnitt (1:1 vs. schlank), Traeger (eigener Skill vs.
      `project-init` vs. `build-check`) und Naming entscheiden; Startkontext ist der Log
      vom 2026-07-27
- [ ] `/dtb:idea-review` fortsetzen — jetzt 9 offene Ideen. Naechstliegend: **#27** (`grill-me`
      ist bereits installiert, nur nie abgegrenzt — entscheidbar ohne Bau) und **#25**
      (Fach-Agenda, fehlendes Glied der Kette #13→#26→#25→#24)
- [ ] Diesen Checkpoint + `INBOX.md` committen (`/dtb:commit-and-push`)
- [ ] build-check entscheiden: streichen (Option A nachholen) oder behalten — braucht den
      pkp-Nutzungsbeleg
- [ ] Reale Abnahmen der 4 „Fertig zum Testen"-Features (haengt am Zielprojekt pkp)
- [ ] Offen aus #31: `skills/CLAUDE.md` als Klasse A aufnehmen oder aus dem Lock streichen
- [ ] pkp-interne Commits (`UI.md` + `project-design/`-Tokens) — separates Repo

---

## Session 2 - 08:10

*(Parallel-Session am Arbeitsplatz. Lief bereits, als Session 1 zuhause committet wurde — daher
zeitlich nach S1 protokolliert, obwohl sie frueher begann.)*

### Implementiert

- **Idee #32 erfasst: `dtb:feature-discover` Schritt 2 hat kein Aufnahmekriterium fuer
  `## Betroffene Module`.** Aufgefallen am eigenen Fehler — die Scan-Tabelle enthielt Pfade ins
  Nachbar-Repo `10x-devs-3.0` (reine Lesequelle). `dtb:impl-plan` parst diesen Abschnitt als
  **projekt-relative Aenderungsziele**; der Wartungs-Hinweis im Skill warnt nur vor **Format**-
  Aenderungen, nennt kein Inhalts-Kriterium, und Spalte „Relevanz" + Ueberschrift „potenziell
  betroffene Dateien" lesen sich als „thematisch relevant" statt „wird geaendert".
  **Bestandspruefung belegt den Systemfehler: 4 von 20 `discovery.md` tragen Fremdpfade** —
  `commit-and-push`, `archive/impl-review/` (oma-personas), `archive/lesson-loop/` (frameworks),
  und `archive/greenfield-autoren-skills/`, wo ad hoc ein Praefix **„Extern:"** erfunden wurde.
  Der eigene Fehler war also der Normalfall, nicht die Abweichung.
- **#15-Discovery gestartet und bewusst abgebrochen** (Nutzer-Entscheidung: braucht
  zusammenhaengenden Fokus). Status `In Arbeit` → `Offen`. Kein `features/`-Ordner, kein
  `discovery.md` — Schritt 6 nie erreicht, der Abbruch ist spurenfrei.
- **Zweiter #15-Befund: das Vorbild ist ein Kettenglied, kein Einzel-Skill.**
  `10x-shape → 10x-prd → 10x-stack-assess → 10x-health-check`. Step 3a liest
  `context/foundation/stack-assessment.md` von `10x-stack-assess` (18 KB): vier Gates (typed,
  convention-based, popular in training data, well-documented) plus **Kompensations-Bloecke fuer
  CLAUDE.md/AGENTS.md** statt Stack-Austausch. Das Kit hat kein Pendant — Greenfield-Ast existiert,
  **Brownfield-Ast fehlt komplett**. Folge: ein Kit-`health-check` erbt einen Schritt, der ins Leere
  greift, und der wertvollste Teil (Kompensationspfad → CLAUDE.md) sitzt im fehlenden Nachbar-Skill.
  Damit ist #15 keine Skill-Frage mehr, sondern eine **Ast-Entscheidung**. Alle sechs
  Entscheidungen sind im Idee-Text verankert; das ueberholte „VOR dem Bau pruefen" ist dort
  korrigiert — der in S1 als „unveraendert offen" notierte Punkt ist damit erledigt.
- **Doppelarbeit an #16 aufgedeckt und aufgeloest.** Dieselbe Idee wurde parallel an beiden
  Arbeitsplaetzen verworfen — mit identischem Ergebnis, aber unterschiedlicher Beleglage.
  Der S1-Text (zuhause) bleibt, weil er **besser belegt** ist: Zeilenverweise
  `opportunity-map SKILL.md:78/:83/:145-157`, die Gegenprobe `grep -rni 'mom'` → null Treffer,
  und der praezisere Abgrenzungspunkt (Selbstauskunft ueber **eigene** Reibung vs. Verzerrung im
  Gespraech mit **Dritten**). Die hier erarbeitete Fassung stuetzte sich nur auf die Leitplanken
  bis Zeile ~70 und leitete daraus einen „Schwellen fehlen"-Restpunkt ab — **widerlegt**:
  `:145-157` enthaelt bereits ein Go/No-Go mit 6 Ranking-Kriterien, drei davon verhaltensbasiert.
  Es fehlen nur *numerische* Schwellen, und genau das steht im S1-Text schon als
  Wiedereinstiegspunkt. Der Restpunkt entfaellt.
- **Abnahme-Abfrage: keines der vier Features abgenommen** — alle bleiben „Fertig zum Testen".

### Dateien

- `dtb-project/project-workflows/INBOX.md` — #32 neu (`Offen`); #15 `In Arbeit` → `Offen` +
  Kettenbefund, sechs Discovery-Entscheidungen und Korrektur des ueberholten „VOR dem Bau pruefen".
  #16 unveraendert aus S1 uebernommen
- `dtb-project/project-changelog/2026-07/2026-07-28.md` — diese Session
- `WORKFLOW_STATUS.md` / `BACKLOG.md` — auf den zusammengefuehrten Stand gebracht

Committet und gepusht als `54fbade` (`df7232c..54fbade`, 4 Dateien, 108 +/12 −). Red-Flag-Scan
sauber (4 Markdown-Dateien, groesste 22 KB, keine Secret-Muster, keine untracked Pfade); ein
eindeutiger Git-Root, kein Monorepo-Sonderfall.

### Kontext

**Zwei Arbeitsplaetze gleichzeitig, ein Repo.** Diese Session lief am Arbeitsplatz, waehrend
zuhause S1 entstand und als `df7232c` gepusht wurde. Der Zusammenfuehrungs-Aufwand blieb klein
(0 lokale Commits, 1 Remote-Commit voraus, Fast-Forward moeglich), weil hier noch nichts committet
war — aber #16 wurde doppelt bearbeitet. Ablauf der Aufloesung: lokale Aenderungen gestasht und
zusaetzlich als Patch gesichert, `pull --ff-only`, dann nur die Substanz nachgetragen, die es
remote nicht gab (#32, #15-Kettenbefund + Vertagung). Der staerker belegte S1-Text zu #16 blieb.

Inhaltlich wiederholte sich das Muster der Vortage: die Session begann als Discovery fuer #15 und
wurde **zweimal durch Nutzer-Rueckfragen umgelenkt** — erst „warum stehen da Pfade aus
10x-devs-3.0?" (→ #32 mit Bestandsbefund), dann „was steht in `stack-assessment.md`?" (→ der
Kettenbefund, der #15 vom Skill zur Ast-Entscheidung macht). Drittes Vorkommen in fuenf Tagen,
dass eine Verstaendnisfrage den Zuschnitt der Aufgabe verschiebt.

**Lehre fuer die Doppelarbeit:** Beide Sessions haben #16 unabhaengig korrekt entschieden — teuer
war nicht der Irrtum, sondern die doppelte Herleitung. Ein Blick auf `origin/master` beim
Session-Start haette es gezeigt; `dtb:workflow-resume` prueft heute nur den lokalen Git-Stand
(`branch --show-current`, `log`, `status`), nicht die Divergenz zum Remote.

### Naechste Schritte

- [ ] **Erfassen: `workflow-resume` um einen Remote-Divergenz-Check erweitern** (`git fetch` +
      `rev-list --left-right --count`), damit Parallelarbeit an zwei Rechnern beim Session-Start
      sichtbar wird statt erst beim Commit — Lehre dieser Session
- [ ] `/dtb:idea-review` fortsetzen — 10 offene Ideen; **#15 dabei ueberspringen** (vertagt).
      Naechstliegend: **#27** (`grill-me` global bereits installiert, ohne Bau entscheidbar) und
      **#25** (Fach-Agenda, fehlendes Glied der Kette #13→#26→#25→#24)
- [ ] Entscheiden: `opportunity-map` traegt `stage: greenfield` im Frontmatter, widerspricht damit
      dem eigenen Brownfield-Hinweis vier Zeilen weiter; `workflow-status` liest die Stage fuer die
      Flow-Visualisierung
- [ ] `git stash drop stash@{0}` — Sicherungsnetz der Zusammenfuehrung, seit `54fbade`
      gegenstandslos. Achtung: `stash@{1}` (`ac81265`, generate-rules-Templates) ist aelter und
      stammt nicht aus dieser Session — nicht mit wegwerfen
- [ ] `#15` bleibt vertagt — Kontext vollstaendig im Idee-Text
- [ ] build-check entscheiden: streichen (Option A nachholen) oder behalten — braucht den
      pkp-Nutzungsbeleg
- [ ] Reale Abnahmen der 4 „Fertig zum Testen"-Features (haengt am Zielprojekt pkp)
- [ ] Offen aus #31: `skills/CLAUDE.md` als Klasse A aufnehmen oder aus dem Lock streichen
- [ ] pkp-interne Commits (`UI.md` + `project-design/`-Tokens) — separates Repo
