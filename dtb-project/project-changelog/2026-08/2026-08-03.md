# 2026-08-03

## Session 1 - 09:27

*(Erste Session auf der Maschine mit Lock `5b29f81` — dem Rechner, dessen Stand vom 2026-07-31
nie gepusht wurde. Die Sessions vom 2026-08-01/02 liefen auf der anderen Maschine.)*

### Implementiert

- **`/dtb:workflow-resume` hat einen Zwei-Maschinen-Konflikt aufgedeckt statt eines naechsten
  Schritts.** Der Remote-Stand war **19 Commits** voraus (zwei komplette Features: `feature-fast`
  #37 und `meeting-agenda` #25), und **alle vier** lokal geaenderten Dateien wurden dort ebenfalls
  angefasst. Der Handoff-Befehl der lokalen Statusdatei (`/dtb:feature-discover` fuer #29) war
  damit ueberholt — die ungepullte Fremdarbeit hatte den Strang bereits ueberholt. Genau der Fall,
  fuer den der Drei-Punkt-Diff im Resume 2026-07-29 eingebaut wurde.
- **Verlustsicherer Weg gewaehlt statt schnellem:** Sicherungs-Branch
  `backup/2026-08-03-pre-merge`, dann `pull --no-rebase` (Merge statt Rebase, damit beide
  Ausgangs-Commits unveraendert in der Historie bleiben). Begruendung im Chat: bei einem Rebase
  waere der eigene Commit neu geschrieben und im Fehlerfall nur ueber `reflog` erreichbar.
- **`/dtb:commit-and-push` (`ecb10ea`):** der ungesicherte Stand 2026-07-31 zuerst committet —
  Red-Flag-Scan ohne Treffer, 5 Dateien, heterogener Diff korrekt als solcher geflaggt. Bewusst
  **vor** dem Pull, damit die untrackte `2026-07-31.md` den Pull nicht hart abbricht
  („untracked working tree file would be overwritten") sondern als regulaerer Merge-Konflikt laeuft.
- **Vier Merge-Konflikte als Union geloest, nichts verworfen** (`594124c`, gepusht):
  - `2026-07-31.md` — **zwei unabhaengige Fassungen desselben Tages**: das Original (126 Z., aus
    dem Chat-Verlauf) und eine Rekonstruktion der anderen Maschine (64 Z., am 2026-08-01 aus
    Commits gebaut, „nicht aus einem Chat-Verlauf"). Jede traegt Exklusives — nur lokal: beide
    kit-sync-Laeufe, #31-Fall, idea-review #29, L14-Erfassung; nur remote: Drift-Bilanz 10/12
    MATCH, Findings-Achsen, `review.md` +234/-105. Original als Session 1, Rekonstruktion als
    gekennzeichneter `## Nachtrag` darunter.
  - `INBOX.md` — #29 `In Arbeit` (nur lokal bekannt) + #28 `Ausgearbeitet` mit erweitertem Text
    (nur remote bekannt); alle 20 Ideen-Zeilen intakt.
  - `WORKFLOW_STATUS.md` — Remote-Fassung als neuere Basis, lokale Punkte eingearbeitet;
    „L7 und L8 heben" gestrichen (erledigt), Handoff auf `kit-sync` vorgezogen.
  - `BACKLOG.md` — Kopfzeile nennt beide Straenge, beide Distributions-Angaben als
    maschinen-lokal markiert (das „hier/dort" der zwei Fassungen widersprach sich).
  - `skills/CLAUDE.md` wurde **automatisch** vereinigt; L7/L8/L14 per Grep verifiziert 3/3.
- **`/dtb:kit-sync sync`: 10 Artefakte uebernommen** (8 Updates + 2 neu), Lock `5b29f81` →
  `594124c`, 41 → 43 Artefakte, Verifikation **43/43 synchron, 0 verwaist**. Die zwei neuen Skills
  `dtb:feature-fast` und `dtb:meeting-agenda` waren auf dieser Maschine **gar nicht installiert** —
  eine Abnahme von `meeting-agenda` haette vorher gegen eine nicht existierende Fassung gelaufen.
  **Unabhaengige Gegenprobe:** `git diff --name-only 5b29f81..594124c -- skills agents commands`
  liefert exakt dieselben 10 Dateien wie die Hash-Klassifikation.
- **Inhaltskontrolle statt blossem Hash-Vergleich** an allen 10 Dateien, je an einer Wirkstelle
  aus dem Merge-Diff: `feature-fast` Eskalations-Schwelle, `meeting-agenda`
  `## Offene Punkte`-Filter + `archive/`-Ausschluss, Fast-Track-Weichen in
  `feature-discover`/`feature-plan`/`impl-plan`, Gegenkante `open-question → meeting-agenda` in
  `pipeline-graph:190`, Schritt 4b + `after: [dtb:idea-review]` in `task`.
- **`/dtb:backlog-status`:** 7 Features, alle Y/Y, kein Feature in Arbeit, keine Feld-Konflikte.
- **Abnahme `commit-and-push`: `Abgenommen` mit dokumentiertem Vorbehalt.** Beleg: mehrfach
  produktiv im Einzel-Repo, zuletzt `ecb10ea`. Das Verifikations-Gate aus
  `workflow-checkpoint` 2.3 wurde vorgezogen und alle 6 `#### Manual`-Kriterien gegen den realen
  Betrieb abgeglichen — **3 belegt** (Bestaetigungs-Muster, gepusht, kit-sync/Lock synchron),
  **1 teilweise** (heterogener Diff real geflaggt; non-fast-forward, kein Tracking-Branch,
  Detached HEAD, clean+up-to-date nie real ausgeloest), **2 unbelegt** (Multi-Root-Gruppierung
  Monorepo/Poly-Repo + Windows-Pfad-Normalisierung — in diesem Einzel-Repo strukturell nicht
  pruefbar, haengt am Monorepo pkp).

### Dateien

- `dtb-project/project-changelog/2026-07/2026-07-31.md` — Original + `## Nachtrag` (196 Z.)
- `dtb-project/project-workflows/{WORKFLOW_STATUS,BACKLOG,INBOX}.md` — Union beider Straenge
- `dtb-project/project-workflows/features/commit-and-push/spec.md` — `**Status:** Abgenommen`
  als **expliziter** Zustand (§1.2) samt Abnahme-Beleg, Vorbehalt und offenem Befund; der
  HTML-Kommentar wurde von „abgeleitete Anzeige, wird synchronisiert" auf „nicht zurueckzusetzen"
  geaendert — sonst haette der naechste Checkpoint das Feld auf „Fertig zum Testen" gezogen
- `skills/CLAUDE.md` — Lektionen-Hebung L7/L8/L14 (aus `ecb10ea`) + remote Konventionszeile
- `~/.claude/` (ausserhalb des Repos): 10 Skill-Kopien (8 aktualisiert, 2 neu),
  `dtb-lock.json` → `594124c`/43 Artefakte, Sicherung `dtb-lock.json.bak.5b29f81`

**Zwei Commits, gepusht** (`c6f0708..594124c`). Uncommittet bei Session-Ende: die drei
Abnahme-Dateien + dieser Checkpoint.

### Kontext

Die Session hat kein Feature gebaut, sondern einen **Verlust verhindert**. Der 31.07. existierte
in zwei Fassungen, weil die andere Maschine die Doku-Luecke rekonstruierte, ohne den Original-Log
zu kennen — beide Seiten hatten Recht, keine war vollstaendig. Das ist der bislang klarste Beleg
fuer Idee #34 (Zwei-Maschinen-Divergenz) und schaerft auch #29: der Verlust drohte nicht im Chat,
sondern **zwischen zwei Rechnern**.

Zweiter Faden: **zwei eigene Pruefkriterien waren falsch angesetzt** und wurden korrigiert statt
durchgewunken. (a) „`pipeline-graph` muss `feature-fast` enthalten" — 0 Treffer, aber die
Repo-Fassung hat ebenfalls 0, weil der Doku-Fix noch offen ist; das Kriterium war nicht an der
Wirkstelle geankert (L8). (b) „`task` muss `#28` nennen" — die #28-Routing-Aufgabe ist ein offener
Punkt, kein Skill-Inhalt. Dazu ein echter Werkzeug-Fehlschlag: Windows-Python konnte nicht nach
`/tmp` schreiben, das Zeilenzahl-Gate fing die leere Lock-Seite ab, bevor sie als
„41 Abweichungen" gelesen wurde (L7, dritter Fall dieser Klasse).

Dritter Faden: Das Abnahme-Gate hat funktioniert. Der Zuruf „kann als abgenommen betrachtet
werden" haette ohne Gate ein glattes „Abgenommen" ergeben; mit Gate steht jetzt ein Status mit
belegtem Umfang und benanntem Vorbehalt. Nebenbei aufgefallen: `#### Manual`-Boxen sind bei
**allen sieben** Features 0/n abgehakt — „unabgehakt" heisst hier also nicht „ungeprueft", und ein
Report, der daraus „alle Kriterien offen" schliesst, waere falsch.

### Naechste Schritte

- [ ] **`/dtb:meeting-agenda` in frischer Session** — jetzt gegen die installierte Fassung
      (Lock `594124c`, 43/43). Bestand ist duenn: genau **eine** offene `[Fach]`-Frage
      (`features/fachfragen-erfassung/spec.md`)
- [ ] `/dtb:task` fuer #28 — schliesst zugleich den Widerspruch, dass #28 auf `Ausgearbeitet`
      steht, aber kein Artefakt hat (alle sieben anderen `Ausgearbeitet`-Ideen verweisen auf einen
      Change-Ordner). Sechster EOL-Beleg heute beim Merge
- [ ] **`commit-and-push` Schritt 6: `behind > 0` als Auffaelligkeits-Stopp** — Abnahme-Befund:
      Schritt 2 erhebt ahead/behind, nutzt es nur fuer „Push ja/nein"; bei `behind 19` musste der
      Nutzer den Lauf selbst unterbrechen. Schritt 6 kennt nur „mehr als ein Remote" / „≠ origin"
- [ ] **`DERIVED_STATE_RULES.md` §1.2 vs. Kit-Praxis** — §1.2 sagt „Abgenommen / Abgeschlossen —
      nur via `/dtb:archive`", `workflow-checkpoint` 2.3 setzt „Abgenommen" aber selbst (mit
      Beleg-Gate); die BACKLOG-Legende trennt beide korrekt, der Kanon nicht. Nach dem Buchstaben
      von §1.2 waere die heutige Abnahme nicht zulaessig gewesen
- [ ] `/dtb:feature-discover` fuer #29 — Praxisbelege jetzt aus zwei Sessions
- [ ] Multi-Root-Abnahme `commit-and-push` im Monorepo pkp — der Vorbehalt von heute
- [ ] `/dtb:idea-review` — 10 offene Ideen (#35/#34/#33, dann #32 → #22)
- [ ] Sicherungs-Branch `backup/2026-08-03-pre-merge` loeschen, wenn der Merge sich bewaehrt hat
- [ ] `.gitattributes`, `#31` generisch, `review.md`-Kopf, `pipeline-graph:197`,
      `implement:157`, `project-health`-Check, `project-init`-Zweitlauf — unveraendert offen
- [ ] `workflow.config.yaml` + `ROADMAP.md` sind **beide** reine Platzhalter (heute bestaetigt) —
      ausfuellen oder als Vorlage belassen entscheiden

---

## Session 2 - 13:03

### Implementiert

- **Abnahme `meeting-dump`: `Abgenommen`.** Beleg (Nutzer): in einem Zielprojekt durchgespielt —
  Projekt und Datum wurden nicht protokolliert, der Lauf ist von diesem Repo aus nicht nachpruefbar.
  Hier mechanisch verifiziert wurden die Manual-Kriterien der Phasen 1+2 (§6-Abgrenzung
  `→ Antwort:` unter `- [x]` vs. `→ Zwischenstand:` unter `- [ ]`, beidseitig explizit;
  Frontmatter-Konventionen vollstaendig). **Gegenprobe in diesem Repo negativ:**
  `project-meetings/` existiert nicht, keine `- [x] [Fach]`-Zeile, kein angewandtes
  `→ Antwort:`/`→ Zwischenstand:` — die Phase-3-Kriterien (Probelauf, 0-Fragen-Pfad,
  Rueckschreibung) bleiben hier unbelegt.

- **Abnahme `open-question`: `Abgenommen`, alle 6 Manual-Kriterien belegt.** K1-K4 heute an der
  **installierten** Kopie gegengeprueft (Ziel-Routing alle vier Ausgaenge; `Status-Neutralitaet
  (Invariante)` Schritt 5.4; drei Spec-Randfaelle vollstaendig; `Bestehende Eintraege unangetastet`
  Schritt 5.3). K5/K6 aus dem Changelog 2026-07-19: P2-Smoke gruen am Wegwerf-Fixture (`ddde4e9`),
  installierter Command-Smoke in Phase 3. **Kein Vorbehalt** — der im Juli notierte „erste echte
  Fach-Frage-Einsatz im Alltag" war ein Wunsch, kein Plan-Kriterium.

- **Kriterien-Pruefung + Abnahme `plan-status-feld` und `fachfragen-erfassung`: beide
  `Abgenommen`, 3/3 Kriterien je Feature, kein Vorbehalt.** Methode: `#### Manual`-Kriterien gegen
  Kanon, **installierte** Skill-Kopien und Changelog-Belege — statt gegen die Checkboxen, die im
  Bestand nie geflippt werden.
  - `plan-status-feld`: §1-§7 lueckenlos und §7 additiv am Ende (Z. 276/395); Schritt 4/5 in
    `plan-review` unangetastet, Schreibschritt 6 am Schritt-5-Ausgang, F1-Wirkstelle korrekt
    (`📝` ausserhalb des Output-Blocks); alle **5 Trockenlauf-Faelle** gegen die installierten
    Leser durchgespielt — Matrix, 10-Zeilen-Fenster, `0/Y`-Guard und Konflikt-Praedikat sind in
    `workflow-next` **und** `workflow-status` praesent. Ausfuehrungsbeleg zusaetzlich vorhanden
    (Schritt 3.4 E2E-Smoke, Changelog 2026-07-30) — das „menschliche Urteil" des Kriteriums war
    damit nicht noetig.
  - `fachfragen-erfassung`: §6 deckt alle vier Aspekte einzeln verortet und widerspruchsfrei zu
    §1/§2 (dortige Checkbox-Aussagen sind auf `## Progress` begrenzt, §2 schon im Titel);
    Anleitung in `feature-discover:243`/`feature-plan:100` imperativ an der Schreibstelle mit
    ausdruecklich zulaessigem untagged-Fall; blinder Trockenlauf im Changelog 2026-07-17 belegt.
- **Muster erkannt und dreifach korrigiert:** Bei `open-question`, `fachfragen-erfassung` und
  `plan-status-feld` stand jeweils eine **Erwartung** als Abnahme-Bedingung im Status
  („erster Fach-Frage-Einsatz im Alltag", „naechste feature-discovery nutzt `[Fach]`", „reale
  Abnahme"), die in keinem `#### Manual`-Kriterium des Plans steht. Wuensche hatten sich zu
  Blockern verfestigt und die Warteschlange kuenstlich verlaengert.


### Dateien

- `features/{meeting-dump,open-question,plan-status-feld,fachfragen-erfassung}/spec.md` —
  `**Status:** Abgenommen` als **expliziter** Zustand (§1.2) mit vollstaendiger Kriterien-Begruendung;
  der HTML-Kommentar wurde je von „abgeleitete Anzeige, wird synchronisiert" auf
  „NICHT von workflow-checkpoint zurueckzusetzen" geaendert — ohne das haette der naechste
  Checkpoint alle vier Felder wieder auf „Fertig zum Testen" gezogen
- `dtb-project/project-workflows/BACKLOG.md` — vier Status-Spalten auf `Abgenommen` mit
  Kurzbegruendung (Vorbehalt / Beleg-Luecke / „alle N Kriterien belegt")
- `dtb-project/project-workflows/WORKFLOW_STATUS.md` — Status-Tabelle (5x Abgenommen),
  Restabnahmen-Zeile praezisiert, Meilenstein + Kontext nachgezogen; 80 Zeilen gehalten
- `dtb-project/project-changelog/2026-08/2026-08-03.md` — die vier Abnahme-Bloecke aus Session 1
  nach hier verschoben (sie entstanden nach dem 09:27-Checkpoint; 155 Zeilen vorher/nachher
  identisch, keine verloren)

### Kontext

Die Session hat die **Abnahme-Warteschlange von 7 auf 2 reduziert** — ohne eine Zeile Code, allein
durch Pruefen. Der Grund war ein systematischer Fehler in der Buchfuehrung: `WORKFLOW_STATUS.md`
fuehrte bei drei Features eine **Erwartung** als Abnahme-Bedingung, die in keinem
`#### Manual`-Kriterium des Plans steht. `plan-status-feld` lag mit Prio **Hoch** seit dem
2026-07-30 abnahmereif da, blockiert von der Formulierung „reale Abnahme"; `fachfragen-erfassung`
seit dem 2026-07-17, blockiert von „naechste feature-discovery nutzt `[Fach]`". Beide Belege
existierten laengst in ihren Bau-Changelogs.

Das Verifikations-Gate hat dabei drei **verschiedene** Ergebnisse geliefert, was gegen die
Vermutung spricht, es winke nur durch: `open-question`/`plan-status-feld`/`fachfragen-erfassung`
ohne Vorbehalt (alle Kriterien belegt), `commit-and-push` mit Vorbehalt (Multi-Root nie gelaufen),
`meeting-dump` mit Beleg-Luecke (Zielprojekt-Lauf nicht protokolliert, von hier nicht nachpruefbar).
Bei `meeting-dump` war die Gegenprobe im Repo sogar **negativ** — `project-meetings/` existiert
nicht, keine `- [x] [Fach]`-Zeile: der Lauf kann nur ausserhalb passiert sein.

Methodisch zweimal das eigene Werkzeug korrigiert (L7/L8-Klasse, heute insgesamt viermal): das
Grep-Muster fuer das Konflikt-Praedikat fand nichts, weil der Satz in beiden Lesern ueber zwei
Zeilen umbricht — beinahe ein Schein-Befund „Praedikat fehlt". Und die `#### Manual`-Boxen sind bei
**allen sieben** Features 0/n abgehakt; ein Report, der daraus „alle Kriterien offen" schliesst,
waere falsch — sie werden im Bestand grundsaetzlich nicht geflippt.

Reihenfolge-Entscheidung am Session-Ende: **Checkpoint vor `commit-and-push`**, weil
`workflow-checkpoint` Dateien schreibt und selbst nicht committet (keine `git commit`-Anweisung im
Skill) — umgekehrt braeuchte dieselbe Arbeitseinheit zwei Commits.

### Naechste Schritte

- [ ] `/dtb:commit-and-push` — 7 Dateien + dieser Log; der Push geht jetzt glatt (Remote eingeholt)
- [ ] **Restabnahme `meeting-agenda`** — `/dtb:meeting-agenda` in frischer Session: liefert
      Probelauf **und** Negativ-Test (die `[Fach]`-Doku-Zeilen aus `fachfragen-erfassung/discovery.md`
      duerfen NICHT in der Agenda erscheinen). Bestand: genau 1 offene `[Fach]`-Frage
- [ ] **Restabnahme `feature-fast`** — braucht zwei UX-Urteile („Veto realistisch?", „nichts
      Wichtiges verdeckt?") plus einen Lauf mit der gefixten Fassung (`1eec2ea`)
- [ ] **`DERIVED_STATE_RULES.md` §1.2 klaeren — jetzt fuer 5 Features relevant:**
      `workflow-next` und `workflow-resume` kennen „Abgenommen" nicht (0 Treffer), `workflow-status`
      behandelt es korrekt (`:34`). Beim naechsten Resume werden alle fuenf entweder als
      „Fertig zum Testen" gefuehrt oder als Feld-Konflikt gemeldet. Dazu der Kanon-Widerspruch:
      §1.2 sagt „nur via `/dtb:archive`", `workflow-checkpoint` 2.3 setzt es selbst
- [ ] **`/dtb:archive` erwaegen** — 5 Features tragen jetzt `**Status:** Abgenommen` und erfuellen
      damit die Archiv-Vorbedingung; dazu 9 INBOX-Eintraege und 19 BACKLOG-Zeilen
- [ ] `/dtb:task` fuer #28 · Nachtrag Projekt+Datum des `meeting-dump`-Laufs · Multi-Root-Abnahme
      `commit-and-push` im Monorepo pkp · `/dtb:idea-review` (10 offene Ideen)
