# Session-Log 2026-08-11

## Session 1 - 09:15

### Implementiert

- **`/dtb:workflow-resume`: die Handoff-Gueltigkeitsbedingung hat zum zweiten Mal
  funktioniert.** Der Handoff vom 2026-08-10 nannte `/dtb:commit-and-push` mit der Klausel
  „gilt, solange `git status --short` nicht leer ist" — beim Resume war der Arbeitsbaum sauber
  und `master` 0/0 zu `origin/master`, die Bedingung also verfallen. Der im selben Block
  mitgeschriebene Nachfolge-Schritt (L20-Bestandsnachlauf) griff ohne Nacharbeit. Zweiter
  Wirksamkeitsbeleg fuer L23 nach dem 2026-08-10.
- **Abgeleiteter Stand unveraendert:** vier Features je Y/Y (`feature-fast` 11/11,
  `output-style-gezielt` 11/11, `feature-start-statusfeld` 5/5, `meeting-agenda` 5/5) →
  durchgehend „Fertig zum Testen", **kein Item „In Arbeit"**, keine Feld-Konflikte. Keine
  Bugs, keine Aufgaben (beide Aufgaben-Ordner am 2026-08-10 archiviert). INBOX: 34 Eintraege,
  **alle** `Offen` — kein `In Arbeit`, kein `Ausgearbeitet`.
- **`/dtb:workflow-next`:** alle vier Items stehen an derselben Pipeline-Position
  (Restabnahme), die Sicht kann sie nur nach Prio ordnen — keine Entscheidungshilfe.
  Nebenbefund zur Praxis von #33.
- **`/dtb:idea-review` gestartet, nach der Uebersicht abgebrochen** (34 offene Ideen gelistet,
  **keine** Statusentscheidung getroffen, INBOX unveraendert). Der Lauf wurde auf eine
  Detailfrage umgelenkt — dritter Beleg desselben Musters, das #33 beschreibt.
- **Worktree-Frage geklaert:** #62 ist der direkte Treffer (Ort der Minimal-Notiz bei hartem
  Worker-Stopp — Haupt-Repo vs. Worktree), #42 nennt Git-Worktrees nur als Kandidaten unter
  mehreren. Keine offene Aufgabe (`task.md`) beruehrt Worktrees.
- **Fremder Nachtrag an INBOX #42 entdeckt — mitten in dieser Sitzung.** `INBOX.md` wurde
  **08:56:44** geaendert, also **nach** beiden Lesevorgaengen dieses Laufs; Urheber ist laut
  Nachtragstext eine parallele Session im pkp-Projekt. Inhalt: (1) Empfehlung der **nativen
  CC-Worktree-Tools** (`EnterWorktree`/`ExitWorktree`, Subagents mit `isolation: worktree`)
  statt manueller `git worktree`-Befehle; (2) Derived-State-Analyse —
  `features/<slug>/`-Artefakte sind worktree-sicher (mergen additiv), Konfliktpotenzial liegt
  ausschliesslich bei `WORKFLOW_STATUS.md`, `BACKLOG.md`, `INBOX.md` und der
  Changelog-Tagesdatei, echtes Verlustrisiko nur bei nicht ableitbaren Inhalten (Prosa,
  Handoff, Prio/Ziel, INBOX-Eintraege); (3) Guard-Vorschlag „eine Anlaufstelle" — global
  schreibende Skills brechen ab, wenn `git rev-parse --git-dir` ≠ `--git-common-dir`,
  Feature-Artefakt-Skills bleiben worktree-faehig.
- **`/dtb:commit-and-push`:** ein Startpfad (`.`) → **1 eindeutiger Root**, kein
  Monorepo-Sammelfall. Arbeitsbaum clean, `ahead 1` → Commit-Schritt korrekt **uebersprungen**
  (kein Leer-Commit), nur Push `5b03e57..6d3f619` nach `origin/master`. Der Nachtrag war zuvor
  als `6d3f619` committet worden (1 Datei, 1 Zeile).
- **Verlustpruefung: 3 Funde, noch NICHT abgesetzt** — zwei Lektionen (instabiler Lesestand
  bei parallelen Sessions; master-Commit-Konvention des Kit-Repos) und eine INBOX-Ergaenzung
  zu #42 (Lese-Seite des Guards). 3 Kandidaten als erfasst gefiltert.

### Dateien

- `INBOX.md` — #42 um den Nachtrag ergaenzt (**fremdgeschrieben** aus der pkp-Session, hier
  nur committet); 34 Eintraege, alle `Offen`, Nummernstand unveraendert
- `2026-08/2026-08-11.md` — dieser Log (neue Datei, Session 1)
- `WORKFLOW_STATUS.md` — neu geschrieben
- `BACKLOG.md` — nur „Letzte Aktualisierung"; Status-Spalte unveraendert (Ableitung stabil)
- ROADMAP-§5-Sync lief zum **zehnten** Mal leer (unausgefuellte Vorlage, keine Change-IDs)

### Kontext

Die Sitzung hat nichts implementiert, aber zwei Dinge belegt.

**Erstens: #42 hat sich selbst einen neuen Beleg geliefert — und zwar auf der Lese-Seite.**
Der Nachtrag entstand, waehrend hier ein `/dtb:idea-review` lief. Der Nutzer fragte zweimal
nach dem Inhalt von #42; beide Antworten stammten aus einem Lesestand, der beim zweiten Mal
schon ueberholt war, und die Abweichung fiel erst auf, weil der Nutzer selbst nachfragte, ob
der Eintrag „vor ein paar Minuten" aktualisiert wurde — nicht durch einen Skill-Schritt. Der
im Nachtrag vorgeschlagene Guard adressiert ausschliesslich **schreibende** Skills; genau
diese Konstellation faengt er nicht. Damit betrifft #42 nicht nur Worktrees, sondern jede
Parallelitaet ueber Projektgrenzen (die Fremdsession lief in einem **anderen** Projekt und
schrieb trotzdem in die globale INBOX dieses Repos). → Verlustfund 1 + 2, noch offen.

**Zweitens: der Handoff-Verfall ist reproduzierbar.** Was am 2026-08-10 einmal aufging, hat
heute erneut funktioniert, diesmal mit umgekehrtem Ausgang — die Bedingung war **verfallen**,
und der Block sagte selbst, was dann gilt. Der Fall „Bedingung greift nicht mehr" ist damit
belegt, nicht nur der Fall „Bedingung haelt". Fuer #35 ist das der Wirksamkeitsnachweis von
Loesungsvariante (a) in ihrer prosaischen Minimalform.

**Nicht getestet, nichts abgenommen:** die vier Restabnahmen stehen unveraendert. Der
Standardzustand „Fertig zum Testen" wurde daher nicht erneut zur Abnahme abgefragt (die Frage
haette in dieser Sitzung keine neue Antwort erzeugt) — die Belegpflicht bleibt offen.

**Einschaetzung zu #42, damit sie nicht neu verhandelt werden muss:** der Nachtrag hebt den
Eintrag von „Idee" auf „ausgearbeitet genug fuer eine Schiene". Empfehlung ist die
**Voll-Schiene** (`/dtb:feature-discover`), nicht der Fast-Track: betroffen sind sieben global
schreibende Skills plus ein neuer Key in `workflow.config.yaml`, und die Spiegel-Kopplung
ueber mehrere SKILL.md-Dateien ist genau die Konstellation, die im Kit zweimal Folgefehler
erzeugt hat.

### Naechste Schritte

- [ ] **3 Verlustfunde dieser Sitzung absetzen** — 2x `/dtb:lesson` (instabiler Lesestand;
      master-Commit-Konvention), 1x `/dtb:idea` (#42 Lese-Seite des Guards)
- [ ] **`/dtb:idea-review` fortsetzen** — 34 offene Ideen, heute nur die Uebersicht; #42 waere
      als erstes einzustufen (Empfehlung: Voll-Schiene)
- [ ] **L20-Bestandsnachlauf** — `feature-start-statusfeld/plan.md:101` +
      `meeting-agenda/plan.md:104`; zahlt auf zwei der vier Restabnahmen ein
- [ ] **L22 + L23 nach `skills/CLAUDE.md` heben** — beide `Applies-to: alle` (#64)
- [ ] **9 Funde aelterer Verlustpruefungen absetzen** — 4 vom 2026-08-07 S1 + 5 vom 2026-08-06
- [ ] Restabnahmen `feature-fast` · `meeting-agenda` · `output-style-gezielt` ·
      `feature-start-statusfeld`
- [ ] #66 · #65 · #64 · #63 · #62 · #59 · #58 · #57 · #56 · #55 · #54 · #53 · #52 · #51 ·
      #42 · #35 · #33 entscheiden
- [ ] Config-Platzhalter (`workflow.config.yaml`) fuellen · ROADMAP-Vorlage fuellen oder
      entfernen (10x leer gesynct) · pkp intern committen (`UI.md` + Tokens)
