# Changelog 2026-08-07

## Session 1 - 11:06

### Implementiert

- **`output-style-gezielt` (#40): von der Idee bis 9/11 in einer Session.** Kette:
  `idea-review` (Dreier-Weiche → Voll-Schiene) → `feature-discover` → `feature-plan` →
  `impl-plan` → `plan-review` → `feature-start` → `implement` (Phasen 1+2 komplett, 3.1/3.2).
- **Zuschnitts-Entscheidungen (idea-review + Discovery):** Nur Ebene (b) — Claudes Fliesstext,
  nicht die Skill-Report-Laengen (25/39 Skills tragen eigene Vorgaben, bleibt eigener Zuschnitt).
  Global statt workflow-gebunden → **Hook-Weg entfiel, #45 ist keine Vorbedingung mehr.**
  Kit-Artefakt statt lokale Config (#34-Muster vermieden); Praezedenz bewusst auf Output Styles
  begrenzt (kein Praejudiz fuer #41/#45). Mechanik gegen die offizielle Doku verifiziert:
  `/output-style` seit v2.1.91 entfernt; `/config` speichert projektlokal → globale Geltung
  braucht `outputStyle` in `~/.claude/settings.json`; `keep-coding-instructions: true` Pflicht;
  wirkt nicht in Subagents.
- **`plan-review` (4 Agenten, Betriebs-Waechter aktiviert): REVISE, 4 WARNs, alle behoben.**
  Kern-Findings: Nachlauf-Kriterien (Commit→Push→Sync erst dann gruen, Lektion-1-Muster),
  Session-Bruch zwischen Aktivierung und Kalibrierung (sonst misst 3.3 nichts), Muster-Beleg als
  echter Wegwerf-Test statt Gedankenexperiment, Rueckweg dokumentieren. Vier Nutzer-Entscheide:
  Klassen-Muster auf `output-styles/dtb-*.md` verengt (deckungsgleich mit Verwaisten-Heuristik),
  Wegwerf-Test real, Sprache Deutsch (endlich bestaetigt), zweite Maschine Check-12-getrieben.
- **Phase 1 (`7172b9c`):** `output-styles/dtb-gezielt.md` — 31 Zeilen, deutsch, Frontmatter
  `name: Gezielt` + `keep-coding-instructions: true`; Leitsatz (worum/Stand/Entscheidung),
  Ausnahme-Klausel (Analyse auf ausdrueckliche Bitte), Vorrang-Regel (Skill-Format schlaegt Stil),
  Substanz-Schutz.
- **Phase 2 (`89ede9c`, `b4205e7`, `17b1319`):** kit-sync um die **erste neue Artefakt-Klasse
  ueberhaupt** erweitert (Tabelle + Verwaisten-Logik auf vier Zielverzeichnisse), Spiegel
  `skills/CLAUDE.md` + Root-`CLAUDE.md` mitgezogen. sync-Lauf: `dtb-gezielt.md` als „neu"
  installiert (sync legte `~/.claude/output-styles/` selbst an), Lock 45 @ 0 Abweichungen.
  **Wegwerf-Test:** `dtb-test.md` committet → als „neu" erkannt OHNE kit-sync-Aenderung
  (Muster belegt) → geloescht → als „verwaist" erkannt → entfernt. Beide Pfade in einem Zyklus.
- **Phase 3 teilweise (3.1/3.2, uncommittet):** `project-health` Check 12 („Style installiert,
  aber nicht aktiviert", inkl. Rueckweg-Doku, beide Meldungs-Richtungen am echten Zustand belegt);
  `outputStyle: "Gezielt"` in `~/.claude/settings.json` gesetzt (Backup:
  `settings.json.bak-output-style`).
- **Bootstrap-Erkenntnis:** Der sync-Lauf lief mit der alten installierten kit-sync-Kopie (drei
  Klassen) — massgeblich war die Klassen-Tabelle des gepushten Stands. Selbst-Update-Fall ad hoc
  entschieden, als Lektion-Kandidat gemeldet (Verlustpruefung, nicht abgesetzt).
- **Verlustpruefung (Schritt 0): 4 Funde, 1 gefiltert** — #41-Nachtrag (TTS via Stop-Hook laengst
  umgesetzt), kit-sync-Bootstrap-Lektion, toter `frameworks/`-Verweis in `skills/CLAUDE.md` Z. 216,
  Wegwerf-Test-Muster als Lektion. Nutzer: weiter ohne Erfassung → als offene Aufgabe vermerkt.

### Dateien

- `output-styles/dtb-gezielt.md` — neu (Kit-Artefakt, Klasse A)
- `skills/dtb-kit-sync/SKILL.md` — Klassen-Zeile + Verwaisten-Abschnitt (4 Verzeichnisse)
- `skills/CLAUDE.md` + `CLAUDE.md` — Klasse-A-Muster-Spiegel
- `skills/dtb-project-health/SKILL.md` — Check 12 (Kopfzeile 11→12, Block, Report-Sektion, consumes)
- `features/output-style-gezielt/{discovery,spec,plan}.md` — neu; plan 9/11 mit SHA-Belegen
- `~/.claude/`: Lock 45 @ `17b1319`, `output-styles/dtb-gezielt.md` installiert, `settings.json`
  mit `outputStyle` (ausserhalb des Repos)

### Kontext

Idee #40 („sparsam") wurde im Interview zu „**gezielt**" praezisiert: Orientierung statt Kuerze —
worum geht es, was ist der Stand, was ist zu entscheiden; Gegner ist Vorwegnahme/Redundanz, nicht
Erklaerung. Der Stil wirkt erst nach Session-Neustart, darum endet die Session planmaessig am
**Session-Bruch vor 3.3**: die Kalibrierung in dieser Session wuerde nichts messen.

ROADMAP-§5-Sync lief zum vierten Mal leer (unausgefuellte Vorlage). Die Resume-Diskrepanz vom
Morgen (Status sagte „Lock 44 @ 926ec71, 3d15d0a nicht verteilt", real war der Lock aktuell) ist
ein weiterer #35-Beleg — dort bereits erfasst.

### Naechste Schritte

- [ ] **Neue Session starten** → `/config` pruefen (loest „Gezielt" auf? Annahme 1) →
      `/dtb:implement output-style-gezielt phase 3` (3.3 Kalibrier-Fall-Set, 5 Faelle inkl.
      Gegenprobe `workflow-next` + Ausnahme-Klausel; Fall-Tabelle in DIESEN Log)
- [ ] 3.4 Nachschaerfen + Abschluss-Verteilung (Commit → Push → Sync, Nachlauf)
- [ ] **4 Funde der heutigen Verlustpruefung absetzen** (#41-Nachtrag · Bootstrap-Lektion ·
      `frameworks/`-Verweis · Wegwerf-Test-Lektion) + die 5 Funde vom 2026-08-06
- [ ] `/dtb:archive` — no-loss-check abgenommen + 8 weitere archivierbare Eintraege
- [ ] Restabnahmen `feature-fast` + `meeting-agenda` · Task `gitattributes-eol` ·
      #35/#50/#33/#49 entscheiden

---

## Session 2 (frische Session, Stil „Gezielt" aktiv)

### Kalibrier-Abnahme output-style-gezielt Schritt 3.3 (Fall-Set 5/5 bestanden)

Vorbedingung erfuellt: Session-Bruch nach 3.2; Annahmen-Probe 1 de facto bestanden — der Stil
war beim Session-Start aktiv, `outputStyle: "Gezielt"` loest gegen das Frontmatter-`name` auf.

| Fall | Prompt | Soll | Ist | Verdikt |
|------|--------|------|-----|---------|
| a — Sachfrage | „Welche Artefakt-Klassen kennt kit-sync und wo landen sie?" | Kurze direkte Antwort, keine Vorwegnahme | Klassen-Tabelle (4x Klasse A) + Seeds, keine ungefragten Zusaetze | bestanden |
| b — Entscheidungsfrage | „Soll ich `backup/2026-08-03-pre-merge` jetzt loeschen?" | Kern/Stand/Entscheidung auf einen Blick | Befund: Branch existiert auf dieser Maschine nicht (nur Maschine 2); 2 Optionen; Nutzer waehlte (1) → Status-Aufgabe umformuliert | bestanden |
| c — Analyse-Bitte | „Analysiere ausfuehrlich Vor-/Nachteile INBOX als Change-Folder" | Ausnahme-Klausel greift: ausfuehrlich | Ausfuehrliche Analyse (4 Pro / 6 Contra / Abwaegung) mit Belegen (#39→#44-Kollision, Monsterzellen #35/#44, Capture-Reibung); Verdikt „flache INBOX behalten" | bestanden |
| d — Gegenprobe Skill | `/dtb:workflow-next` | Report-Format unveraendert (Vorrang-Regel) | Fester Report: 5 nummerierte Eintraege ≤ 15 Zeilen, Konflikt-Zeile — Format ungebrochen | bestanden |
| e — Substanz-Schutz | „Kit-Stand: Artefakte im Lock, Stand, Abweichungen?" | Alle Zahlen/SHAs/Pfade vollstaendig | 45 @ `17b1319`, Quelle GitHub, genau 1 Abweichung (`dtb-project-health` seit Lock geaendert, Hashes `eea9aa6c…`≠`f0742a57…`), Einordnung als 3.4-Nachlauf | bestanden |

Nebenbefunde der Session: `WORKFLOW_STATUS.md`-Aufgabe Sicherungs-Branch auf „Maschine 2"
praezisiert (Branch existiert hier nicht); `grep -P`-Locale-Ausfall erneut aufgetreten
(Lektion 7 angewandt: portable Wiederholung statt Datenbefund).

### Implementiert

- **Phase 3 abgeschlossen (`845e356`), Feature 11/11 → Fertig zum Testen.** 3.3 Kalibrier-Abnahme
  (Tabelle oben, 5/5 vom Nutzer bestanden), 3.4 Nachschaerfen entfiel mangels Befund; Nachlauf
  Commit → Push → sync gelaufen, Abschluss-`check` 0 Abweichungen. SHA-Nachtrag fuer 3.1-3.4.
- **`impl-review` (3 Dimensionen, 2 Sub-Agenten): NEEDS ATTENTION, 0 blocking.** Plan Adherence
  **7/7 MATCH**, Scope Discipline **0 EXTRA**, Rules uebersprungen (in `project-rules/` liegen nur
  `DERIVED_STATE_RULES.md` + `lessons.md`, beide per Definition ausgeschlossen). 9 Findings:
  4 non-blocking, 5 nits — **alle gefixt** (`1758671`), Snapshot in `features/output-style-gezielt/review.md`.
  Kern-Fixes: Check 12 behauptete zweimal mehr als er misst (Ueberclaim bei korruptem Lock;
  Report-Zeile `{name}: aktiv` statt „outputStyle gesetzt"); im Style fehlte die Praezedenz
  zwischen Orientierungspflicht und Redundanz-Verbot; staler `Check 5`-Verweis in kit-sync (Bestand).
- **Befund aus der Triage selbst:** Der F8-Fix senkte das Phase-2-Kriterium
  `grep -c "output-styles/dtb-\*" >= 2` auf 1 — ein Gate einer laengst abgeschlossenen Phase wurde
  nachtraeglich rot. Nachgebessert (wieder 2), als **Lektion 13** erfasst; Lektion 12 deckte den
  Fall nicht ab (dort Anweisungen im selben Abschnitt, hier ein Kriterium in einer anderen Datei).
- **Distribution:** `/dtb:kit-sync sync` — 3 Updates uebernommen (`dtb-kit-sync`,
  `dtb-project-health`, `dtb-gezielt.md`), Lock `845e356` → **`1758671`**, Verifikation
  **45/45 synchron, 0 Abweichungen**; Fixes in den installierten Kopien stichprobenhaft belegt.
- **Verlustpruefung (Schritt 0): 3 Funde, 2 gefiltert — erstmals ALLE abgesetzt** (die vier
  Vorsessions liessen ihre Funde liegen): **L13** (Triage-Fix bricht Checkpoint-Kriterium),
  **L14** (Fall-Set-Kalibrierung ersetzt keinen Text-Review — 5/5 bestanden, der Reviewer fand
  den Regel-Widerspruch trotzdem), **INBOX #51** (`impl-review` sollte nach Klasse-A-Fixes auf
  den kit-sync-Nachlauf hinweisen).

### Dateien

- `output-styles/dtb-gezielt.md` — Praezedenz-Klausel (F3), Synonym-Dopplung gekuerzt (F9)
- `skills/dtb-project-health/SKILL.md` — Check 12: Ueberclaim + Report-Zeile + consumes-Notation
  + Trenner + Muster (F1/F2/F5/F6/F7)
- `skills/dtb-kit-sync/SKILL.md` — `Check 5` → `Check 11` (F4), Verwaisten-Klammer generalisiert (F8)
- `features/output-style-gezielt/{plan,spec,review}.md` — 11/11 + SHA-Belege, Status, Review-Snapshot
- `project-rules/lessons.md` — L13, L14 (ungetrackt, bleibt lokal)
- `project-workflows/INBOX.md` — #51 neu
- `~/.claude/`: Lock 45 @ `1758671`, drei Kopien aktualisiert (ausserhalb des Repos)

### Naechste Schritte

- [ ] `output-style-gezielt` im Alltag beobachten → Abnahme im naechsten Checkpoint
- [ ] `/dtb:archive` — `no-loss-check` abgenommen + 10 weitere archivierbare Eintraege
- [ ] **9 Funde aelterer Verlustpruefungen** (4 vom 2026-08-07 S1, 5 vom 2026-08-06) absetzen
- [ ] L11/L12/L13 nach `skills/CLAUDE.md` heben (lessons.md ungetrackt — #34)
- [ ] Restabnahmen `feature-fast` + `meeting-agenda` · Task `gitattributes-eol` starten
- [ ] #51/#50/#35/#33/#49 entscheiden · Config-Platzhalter (`workflow.config.yaml`) fuellen

---

## Session 3 - Nachmittag

### Implementiert

- **`feature-start-statusfeld` (#50): von der Triage bis 5/5 an einem Nachmittag.** Kette:
  `idea-review` (Triage-Sicht statt Einzelentscheidungen — **siebter** manueller #33-Lauf) →
  `feature-fast` → `plan-review` (REVISE, 4 WARNs behoben) → `feature-start` → `implement`
  (2 Phasen) → `impl-review` (**REJECTED**, 9 Findings, alle gefixt).
- **Zuschnitt-Entscheidung:** `feature-start` schreibt Anzeigefelder **gar nicht mehr**, statt
  sie korrekt zu setzen — die Ursache verschwindet, ein Schreiber pro Feld (§1.3). Der Defekt
  war groesser als INBOX #50 beschrieb: **alle drei Lanes** (Feature Z. 96/97, Bug Z. 101-103,
  Aufgabe Z. 107-109), nicht nur Features.
- **Phase 1 (`43e9c9a`):** Schritt 4 „Aktivieren" → „Artefakte lesen"; acht Schreibanweisungen
  entfernt; Frontmatter (`description`, `produces: [features/*/plan.md]`, `allowed-tools` ohne
  `Write`); Ausgabe-Templates + Wichtig-Block. Erhalten: Eligibility-Gate, vier Lese-Schritte,
  Progress-Nachruestung, Auswahlregel-Zeile.
- **Phase 2 (`4047ea3`):** Doku-Spiegel `skills/CLAUDE.md:107` korrigiert (Z. 104 mit der
  korrekten `workflow-checkpoint`-Aussage bewusst unangetastet — Wirkstellen-Ankerung);
  Gegenprobe ueber 30 Erwaehnungen, ein Nachzuegler (`dtb-implement:228`) gefixt.
  Verteilung: Lock `1758671` → `4047ea3`, 45/45 synchron.
- **Verhaltenstests an der installierten Fassung:** realer Start von `gitattributes-eol` +
  Konfliktpruefung → **kein Feldkonflikt** (Ableitung, `task.md` und BACKLOG alle `Offen`);
  Progress-Nachruestung per Wegwerf-Plan getestet und rueckstandsfrei entfernt.
- **`impl-review`: REJECTED — 1 blocking, 4 non-blocking, 4 nits, alle 9 gefixt.**
  **F1 (blocking):** Der umgebaute Skill behauptete an zwei Stellen „einziger Pfleger ist
  `dtb:workflow-checkpoint`" fuer drei Ziele inkl. `bug.md` — der Checkpoint pflegt `bug.md`
  nachweislich nicht. **Dieselbe Fehlerklasse, gegen die das Feature gebaut wurde**, im selben
  Commit neu eingesetzt; sieben gruene Automated-Kriterien haben es nicht gesehen.
  **F2:** `allowed-tools` ohne `Glob`, obwohl Schritt 2 `features/*/{bug,task}.md` scannt →
  `Read, Glob, Edit`; der Rueckweg im Plan nannte den falschen Fehlermodus (`Write`).
  **F4:** `skills/CLAUDE.md:105` + `dtb-plan-review:47` nannten `feature-start` als
  Alternativ-Erzeuger fuer ein FEHLENDES `plan.md` — genau dann verweigert der Skill selbst.
- **Lektion 13 zweimal bewaehrt (am Tag ihrer Erfassung):** der F1-Fix machte Kriterium P1-3
  (`grep WORKFLOW_STATUS` in Schritt 4 = 0) nachtraeglich rot; gefangen, weil die Kriterien
  nach jedem Fix erneut liefen. Nach der Triage alle 11 Phasen-Kriterien erneut gruen.
- **Verlustpruefung (2 Laeufe): 6 Funde, alle abgesetzt** — L13/L14 + #51 (Lauf 1),
  **L15** (Feature, das eine Fehlerklasse abschafft, muss den eigenen neuen Text dagegen
  pruefen), **L16** (Gegenprobe-Pruefragefrage vorher ausformulieren), **#54** (Negativ-Kriterien
  auf Sprach-Muster koennen legitime Inhalte verbieten — Positiv-Gegenprobe vorschlagen).

### Dateien

- `skills/dtb-feature-start/SKILL.md` — Kern-Umbau + 6 Triage-Fixes
- `skills/CLAUDE.md` — Z. 107 (Rolle) + Z. 105 (Redirect, F4)
- `skills/dtb-implement/SKILL.md` — Nachzuegler Z. 228
- `skills/dtb-plan-review/SKILL.md` — Meldeblock Z. 47 (F4)
- `features/feature-start-statusfeld/{discovery,spec,plan,review}.md` — neu, 5/5 mit SHA-Belegen
- `project-rules/lessons.md` — L15, L16 (ungetrackt) · `INBOX.md` — #52, #53, #54 neu, #50 ausgearbeitet

### Kontext

Die Triage-Sicht ueber 22 offene Ideen wurde zum **siebten Mal** von Hand erzeugt (#33 unerledigt).
Aus ihr ging #50 als kritischster Posten hervor: Der Defekt korrumpierte die Konfliktmeldung
selbst — ein ⚠, das bei jedem Start erscheint, entwertet jede echte Warnung. Das Feature loest
das an der Wurzel. Der `impl-review` belegt zugleich, warum er nicht optional ist: kein
mechanisches Kriterium haette F1 gefunden.

### Naechste Schritte

- [ ] **Triage-Fixes committen + pushen + `/dtb:kit-sync sync`** — drei Klasse-A-Dateien
      betroffen, installierte Kopien laufen noch mit der ungefixten Fassung (**zweiter
      #51-Beleg an einem Tag**)
- [ ] Danach Verhaltenstests wiederholen → Abnahme `feature-start-statusfeld` im naechsten Checkpoint
- [ ] `/dtb:archive` — `no-loss-check` + 11 weitere archivierbare Eintraege
- [ ] 9 Funde aelterer Verlustpruefungen (4 vom Vormittag, 5 vom 2026-08-06) absetzen
- [ ] L11-L16 nach `skills/CLAUDE.md` heben (lessons.md ungetrackt — #34)
- [ ] #54 · #53 · #52 · #51 · #35 · #33 entscheiden · Restabnahmen `feature-fast` + `meeting-agenda`
