# Changelog 2026-08-07

## Session 1 - 11:06

### Implementiert

- **`output-style-gezielt` (#40): von der Idee bis 9/11 in einer Session.** Kette:
  `idea-review` (Dreier-Weiche → Voll-Schiene) → `feature-discover` → `feature-plan` →
  `impl-plan` → `plan-review` → `feature-start` → `implement` (Phasen 1+2 komplett, 3.1/3.2).
- **Zuschnitts-Entscheidungen (idea-review + Discovery):** Nur Ebene (b) — Claudes Fliesstext,
  nicht die Skill-Report-Laengen (25/39 Skills tragen eigene Vorgaben, bleibt eigener Zuschnitt).
  Global statt workflow-gebunden → **Hook-Weg entfiel, #45 ist keine Vorbedingung mehr.**
  Kit-Artefakt statt lokale Config (#34-Muster vermieden); Praezedenz bewusst auf Output Styles
  begrenzt (kein Praejudiz fuer #41/#45). Mechanik gegen die offizielle Doku verifiziert:
  `/output-style` seit v2.1.91 entfernt; `/config` speichert projektlokal → globale Geltung
  braucht `outputStyle` in `~/.claude/settings.json`; `keep-coding-instructions: true` Pflicht;
  wirkt nicht in Subagents.
- **`plan-review` (4 Agenten, Betriebs-Waechter aktiviert): REVISE, 4 WARNs, alle behoben.**
  Kern-Findings: Nachlauf-Kriterien (Commit→Push→Sync erst dann gruen, Lektion-1-Muster),
  Session-Bruch zwischen Aktivierung und Kalibrierung (sonst misst 3.3 nichts), Muster-Beleg als
  echter Wegwerf-Test statt Gedankenexperiment, Rueckweg dokumentieren. Vier Nutzer-Entscheide:
  Klassen-Muster auf `output-styles/dtb-*.md` verengt (deckungsgleich mit Verwaisten-Heuristik),
  Wegwerf-Test real, Sprache Deutsch (endlich bestaetigt), zweite Maschine Check-12-getrieben.
- **Phase 1 (`7172b9c`):** `output-styles/dtb-gezielt.md` — 31 Zeilen, deutsch, Frontmatter
  `name: Gezielt` + `keep-coding-instructions: true`; Leitsatz (worum/Stand/Entscheidung),
  Ausnahme-Klausel (Analyse auf ausdrueckliche Bitte), Vorrang-Regel (Skill-Format schlaegt Stil),
  Substanz-Schutz.
- **Phase 2 (`89ede9c`, `b4205e7`, `17b1319`):** kit-sync um die **erste neue Artefakt-Klasse
  ueberhaupt** erweitert (Tabelle + Verwaisten-Logik auf vier Zielverzeichnisse), Spiegel
  `skills/CLAUDE.md` + Root-`CLAUDE.md` mitgezogen. sync-Lauf: `dtb-gezielt.md` als „neu"
  installiert (sync legte `~/.claude/output-styles/` selbst an), Lock 45 @ 0 Abweichungen.
  **Wegwerf-Test:** `dtb-test.md` committet → als „neu" erkannt OHNE kit-sync-Aenderung
  (Muster belegt) → geloescht → als „verwaist" erkannt → entfernt. Beide Pfade in einem Zyklus.
- **Phase 3 teilweise (3.1/3.2, uncommittet):** `project-health` Check 12 („Style installiert,
  aber nicht aktiviert", inkl. Rueckweg-Doku, beide Meldungs-Richtungen am echten Zustand belegt);
  `outputStyle: "Gezielt"` in `~/.claude/settings.json` gesetzt (Backup:
  `settings.json.bak-output-style`).
- **Bootstrap-Erkenntnis:** Der sync-Lauf lief mit der alten installierten kit-sync-Kopie (drei
  Klassen) — massgeblich war die Klassen-Tabelle des gepushten Stands. Selbst-Update-Fall ad hoc
  entschieden, als Lektion-Kandidat gemeldet (Verlustpruefung, nicht abgesetzt).
- **Verlustpruefung (Schritt 0): 4 Funde, 1 gefiltert** — #41-Nachtrag (TTS via Stop-Hook laengst
  umgesetzt), kit-sync-Bootstrap-Lektion, toter `frameworks/`-Verweis in `skills/CLAUDE.md` Z. 216,
  Wegwerf-Test-Muster als Lektion. Nutzer: weiter ohne Erfassung → als offene Aufgabe vermerkt.

### Dateien

- `output-styles/dtb-gezielt.md` — neu (Kit-Artefakt, Klasse A)
- `skills/dtb-kit-sync/SKILL.md` — Klassen-Zeile + Verwaisten-Abschnitt (4 Verzeichnisse)
- `skills/CLAUDE.md` + `CLAUDE.md` — Klasse-A-Muster-Spiegel
- `skills/dtb-project-health/SKILL.md` — Check 12 (Kopfzeile 11→12, Block, Report-Sektion, consumes)
- `features/output-style-gezielt/{discovery,spec,plan}.md` — neu; plan 9/11 mit SHA-Belegen
- `~/.claude/`: Lock 45 @ `17b1319`, `output-styles/dtb-gezielt.md` installiert, `settings.json`
  mit `outputStyle` (ausserhalb des Repos)

### Kontext

Idee #40 („sparsam") wurde im Interview zu „**gezielt**" praezisiert: Orientierung statt Kuerze —
worum geht es, was ist der Stand, was ist zu entscheiden; Gegner ist Vorwegnahme/Redundanz, nicht
Erklaerung. Der Stil wirkt erst nach Session-Neustart, darum endet die Session planmaessig am
**Session-Bruch vor 3.3**: die Kalibrierung in dieser Session wuerde nichts messen.

ROADMAP-§5-Sync lief zum vierten Mal leer (unausgefuellte Vorlage). Die Resume-Diskrepanz vom
Morgen (Status sagte „Lock 44 @ 926ec71, 3d15d0a nicht verteilt", real war der Lock aktuell) ist
ein weiterer #35-Beleg — dort bereits erfasst.

### Naechste Schritte

- [ ] **Neue Session starten** → `/config` pruefen (loest „Gezielt" auf? Annahme 1) →
      `/dtb:implement output-style-gezielt phase 3` (3.3 Kalibrier-Fall-Set, 5 Faelle inkl.
      Gegenprobe `workflow-next` + Ausnahme-Klausel; Fall-Tabelle in DIESEN Log)
- [ ] 3.4 Nachschaerfen + Abschluss-Verteilung (Commit → Push → Sync, Nachlauf)
- [ ] **4 Funde der heutigen Verlustpruefung absetzen** (#41-Nachtrag · Bootstrap-Lektion ·
      `frameworks/`-Verweis · Wegwerf-Test-Lektion) + die 5 Funde vom 2026-08-06
- [ ] `/dtb:archive` — no-loss-check abgenommen + 8 weitere archivierbare Eintraege
- [ ] Restabnahmen `feature-fast` + `meeting-agenda` · Task `gitattributes-eol` ·
      #35/#50/#33/#49 entscheiden

---

## Session 2 (frische Session, Stil „Gezielt" aktiv)

### Kalibrier-Abnahme output-style-gezielt Schritt 3.3 (Fall-Set 5/5 bestanden)

Vorbedingung erfuellt: Session-Bruch nach 3.2; Annahmen-Probe 1 de facto bestanden — der Stil
war beim Session-Start aktiv, `outputStyle: "Gezielt"` loest gegen das Frontmatter-`name` auf.

| Fall | Prompt | Soll | Ist | Verdikt |
|------|--------|------|-----|---------|
| a — Sachfrage | „Welche Artefakt-Klassen kennt kit-sync und wo landen sie?" | Kurze direkte Antwort, keine Vorwegnahme | Klassen-Tabelle (4x Klasse A) + Seeds, keine ungefragten Zusaetze | bestanden |
| b — Entscheidungsfrage | „Soll ich `backup/2026-08-03-pre-merge` jetzt loeschen?" | Kern/Stand/Entscheidung auf einen Blick | Befund: Branch existiert auf dieser Maschine nicht (nur Maschine 2); 2 Optionen; Nutzer waehlte (1) → Status-Aufgabe umformuliert | bestanden |
| c — Analyse-Bitte | „Analysiere ausfuehrlich Vor-/Nachteile INBOX als Change-Folder" | Ausnahme-Klausel greift: ausfuehrlich | Ausfuehrliche Analyse (4 Pro / 6 Contra / Abwaegung) mit Belegen (#39→#44-Kollision, Monsterzellen #35/#44, Capture-Reibung); Verdikt „flache INBOX behalten" | bestanden |
| d — Gegenprobe Skill | `/dtb:workflow-next` | Report-Format unveraendert (Vorrang-Regel) | Fester Report: 5 nummerierte Eintraege ≤ 15 Zeilen, Konflikt-Zeile — Format ungebrochen | bestanden |
| e — Substanz-Schutz | „Kit-Stand: Artefakte im Lock, Stand, Abweichungen?" | Alle Zahlen/SHAs/Pfade vollstaendig | 45 @ `17b1319`, Quelle GitHub, genau 1 Abweichung (`dtb-project-health` seit Lock geaendert, Hashes `eea9aa6c…`≠`f0742a57…`), Einordnung als 3.4-Nachlauf | bestanden |

Nebenbefunde der Session: `WORKFLOW_STATUS.md`-Aufgabe Sicherungs-Branch auf „Maschine 2"
praezisiert (Branch existiert hier nicht); `grep -P`-Locale-Ausfall erneut aufgetreten
(Lektion 7 angewandt: portable Wiederholung statt Datenbefund).
