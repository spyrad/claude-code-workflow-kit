# 2026-08-06

## Session 1 - 07:47

> Session begann am 2026-08-05 abends mit `/dtb:workflow-resume` und lief ueber Mitternacht;
> der erste Commit (`e04e4a9`) traegt daher noch das Datum des Vortags.

### Implementiert

- **`/dtb:workflow-resume`:** Remote synchron (0/0), aber der Arbeitsbaum war **nicht** sauber —
  der S3-Checkpoint vom 2026-08-05 (Log-Eintrag + Status, 109 Zeilen) lag uncommittet im
  Arbeitsbaum. Der Handoff-Block behauptete „Arbeitsbaum sauber". Kein Feature „In Arbeit"
  (feature-fast 11/11, meeting-agenda 5/5), kein `Abgenommen` gesetzt.

- **Handoff-Abweichung auf Nutzerfrage geprueft — strukturelle Ursache gefunden, vierter
  #35-Beleg mit neuer Fehlerrichtung.** Zeitachse: HEAD `9ac66c5` committet 16:24:48, der
  Session-3-Eintrag traegt „16:31" — der Checkpoint lief **nach** dem Commit und blieb selbst
  uncommittet. Die committete Vorfassung des Handoffs („der Arbeitsbaum ist sauber und mit
  `origin/master` synchron") war um 16:24 **wahr**; der Checkpoint um 16:31 hat sie umformuliert
  und dabei erneut behauptet, waehrend er selbst zwei Dateien schmutzig machte — die Aussage war
  **im Augenblick ihrer Niederschrift falsch**, nicht erst nachtraeglich ueberholt.
  **Ursache:** `workflow-checkpoint` liest `git status --short` in Schritt 1 („Informationen
  sammeln") und schreibt Log + Status in Schritt 3/4. Der Lesezeitpunkt liegt zwangslaeufig vor
  dem eigenen Schreiben — **jeder** Checkpoint hinterlaesst deterministisch einen schmutzigen
  Arbeitsbaum, eine „sauber"-Aussage im Handoff ist immer falsch, ausser es folgt ein Commit.
  Das Skill-Template verlangt die Aussage nicht (es kennt nur `**Naechster Befehl:**` /
  `**Empfehlung:**`) — sie ist Freitext-Zusatz frueherer Sessions.
  **Beide Fehlerrichtungen jetzt belegt:** S4 (2026-08-04) sagte „drei Dateien uncommittet",
  obwohl `71b6404` sie mitgenommen hatte (zu pessimistisch); S3 sagte „sauber", obwohl zwei
  offen waren (zu optimistisch — verschweigt Arbeit, der schlechtere Fall).
  **Praxistest von #35 Option (a):** S3 hatte die Selbstschutz-Klausel bereits von Hand
  eingebaut („ist HEAD beim Resume ein anderer, zuerst gegenpruefen") — sie griff **nicht**,
  weil HEAD identisch war und der Fehler im Arbeitsbaum sass. Eine reine SHA-Bedingung deckt den
  Fall nicht ab; noetig sind zwei Achsen (`HEAD == <sha>` **und** Arbeitsbaum-Zustand).

- **`/dtb:commit-and-push` (2 Laeufe):**
  - `e04e4a9` — den offenen S3-Checkpoint nachgeholt (2 Dateien, +109/-24). Damit wurde der
    Handoff-Satz „Arbeitsbaum sauber" rueckwirkend wahr.
  - `6045f4c` — Discovery + Ideen dieser Session (2 Dateien, +232/-3). Heterogener Diff bewusst
    nicht aufgeteilt (beide Themen aus derselben Session).

- **`/dtb:feature-discover 29` → `features/no-loss-gate/discovery.md` (224 Zeilen).** Alle fuenf
  im INBOX-Eintrag offen gelassenen Punkte sind entschieden:
  | Offener Punkt | Entscheidung |
  |---|---|
  | (1) Erkennungsmechanik | Zweistufig — Heuristik erzeugt Kandidaten, Abgleich filtert Erfasstes weg |
  | (2) Grad der Automatik | Melden mit fertig formuliertem Befehl je Fund; schreibt nichts selbst |
  | (3) Pipeline-Verortung | `next: [dtb:workflow-checkpoint]`; die drei Routing-Kanten bewusst NICHT |
  | (4) read-only vs. schreibend | Read-only, `produces: []` |
  | (5) Rechner-Divergenz | **Ausgeschlossen** — gehoert zu #34 bzw. `workflow-resume` |
  Zwei Festlegungen, die im Eintrag noch nicht standen: **empfehlend statt blockierend** (ein
  hartes Gate ist ohnehin nicht durchsetzbar, weil `workflow-checkpoint`
  `disable-model-invocation: true` traegt) und **kein `disable-model-invocation`** fuer den
  Detektor selbst — belegte Kit-Konvention: von 38 Skills tragen genau 5 keine Sperre
  (`backlog-status`, `meeting-agenda`, `session-summary`, `workflow-next`, `workflow-status`),
  exakt die read-only Skills. Das loest nebenbei das Ausloeser-Problem teilweise: ein Skill, den
  nur der Mensch starten kann, verlangt genau das Erinnern, das er verhindern soll.

- **`/dtb:idea-review` zweimal angesetzt, beide Male ohne Einzelentscheidung beendet.** Der
  Review wurde auf Nutzerwunsch zu #29 (erster Anlauf) und zu #33/#45/#49 (zweiter Anlauf)
  umgelenkt. **#35 bleibt unentschieden** — vierter und fuenfter Fall des Musters, das #33
  begruendet (dort drei Faelle vom 2026-07-30 dokumentiert).

- **Fuenf INBOX-Schreibvorgaenge:** #35 um den Praxistest-Nachtrag erweitert · **#46** halbseitige
  Pipeline-Kanten · **#47** Reihenfolge-Verdrehung in `feature-discover` · **#48** fehlender
  Duplikat-Schutz · **#49** Worker-Tauglichkeitspruefung · Gegenverweis in #43 auf #49 · #29 auf
  `discovery.md` verlinkt.

- **Vollscan Pipeline-Kanten (Grundlage fuer #46):** Das Kit notiert Kanten reziprok, aber
  **sechs** sind einseitig — `feature-fast→plan-review`, `greenfield-roadmap→feature-discover`,
  `meeting-dump→workflow-next`, `open-question→workflow-next`, `open-question→meeting-agenda`,
  `meeting-agenda←meeting-dump`. Vier betreffen read-only Sichten (moeglicherweise Absicht),
  zwei sind echte Nachzieh-Faelle.

- **Vollscan Duplikat-Schutz (Grundlage fuer #48):** `lesson`, `open-question` und `meeting-dump`
  pruefen vor dem Schreiben; `idea`, `task` und `bug-report` nicht. Die drei mit Schutz stammen
  aus den Features `lesson-loop`/`fachfragen-erfassung`/`meeting-dump`, die drei ohne aus dem
  Grundbestand. **Beinahe-Fall belegt:** derselbe #35-Befund wurde als Nachtrag geschrieben und
  unmittelbar danach per `/dtb:idea` erneut abgesetzt — verhindert hat die Dublette allein der
  Gespraechskontext.

### Dateien

- `dtb-project/project-workflows/features/no-loss-gate/discovery.md` — **neu** (224 Zeilen,
  `<!-- resume: done -->`, Status Abgeschlossen)
- `dtb-project/project-workflows/INBOX.md` — #35-Nachtrag, #43-Gegenverweis, #29-Link,
  vier neue Eintraege #46-#49 (jetzt 21 offen, #29 In Arbeit, #28 Ausgearbeitet)
- `dtb-project/project-changelog/2026-08/2026-08-05.md` + `WORKFLOW_STATUS.md` — der offene
  S3-Checkpoint, mit `e04e4a9` nachgeholt
- Kein Skill/Seed geaendert — kit-sync nicht noetig

### Kontext

Die Session hat kein Feature gebaut, sondern **eine Discovery abgeschlossen und den Bestand
geschaerft**. Der inhaltliche Ertrag steht in `no-loss-gate/discovery.md`; der methodische liegt
in zwei Vollscans, die je einen Systemfehler von der Anekdote zum belegten Muster gehoben haben
(#46: sechs Faelle statt einem; #48: drei von sechs Capture-Skills ungeschuetzt).

**Der #35-Befund ist der wichtigste Einzelertrag:** Die Ursache ist nicht Disziplin, sondern die
Schrittfolge von `workflow-checkpoint` selbst — und der von #35 vorgeschlagene Fix (a) wurde
durch S3 unfreiwillig getestet und als unzureichend erwiesen. Damit hat #35 zum ersten Mal einen
Befund, der die Loesung praezisiert statt nur das Problem zu wiederholen.

**Nebenbefund zu #42:** Waehrend dieser Session erschien **#45** (Claude-Code-Hooks) in der INBOX,
erfasst von einer anderen Session ohne Kenntnis dieser. Der Edit-Konflikt wurde vom Werkzeug
gemeldet und aufgeloest, ohne Schaden — aber es ist der zweite dokumentierte Fall paralleler
INBOX-Schreibzugriffe nach der Nummernkollision vom 2026-08-05.

**Was NICHT passiert ist:** `/dtb:idea-review` hat zum vierten und fuenften Mal keine
Einzelentscheidung produziert. Der Bestand wuchs in dieser Session von 16 auf 21 offene Ideen.
Das ist kein Disziplinproblem — bei dieser Groesse ist die Priorisierungs-Sicht (#33) das
fehlende Werkzeug, nicht der sequenzielle Durchgang.

Keine `## Progress`-Checkbox wurde bewegt — es wurde kein Feature implementiert.

### Naechste Schritte

- [ ] `/dtb:feature-plan no-loss-gate` — Discovery vollstaendig, Spec fehlt; `plan-review` ist
      bei diesem Feature nicht optional (Spiegel-Kopplung ueber 6 Dateien)
- [ ] **#33 entscheiden** — fuenfter Beleg heute; Triage-Sicht bauen ODER einmal ad hoc
      durchfuehren und danach mit klarer Reihenfolge in den Review
- [ ] **#35 entscheiden** — steht seit 2026-08-05 S2 aus; der heutige Nachtrag verschaerft den
      Zuschnitt (Fix (a) reicht nicht), Empfehlung weiterhin Voll-Schiene
- [ ] **#49 gegen #33 abgrenzen** — beide scannen dieselbe Liste, nur mit anderem Raster;
      Zusammenlegung ist offener Punkt (1) in #49
- [ ] `/dtb:kit-sync check` **auf dieser Maschine** — seit der Parallelarbeit unklar
      (S4 meldete 43/43 @ `71b6404`, die andere Maschine notierte `18a92da`)
- [ ] Task `gitattributes-eol` starten — heute **drei** weitere LF→CRLF-Warnungen bei
      `git add`/`git diff`, der Beleg waechst mit jeder Session
- [ ] Restabnahme `meeting-agenda` (dreigeteilt) · Restabnahme `feature-fast` (2 UX-Urteile
      + Lauf mit gefixter Fassung `1eec2ea`)
- [ ] Platzhalter entscheiden: `workflow.config.yaml` (`project_name`, `repos[].name` noch
      `[Projektname]`/`[Repo-Name]`) und `ROADMAP.md` (unausgefuellte Vorlage — der §5-Sync
      dieses Checkpoints lief erneut ins Leere)
- [ ] Sicherungs-Branch `backup/2026-08-03-pre-merge` loeschen, wenn der Merge sich bewaehrt hat
- [ ] pkp intern committen: `UI.md` + `project-design/`-Tokens (separates Repo)

---

## Session 2 - 21:40

### Implementiert

- **Feature `no-loss-check` (#29) vollstaendig umgesetzt** — Spec, Plan, zwei Reviews und alle 11
  Umsetzungsschritte in einer Sitzung. Vier Phasen-Commits: `110c208` (Phase 1), `471a0e7`
  (Phase 2), `1110b09` (3.1/3.2), `926ec71` (3.3/3.4), alle nach `origin/master` gepusht. Jede
  `## Progress`-Zeile traegt ihren SHA-Beleg.

- **Umbenannt `no-loss-gate` → `no-loss-check`** (`/dtb:feature-plan`, per `git mv`): „Gate"
  versprach eine Blockade, die der Skill laut Discovery bewusst nicht leistet. INBOX #29 und
  BACKLOG nachgezogen; der Idee-Titel bleibt historisch.

- **`/dtb:plan-review`: REVISE** — 7 WARNs, alle eingearbeitet, Plan danach `Reviewed`.
  Wesentlich: Schritt 1.0 (Baseline + Ausloeser-Vorabprobe) als Abbruchpunkt eingezogen,
  Kalibrier-Menge mit 5 Faellen im Plan festgeschrieben statt erst in 3.4, der echte Lauf vom
  Plan-Schritt zum Abnahme-Kriterium umgewidmet, hartes Gate fuer 3.3 (Lektion 7).

- **Schritt 1.0 — Ausloeser-Annahme praktisch belegt:** `dtb:implement` (gesperrt) stiess
  `dtb:workflow-next` (ungesperrt) an, ohne ein Skill-Tool in `allowed-tools` zu fuehren.
  Zweiter Beleg nach der Installation: `dtb-no-loss-check` erschien sofort in der Aufrufliste.

- **`/dtb:project-health` Baseline (Schritt 1.0):** 8 bestanden, 4 Warnungen, 0 Fehler. Die vier:
  8 archivierbare Eintraege · Config-Platzhalter · uncommittete Aenderungen ·
  Zirkel-/`consumes`-Check uebersprungen. **Vergleichslauf nach Phase 3:** 7 / 5 / 0 — genau ein
  neuer Befund (Anzeige-Drift `no-loss-check`), bauartbedingt und von diesem Checkpoint
  aufgeloest. Skills 38 → 39, Pipeline-Referenzen 22 → 23, Lock 43 → 44.

- **`/dtb:kit-sync sync` zweimal.** Erster Lauf **abgebrochen**: `origin/master` kannte den neuen
  Skill nicht (0 Treffer), 2 Commits ungepusht — ein Sync haette „nichts abzugleichen" gemeldet
  und damit gegen die falsche Seite verglichen. Nach Commit + Push: 4 Artefakte uebernommen
  (1 neu, 3 Updates), spaeter 1 weiteres nach der Kalibrier-Nachschaerfung. Lock: 44 Artefakte
  @ `926ec71`, Verifikation 0 Abweichungen. **Nebenbefund:** Der widerspruechliche kit-sync-Stand
  dieser Maschine war keiner — der Lock stand auf `71b6404` mit 43/43 synchron, `18a92da`
  gehoerte zur anderen Maschine. Offene Aufgabe damit erledigt.

- **Kalibrierung (Schritt 3.4): 5 von 5 Faellen getroffen.** Faelle 1-4 gegen den Bestand OHNE den
  jeweiligen Eintrag geprueft (Zustand zum Zeitpunkt des Auftauchens), Fall 5
  (Unterdrueckungs-Test) mit. **Befund:** Stufe 2 las nur die §6-Kanonform und haette jede als
  untagged Bullet festgehaltene Frage bei jedem Lauf erneut gemeldet — in diesem Projekt sofort
  5 Falsch-Positive. Nachgeschaerft auf alle Inhaltszeilen in `## Offene Punkte`.

- **`/dtb:impl-review` zweimal, beide REJECTED, beide vollstaendig triagiert (17 Fixed, 0 Skipped).**
  Lauf 1 (zwei Sub-Agents, Drift + Craft): 10 Findings, 2 blocking. Schwerwiegendster: die
  Versioniert-Kennzeichnung per `.gitignore`-Pfad-Lookup — die Datei traegt Glob-Muster mit
  Last-Match-wins; im Kit wird `lessons.md` ueber `dtb-project/project-rules/*` ignoriert, der
  Klartext steht dort nur in einem Kommentar. Lauf 2 (fokussierte Verifikation): 8/10 sauber,
  **6 neue Probleme durch die Fixes** — darunter G1, wo zwei eigene Fixes sich gegenseitig die
  Grundlage entzogen (F8 schob Rechenschaft an eine Kopfzeile ab, die F9 kurz darauf inhaltsfrei
  machte).

- **Erster produktiver Lauf des Skills** als Schritt 0 dieses Checkpoints: 4 Funde (2 Lektionen,
  2 Ideen), 5 korrekt unterdrueckt, keine Falsch-Positiven. Damit sind die drei offenen
  Manual-Kriterien der Phase 3 faktisch belegt — der Status bleibt auf Nutzerwunsch dennoch
  „Fertig zum Testen", weil der dritte Verifikations-Lauf gegen die gefixte Fassung aussteht.

- **4 Lektionen erfasst (L9-L12):** Sperre wirkt nur auf die Modell-Initiative · Ersetzungsprobe
  statt Prozent-Quote bei unscharfen Schwellen · `.gitignore` nie per Pfad-Grep auswerten ·
  Nachbarschafts-Regel gilt auch fuer Review-Fixes.

- **Idee #50 erfasst:** `dtb:feature-start` weist an, beim Start „In Arbeit" zu setzen —
  `DERIVED_STATE_RULES.md` §1.1 leitet bei 0 abgehakten Checkboxen aber „Geplant" ab. Jeder
  Feature-Start erzeugt so einen Feld-Konflikt. Beim Start von `no-loss-check` bewusst dem Kanon
  gefolgt statt der Skill-Anweisung.

### Dateien

- `skills/dtb-no-loss-check/SKILL.md` — **neu**, 362 Zeilen (Contract, Stufe 1/2, Report,
  Leer-Fall, drei Randfaelle)
- `skills/dtb-workflow-checkpoint/SKILL.md` — „Schritt 0: Verlustpruefung" vor dem
  Informationen-Sammeln; Gegenkante `after: [dtb:impl-review, dtb:no-loss-check]`. Bestehende
  Schritt-Nummern bewusst NICHT umnummeriert (`DERIVED_STATE_RULES.md` §1.2 und andere Skills
  verweisen auf „Schritt 2/2.3/4")
- `skills/dtb-session-summary/SKILL.md` — Abgrenzungs-Hinweis, Frontmatter-Kanten bewusst `null`
- `skills/dtb-workflow-status/SKILL.md` — Zeile in der handgepflegten Tabelle „Beteiligte Skills
  & Agents"; vom repo-weiten Namens-Grep gefunden (Lektion 3)
- `CLAUDE.md` — Session-lifecycle-Zeile
- `features/no-loss-check/` — `spec.md` (188 Z.), `plan.md` (11 Schritte, alle mit SHA),
  `review.md` (beide Review-Laeufe mit Entscheidungs-Historie), `discovery.md` umbenannt
- `dtb-project/project-rules/lessons.md` — +4 (L9-L12); **ungetrackt**, reist nicht mit
- `INBOX.md` — #29 → Ausgearbeitet, #50 neu

### Kontext

Die Session hat ein Feature von der Discovery bis „Fertig zum Testen" durchgezogen — der erste
vollstaendige Durchlauf der Voll-Schiene in einer Sitzung. Der methodische Ertrag liegt aber in
den beiden Review-Laeufen: **Der wertvollste Befund war jeweils einer, den der Autor selbst nicht
gesehen haette.** F1 war eine sachlich falsche Regel, die ich geschrieben und fuer plausibel
gehalten hatte; G1 waren zwei eigene Fixes, die sich gegenseitig aufhoben. Ohne unabhaengige
Sub-Agents waeren beide durchgerutscht.

Zweiter Ertrag: **Zwei Skills liessen sich nicht ausfuehren, weil sie gesperrt sind**
(`project-health`, `kit-sync`) — die Schritte 1.0 und 3.3 mussten vom Menschen gestartet werden.
Zugleich wurde belegt, dass ein gesperrter Skill sehr wohl einen ungesperrten anstossen kann: die
Sperre trifft die Initiative, nicht die Anweisungskette.

### Naechste Schritte

- [ ] **Triage-Fixes verteilen:** `/dtb:kit-sync sync` — die installierte Kopie traegt noch den
      Stand `926ec71` ohne die 17 Review-Fixes
- [ ] **Dritter `/dtb:impl-review no-loss-check`** gegen die gefixte Fassung — noetig fuer
      APPROVED (aktuell NEEDS ATTENTION, dokumentiert in `review.md`)
- [ ] **Vier Funde der Verlustpruefung absetzen** — 2 Lektionen (Nachlauf-Kriterien koennen erst
      nach dem Commit gruen werden; kit-sync gegen ungepushten Stand), 2 Ideen (README „Skills
      Overview" veraltet; `project-health` Check 8 faktisch nie ausgefuehrt)
- [ ] **L11/L12 nach `skills/CLAUDE.md` heben** — `lessons.md` ist ungetrackt, die Regeln reisen
      sonst nicht mit (Hebe-Hinweis aus `dtb:lesson`)
- [ ] **#50 entscheiden** — `feature-start` gegen den Status-Kanon
- [ ] **#35 entscheiden** — seit 2026-08-05 offen, Empfehlung weiterhin Voll-Schiene
- [ ] **#33 entscheiden** — `/dtb:idea-review` hat zum fuenften Mal keine Einzelentscheidung
      produziert; Bestand jetzt 23 offene Ideen
- [ ] Task `gitattributes-eol` starten — heute weitere LF→CRLF-Warnungen bei jedem `git add`
- [ ] Restabnahme `meeting-agenda` (dreigeteilt) · Restabnahme `feature-fast` (2 UX-Urteile)
- [ ] Config-Platzhalter fuellen · `config.paths.rules` ergaenzen, damit der Fallback nicht
      stillschweigend traegt
