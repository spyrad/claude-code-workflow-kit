# Ideen-Inbox

> Schnell erfasste Ideen und Gedanken. Ausarbeitung mit `/dtb:feature-discover`.

---

| #   | Datum      | Idee | Status |
| --- | ---------- | ---- | ------ |
| 11  | 2026-07-09 | Greenfield-Strang (`greenfield-prd` + `greenfield-roadmap`) nach 10x-3.0 neu konzipieren: aktuell reine Lese-/Übersichts-Werkzeuge (read-only, `produces: []`) — Vorbild 10x-prd/10x-roadmap sind echte Autoren-Skills, die PRD/Roadmap generieren. Umfasst ggf. Write-Fähigkeit, Technical-Leak-Lint + „nie erfinden → Open Questions" (aus Idee #5, dort bewusst ausgeklammert). Erst nach Sichtung 10x-3.0-Kursstand angehen; **Funde SKILL_10X-Abnahme:** verschachtelte Code-Fences im Ausgabe-Template (Template-Ende mehrdeutig, Rendering-Risiko), kein Fallback bei fehlender ROADMAP.md, `disable-model-invocation: false` (einziger auto-invoke-Peer) → features/greenfield-autoren-skills/discovery.md → features/greenfield-autoren-skills/spec.md | Ausgearbeitet |
| 10  | 2026-07-08 | kit-sync „pinned/hold"-Zustand: Artefakte, die bewusst von der Repo-Version abweichen sollen, im Lock als `mode: pinned` (statt `synced`) markieren — kit-sync meldet sie als eigenen Zustand „gepinnt (bewusst abweichend)" und haelt sie aus dem „Update verfuegbar"-Sammel-Ja (Buendel 1) heraus, statt sie wie ein Routine-Update zum Ueberschreiben vorzuschlagen. Motiviert durch die pitch-coach-Divergenz (oma-personas-Kopie) bei der KIT_SYNC-Praxiserprobung 2026-07-08: mechanisch korrekt als „Update verfuegbar" klassifiziert, obwohl bewusst gehalten → musste manuell uebersprungen werden und taucht bei jedem check erneut auf. Erweiterung zu Idee #4 (KIT_SYNC); haengt inhaltlich an Klaerung #8 (Persona-Quellen), ueberlebt diese aber (bewusste Abweichungen bleiben generisch moeglich) | Offen |
| 14  | 2026-07-11 | Codebase-Research-Step in `impl-plan`/`feature-plan` verankern (Vorbild 10x, wo die Codebase-Recherche direkt VOR dem impl-plan sass): Ein expliziter Ist-Analyse-Schritt (Glob/Grep der betroffenen Module) existiert bei uns nur in `feature-discover` Schritt 2 („Codebase-Scan" → `## Betroffene Module`); `plan-review` Schritt 2d („Grounding") verifiziert nur nachgelagert einen fertigen Plan. Wer `feature-discover` ueberspringt und direkt zu `feature-plan`/`impl-plan` geht, hat KEINEN erzwungenen Codebase-Scan mehr — beide haben zwar `Glob, Grep` in `allowed-tools`, nutzen sie aber nicht angeleitet fuer eine Ist-Analyse. Idee: angeleiteten Codebase-Research-Schritt in `impl-plan` (Spec→Ist-Code-Abgleich vor Schrittplanung) ergaenzen. Offen: Redundanz zu feature-discover vermeiden (nur wenn discovery.md fehlt?), Scope → `features/codebase-research-step/discovery.md`, `features/codebase-research-step/spec.md` | Ausgearbeitet |
| 18  | 2026-07-14 | `feature-discover` optionalen ROADMAP-Slice-Lookup geben (Roadmap→Discovery-Brücke schließen): Aktuell ist der Hand-off `greenfield-roadmap` → `/dtb:feature-discover <change-id>` nur konventionsbasiert — `feature-discover` liest die `ROADMAP.md` NICHT (`consumes: [INBOX.md, workflow.config.yaml]`), sein Schritt 1 sucht das Argument in der INBOX; ein Roadmap-Slice steht dort nicht → String dient nur als Slug-Seed, Slice-Kontext (Outcome + PRD-Bezug) muss manuell mitgebracht werden. Idee: Wenn das Argument einer Change-ID in `project-strategy/ROADMAP.md` entspricht, den Slice-Block (Outcome, Prerequisites, PRD-Bezug) als Discovery-Startkontext einlesen; `consumes` um ROADMAP.md erweitern. Offen: (1) Reihenfolge INBOX- vs. ROADMAP-Lookup (Slug direkt vs. Nummer/Stichwort); (2) Drift-Vermeidung — nur lesen, kein zweiter Speicherort; (3) Verhalten wenn Slug in beiden (INBOX + ROADMAP) vorkommt. Anschluss an #11 (Greenfield-Autoren-Skills), schließt die dort bewusst offen gelassene Rough Edge | Offen |
| 17  | 2026-07-14 | `10x-opportunity-map`-Pendant bauen: Skill, der wiederkehrende Reibung/unerfüllten Bedarf in ein Entscheidungs-Artefakt überführt — **build vs. buy vs. complement vs. wait** (Vorbild `10x-devs-3.0/.claude/skills/10x-opportunity-map`, schreibt `context/team/opportunity-map.md`). Kein Implementierungsplan, sondern Klassifikation VOR dem Bau: Signale normalisieren (schwache „bau ein Dashboard"-Ideen vom echten Schmerz trennen), je Signal existierende Antwort/dünnes Komplement/erste nützliche Version/Datenrisiko/Richtung, genau 1 empfohlener Kandidat, dünne read-only Erstversion, Data-Risk-Guardrail früh. Sitzt im Vorfeld der Greenfield-Kette (vor shape: ob/wie überhaupt bauen). Kit hat KEIN Pendant; verwandt mit #16 (mom-test) und #11 (Greenfield-Strang). Offen: eigener Skill vs. Erweiterung von `feature-discover`; Derived-State-Verortung des Artefakts | Offen |
| 16  | 2026-07-14 | `10x-mom-test`-Pendant bauen: Ideen-Validierungs-Skill nach „The Mom Test" — prüft VOR dem Bau, ob das Problem echt ist, über Fragen zu vergangenem Verhalten statt Meinungen zur Lösung (Vorbild `10x-devs-3.0/.claude/skills/10x-mom-test`, schreibt `context/team/mom-test-validation.md`). Liefert: Hypothesen-Extraktion (Fakt vs. Wunschdenken), nicht-leitende Kritik, Interview-Leitfaden (8-12 neutrale Fragen), Umfrage (6-10 Fragen + Screener), Go/No-Go-Kriterien mit konkreten Schwellen. Ergänzt unsere `feature-discover` (klärt Anforderungen, prüft aber nicht die Problem-Validität). Kit hat KEIN Pendant; verwandt mit #17 (opportunity-map) und #11 (Greenfield-Strang). Offen: eigener Skill vs. Discovery-Vorstufe; Trigger/Abgrenzung zu feature-discover | Offen |
| 15  | 2026-07-14 | `10x-health-check`-Pendant bauen: Audit eines bestehenden (Brownfield-)Projekts — Dependency-Audit, Security-Scan, Test-Runner-/CI-/Missing-Config-Analyse, priorisierte Fixes + Agent-Readiness-Verdikt (Vorbild `10x-devs-3.0/.claude/skills/10x-health-check`, schreibt `context/foundation/health-check.md`). VOR dem Bau: gegen den bestehenden `dtb:project-health` prüfen und die Unterschiede herausarbeiten (project-health = Kit-/Workflow-Lint: Frontmatter, Derived-State, Drift, UPPER_SNAKE_CASE etc.; 10x-health-check = Projekt-Substanz: Abhängigkeiten/Security/CI). Offen: eigener Skill vs. project-health erweitern; Überschneidung/Abgrenzung; Brownfield-Ast war bei #11 bewusst ausgeklammert — hier ggf. Wiedereinstieg | Offen |
| 13  | 2026-07-10 | Fachfragen-/Stakeholder-Fragen-Agenda: Waehrend discovery (und spaeter spec/plan) tauchen Fragen auf, die nicht sofort/allein beantwortbar sind, sondern ins woechentliche Fach-Meeting gehoeren. Derived-State-konform umsetzen: Quelle bleibt die Frage im `## Offene Punkte` des Features, markiert als Fachfrage (Tag `[Fach]` + Checkbox = Status offen/beantwortet, Antwort im selben Kontext nachgetragen). Neue rein-lesende Ansicht (z.B. `/dtb:offene-fragen` bzw. `/dtb:fach-agenda`) scannt `features/*/{discovery,spec,plan}.md`, sammelt offene `[Fach]`-Fragen, gruppiert nach Feature → fertige Meeting-Agenda (abgeleitet, kein zweiter Speicherort — NICHT ins BACKLOG duplizieren = Drift). Offene Design-Punkte: (1) Antwort-Rueckfluss manuell vs. Schreib-Modus der Agenda; (2) Reichweite nur discovery.md oder auch spec/plan; (3) Tag-Syntax `[Fach]`/`[Stakeholder]` vs. eigene Sektion `## Fachfragen` | Offen |
