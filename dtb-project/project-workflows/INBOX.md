# Ideen-Inbox

> Schnell erfasste Ideen und Gedanken. Ausarbeitung mit `/dtb:feature-discover`.

---

| #   | Datum      | Idee | Status |
| --- | ---------- | ---- | ------ |
| 25  | 2026-07-17 | Rein-lesender Fach-Agenda-Skill (z.B. `/dtb:fach-agenda`): sammelt offene `[Fach]`-Fragen aus `features/*/{discovery,spec,plan}.md`, gruppiert nach Feature → fertige Meeting-Agenda (abgeleitet, `produces: []`, kein zweiter Speicherort). Konsumiert die Fach-Frage-Konvention aus `DERIVED_STATE_RULES.md` §6 (Feature fachfragen-erfassung, 2026-07-17). Muster: bestehende read-only Skills (`workflow-next`/`backlog-status`). Abzugrenzen von #24 (dort der Antwort-Rueckfluss aus dem Meeting, hier die Lese-Ansicht der offenen Fragen). Offen: Slug-Name (`fach-agenda` vs. `offene-fragen`); ob `[x]`-beantwortete Fragen ausgeblendet oder als „zuletzt geklaert" mitgezeigt werden. | Offen |
| 24  | 2026-07-17 | Skill fuer Besprechungs-Nachbereitung: Punkte/Antworten/Input aus der letzten Projekt-Besprechung eingeben und gegen die offenen Fragen der Features pruefen. Abgleich Meeting-Ergebnisse ↔ offene Punkte in `features/*/{discovery,spec,plan}.md`; beantwortete Fragen zuordnen/nachtragen, unbeantwortete/neue Punkte sichtbar machen. Verwandt mit #13 (Fachfragen-/Stakeholder-Agenda — dort die Lese-Ansicht, hier der Rueckfluss der Antworten). Offen: Traeger (eigener Skill vs. Erweiterung von #13), Derived-State-konformer Rueckschreib-Weg (Antwort im selben `## Offene Punkte`-Kontext, kein zweiter Speicherort), Input-Form (Freitext-Dump vs. strukturiert). | Offen |
| 23  | 2026-07-16 | Multi-Repo-Begriff kitweit auf Git-toplevel vereinheitlichen: `dtb:implement` + `DERIVED_STATE_RULES.md` §2 Regel 5 definieren „Multi-Repo" ueber die **Anzahl `config.repos`-Eintraege** (löst `repo-name@SHA`-Praefix in `## Progress` aus). `dtb:commit-and-push` (Feature 2026-07-16) definiert Repo-Identitaet dagegen ueber `git rev-parse --show-toplevel` — robuster, weil config-unabhaengig. Divergenz sichtbar bei pkp (Monorepo): `config.repos` hat 2 Eintraege (`pkp-frontend`/`pkp-backend`) → implement schreibt `pkp-frontend@sha`-Praefixe, obwohl es faktisch **ein** Repo (ein `.git`) ist → die SHA-Belege suggerieren zwei Repos. Idee: §2 Regel 5 + implement auf toplevel-Gruppierung umstellen (Multi-Repo = mehr als ein eindeutiger toplevel, nicht mehr als ein config.repos-Eintrag). Offen: Rueckwaertskompatibilitaet bestehender `## Progress`-SHAs mit Praefix; Auswirkung auf `repo-sync`/`build-check`. Kontext: plan-review commit-and-push 2026-07-16 (Architekt-Befund Multi-Repo-Divergenz) | Offen |
| 22  | 2026-07-15 | Seed-Skew aktiv melden: Seeds (`DERIVED_STATE_RULES.md`, `settings.json`) sind bewusst „copied once, never drift-checked" (Klasse B) — mit der §2-Härtung (Feature verifikations-gate) entsteht aber realer Skew: global verteilte Klasse-A-Skills (z.B. `dtb:implement`) setzen das gehärtete §2 voraus, Bestandsprojekte tragen den alten Seed. Idee: `kit-sync check` oder `project-health` meldet veraltete Seeds als eigenen Hinweis-Zustand (informativ, KEIN Auto-Update — Seeds bleiben projektgehörig, Design-Entscheidung Klasse B bleibt unangetastet). Offen: Träger (kit-sync vs. project-health), Erkennungsmechanik (Seed-Hash im Lock vs. Versions-Marker in der Datei), Abgrenzung zur bewussten Projekt-Anpassung des Seeds. Kontext: plan-review verifikations-gate 2026-07-15 (Architekt-Frage Version-Skew) | Offen |
| 21  | 2026-07-15 | `dtb:commit-and-push` bauen (Vorbild `10x-commit-and-push` — dort bewusst ein Command, kein Skill): Routine-Commit+Push-Schritt, den das Kit nicht hat — `repo-sync` ist read-only Monitoring, kein Skill autorisiert Commits, obwohl das Progress-Modell Commit-SHAs als Beleg verlangt (der Commit ist heute der unbegleitetste Moment im Workflow). Umfang nach 10x-Vorbild: parallele Status-Checks, Red-Flag-Scan VOR `git add` (Secrets/`.env`/Keys, Dateien >1 MB, `settings.local.json` tracked), Message-Generierung im Repo-Stil (imperativer Titel, heredoc, Co-Authored-By), Sicherheitsregeln hart (nie `--force`/`--no-verify`/`--amend`; non-fast-forward → `pull --rebase` vorschlagen; fehlender Tracking-Branch → fragen), kompaktes Summary; explizit Routine-Werkzeug (heterogener Diff → manuell aufteilen). DTB-Besonderheiten: Multi-Repo-Fähigkeit (`config.repos` — Repo wählen oder alle) und optionale SHA-Rückschreibung in `plan.md ## Progress` (Brücke zu #19: kommt dort Variante a, wohnt das Phase-Ende-Ritual im implement-Skill und commit-and-push bleibt der Routine-Pfad daneben; kommt nur Variante b, bringt commit-and-push die SHA-Rückschreibung mit). Form klären: Skill vs. Command (beide Klasse A in kit-sync). Kontext: Session 2026-07-15 → features/commit-and-push/discovery.md → features/commit-and-push/spec.md | Ausgearbeitet |
| 20  | 2026-07-15 | `dtb:impl-review` nach 10x-Vorbild (`10x-impl-review`) statt `dtb:code-review`: EIN Review-Skill mit drei Dimensionen — (1) Plan-Drift-Check mechanisch (Plan↔Code: MATCH/DRIFT/MISSING/EXTRA, kein Persona-Job), (2) Craft-Urteil durch geladene Reviewer-Personas `persona-principled-reviewer` (Konsistenz/Patterns/Prinzipien) + `persona-torvalds-reviewer` (Abstraktion/Performance/Geschmack) — `kent-beck` bewusst draußen (Dialog-Persona, läuft im automatisierten Artefakt-Review ins Leere), (3) Rules-Konformität faltet heutigen code-review ein (gegen `project-rules/*.md`; fehlen Rules → still überspringen statt Abbruch — 10x kennt kein Rules-Doc-Konzept, wir behalten es als Dimension). Danach `dtb:code-review` aus dem Workflow entfernen. Vorbedingungen: Reviewer-Personas ins Kit vendoren + als kit-sync-Klasse verteilen (Kit-Kopie = Quelle der Wahrheit, kein Sync zu oma-personas; löst zugleich #10-Auslöser pitch-coach auf); `plan-review` + dessen 4 Agenten bleiben unberührt (anderes Objekt: Plan, nicht Code). Kontext: Session 2026-07-15 (10x-Vergleich, Persona-Faden) → features/impl-review/discovery.md → features/impl-review/spec.md | Ausgearbeitet |
| 19  | 2026-07-15 | Verifikations-Gate in den Umsetzungs-Loop einbetten (Vorbild `10x-implement` „Verification Approach"): Progress-Checkbox darf erst flippen, wenn die Checkpoint-Kriterien der Phase grün sind — SHA wird Verifikations-Beleg statt nur Commit-Beleg. Heutige Lücke: kein Implementierungs-Skill nach `feature-start`, Abhaken ist Ehrensystem („nach jedem umgesetzten Schritt"), „Fertig zum Testen"→„Abgenommen" verlangt keinerlei Test-Nachweis; build-check/code-review separat+überspringbar. Träger klären: (a) neuer `dtb:implement`-artiger Skill, der den Plan phasenweise treibt (10x-Vorbild, inkl. Phase-Ende-Ritual), vs. (b) leichter: Regel-Härtung in DERIVED_STATE_RULES §2 (Konvention statt Skill). Folge: `dtb:build-check` entkernen — Restfunktionen bewusst verorten: Multi-Repo-„deploy-ready"-Sammel-Check (bei pkp real nützlich) behalten/umbenennen oder streichen; Event-Konsistenz-Check (FastWS-spezifisch) → Projekt-Rule statt Kit-Skill. Kontext: Session 2026-07-15 (Gate-Lücke-Analyse, 10x-Vergleich) → features/verifikations-gate/discovery.md → features/verifikations-gate/spec.md | Ausgearbeitet |
| 11  | 2026-07-09 | Greenfield-Strang (`greenfield-prd` + `greenfield-roadmap`) nach 10x-3.0 neu konzipieren: aktuell reine Lese-/Übersichts-Werkzeuge (read-only, `produces: []`) — Vorbild 10x-prd/10x-roadmap sind echte Autoren-Skills, die PRD/Roadmap generieren. Umfasst ggf. Write-Fähigkeit, Technical-Leak-Lint + „nie erfinden → Open Questions" (aus Idee #5, dort bewusst ausgeklammert). Erst nach Sichtung 10x-3.0-Kursstand angehen; **Funde SKILL_10X-Abnahme:** verschachtelte Code-Fences im Ausgabe-Template (Template-Ende mehrdeutig, Rendering-Risiko), kein Fallback bei fehlender ROADMAP.md, `disable-model-invocation: false` (einziger auto-invoke-Peer) → features/greenfield-autoren-skills/discovery.md → features/greenfield-autoren-skills/spec.md | Ausgearbeitet |
| 10  | 2026-07-08 | kit-sync „pinned/hold"-Zustand: Artefakte, die bewusst von der Repo-Version abweichen sollen, im Lock als `mode: pinned` (statt `synced`) markieren — kit-sync meldet sie als eigenen Zustand „gepinnt (bewusst abweichend)" und haelt sie aus dem „Update verfuegbar"-Sammel-Ja (Buendel 1) heraus, statt sie wie ein Routine-Update zum Ueberschreiben vorzuschlagen. Motiviert durch die pitch-coach-Divergenz (oma-personas-Kopie) bei der KIT_SYNC-Praxiserprobung 2026-07-08: mechanisch korrekt als „Update verfuegbar" klassifiziert, obwohl bewusst gehalten → musste manuell uebersprungen werden und taucht bei jedem check erneut auf. Erweiterung zu Idee #4 (KIT_SYNC); haengt inhaltlich an Klaerung #8 (Persona-Quellen), ueberlebt diese aber (bewusste Abweichungen bleiben generisch moeglich) | Offen |
| 18  | 2026-07-14 | `feature-discover` optionalen ROADMAP-Slice-Lookup geben (Roadmap→Discovery-Brücke schließen): Aktuell ist der Hand-off `greenfield-roadmap` → `/dtb:feature-discover <change-id>` nur konventionsbasiert — `feature-discover` liest die `ROADMAP.md` NICHT (`consumes: [INBOX.md, workflow.config.yaml]`), sein Schritt 1 sucht das Argument in der INBOX; ein Roadmap-Slice steht dort nicht → String dient nur als Slug-Seed, Slice-Kontext (Outcome + PRD-Bezug) muss manuell mitgebracht werden. Idee: Wenn das Argument einer Change-ID in `project-strategy/ROADMAP.md` entspricht, den Slice-Block (Outcome, Prerequisites, PRD-Bezug) als Discovery-Startkontext einlesen; `consumes` um ROADMAP.md erweitern. Offen: (1) Reihenfolge INBOX- vs. ROADMAP-Lookup (Slug direkt vs. Nummer/Stichwort); (2) Drift-Vermeidung — nur lesen, kein zweiter Speicherort; (3) Verhalten wenn Slug in beiden (INBOX + ROADMAP) vorkommt. Anschluss an #11 (Greenfield-Autoren-Skills), schließt die dort bewusst offen gelassene Rough Edge | Offen |
| 17  | 2026-07-14 | `10x-opportunity-map`-Pendant bauen: Skill, der wiederkehrende Reibung/unerfüllten Bedarf in ein Entscheidungs-Artefakt überführt — **build vs. buy vs. complement vs. wait** (Vorbild `10x-devs-3.0/.claude/skills/10x-opportunity-map`, schreibt `context/team/opportunity-map.md`). Kein Implementierungsplan, sondern Klassifikation VOR dem Bau: Signale normalisieren (schwache „bau ein Dashboard"-Ideen vom echten Schmerz trennen), je Signal existierende Antwort/dünnes Komplement/erste nützliche Version/Datenrisiko/Richtung, genau 1 empfohlener Kandidat, dünne read-only Erstversion, Data-Risk-Guardrail früh. Sitzt im Vorfeld der Greenfield-Kette (vor shape: ob/wie überhaupt bauen). Kit hat KEIN Pendant; verwandt mit #16 (mom-test) und #11 (Greenfield-Strang). Offen: eigener Skill vs. Erweiterung von `feature-discover`; Derived-State-Verortung des Artefakts → features/opportunity-map/discovery.md → features/opportunity-map/spec.md | Ausgearbeitet |
| 16  | 2026-07-14 | `10x-mom-test`-Pendant bauen: Ideen-Validierungs-Skill nach „The Mom Test" — prüft VOR dem Bau, ob das Problem echt ist, über Fragen zu vergangenem Verhalten statt Meinungen zur Lösung (Vorbild `10x-devs-3.0/.claude/skills/10x-mom-test`, schreibt `context/team/mom-test-validation.md`). Liefert: Hypothesen-Extraktion (Fakt vs. Wunschdenken), nicht-leitende Kritik, Interview-Leitfaden (8-12 neutrale Fragen), Umfrage (6-10 Fragen + Screener), Go/No-Go-Kriterien mit konkreten Schwellen. Ergänzt unsere `feature-discover` (klärt Anforderungen, prüft aber nicht die Problem-Validität). Kit hat KEIN Pendant; verwandt mit #17 (opportunity-map) und #11 (Greenfield-Strang). Offen: eigener Skill vs. Discovery-Vorstufe; Trigger/Abgrenzung zu feature-discover | Offen |
| 15  | 2026-07-14 | `10x-health-check`-Pendant bauen: Audit eines bestehenden (Brownfield-)Projekts — Dependency-Audit, Security-Scan, Test-Runner-/CI-/Missing-Config-Analyse, priorisierte Fixes + Agent-Readiness-Verdikt (Vorbild `10x-devs-3.0/.claude/skills/10x-health-check`, schreibt `context/foundation/health-check.md`). VOR dem Bau: gegen den bestehenden `dtb:project-health` prüfen und die Unterschiede herausarbeiten (project-health = Kit-/Workflow-Lint: Frontmatter, Derived-State, Drift, UPPER_SNAKE_CASE etc.; 10x-health-check = Projekt-Substanz: Abhängigkeiten/Security/CI). Offen: eigener Skill vs. project-health erweitern; Überschneidung/Abgrenzung; Brownfield-Ast war bei #11 bewusst ausgeklammert — hier ggf. Wiedereinstieg | Offen |
| 13  | 2026-07-10 | Fachfragen-/Stakeholder-Fragen-Agenda: Waehrend discovery (und spaeter spec/plan) tauchen Fragen auf, die nicht sofort/allein beantwortbar sind, sondern ins woechentliche Fach-Meeting gehoeren. Derived-State-konform umsetzen: Quelle bleibt die Frage im `## Offene Punkte` des Features, markiert als Fachfrage (Tag `[Fach]` + Checkbox = Status offen/beantwortet, Antwort im selben Kontext nachgetragen). Neue rein-lesende Ansicht (z.B. `/dtb:offene-fragen` bzw. `/dtb:fach-agenda`) scannt `features/*/{discovery,spec,plan}.md`, sammelt offene `[Fach]`-Fragen, gruppiert nach Feature → fertige Meeting-Agenda (abgeleitet, kein zweiter Speicherort — NICHT ins BACKLOG duplizieren = Drift). Offene Design-Punkte: (1) Antwort-Rueckfluss manuell vs. Schreib-Modus der Agenda; (2) Reichweite nur discovery.md oder auch spec/plan; (3) Tag-Syntax `[Fach]`/`[Stakeholder]` vs. eigene Sektion `## Fachfragen` → features/fachfragen-erfassung/discovery.md → features/fachfragen-erfassung/spec.md | Ausgearbeitet |
