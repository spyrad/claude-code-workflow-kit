# Ideen-Inbox

> Schnell erfasste Ideen und Gedanken. Ausarbeitung mit `/dtb:feature-discover`.

---

| #   | Datum      | Idee | Status |
| --- | ---------- | ---- | ------ |
| 67  | 2026-08-16 | **Orchestrator/Worker-Uebergaben ueber Herdr automatisieren (1:n)** — Aufbauend auf #42, das Schreibgrenze, Guard und Hand-off-Block definiert, aber alle Uebergaben manuell laesst (Pane wechseln, cd, Session starten, Auftrag kopieren, Block zuruecktragen). Ziel: der Mensch trifft nur noch zwei Entscheidungen — Freigabe der Aufgaben-Zuteilung am Anfang, Abnahme des Diffs am Ende; alles Mechanische dazwischen laeuft automatisch. Drei Automatisierungs-Schritte: (a) **HINWEG** — Orchestrator legt pro Aufgabe Worktree an, oeffnet Pane (`herdr pane split`), startet Worker (`herdr agent start`), uebergibt den Auftrag per `herdr agent prompt`. KEINE Team-Registry noetig: die Orchestrator-Rolle folgt mechanisch aus dem Ort (git-dir-Vergleich = Haupt-Checkout), die eigene Adresse aus `$HERDR_PANE_ID`, laufende Worker aus `herdr agent list` — eine Datei wuerde nur fluechtige Pane-IDs persistieren (Zustandsaussage ohne Pfleger, #35-Familie). Stattdessen traegt der AUFTRAGSTEXT alles: Worktree-Pfad, Change-Slug, Orchestrator-Pane-Adresse, Rueckkanal-Anweisung — Chat-Text statt Datei, wie der Hand-off-Block selbst. Loest nebenbei die am 2026-08-15 belegte Rollen-Luecke (task.md kennt keinen Ausfuehrungsort, die Worker-Rolle lebt nur im Chat); (b) **RUECKWEG** — Worker ruft am Ende `/dtb:workflow-checkpoint`, Guard bricht ab und erzeugt den Hand-off-Block, Worker schickt ihn WOERTLICH mit Kopfzeile per `herdr agent prompt` an die Orchestrator-Pane (Anweisung steht im Auftragstext); (c) **ABSCHLUSS** — Orchestrator meldet „Worker N fertig", Mensch nimmt den Diff ab, Orchestrator merged, checkpointet und entfernt Worktree + Pane. Bei mehr Aufgaben als Workern: Warteschlange — freier Worker bekommt die naechste; Reihenfolge aus der Abhaengigkeitsanalyse der dtb:worker-Erkennungs-Sicht, Aufgaben mit gemeinsamen Dateien nie parallel. **BELEGT 2026-08-16** an w3:p1 ↔ w3:p4: beide Richtungen funktionieren. Drei Befunde aus dem Test: (1) PULL (`agent read`) ist fuer den Hand-off UNBRAUCHBAR — Claude Code rendert auf dem Alternate Screen, gescrollter Text erreicht Herdrs Scrollback nicht; darum PUSH durch den Worker; (2) `herdr agent start` ohne Agent-Argumente scheitert unter PowerShell (`Start-Process -ArgumentList ''`) — Workaround: Dummy-Argument; (3) per `agent prompt` zugestellte Nachrichten sind fuer den Empfaenger NICHT von menschlicher Eingabe unterscheidbar — die WORKTREE-HANDOFF-Kopfzeile ist der einzige Erkennungsanker der Empfangsseite. **Zu klaeren:** (1) TRAEGER — Pane-Modus in `dtb:worker` (der Subagenten-Modus macht Worktree-Anlage schon) vs. eigener Skill; Unterschied zum heutigen dtb:worker: echte Panes sind beobachtbar und unterbrechbar, Background-Subagenten blind — fuer Aufgaben mit Zwischenentscheidung der Unterschied zwischen „nicht autonom moeglich" und „moeglich"; (2) TRANSPORTNEUTRALITAET — #42 haelt Format und Empfangsseite bewusst traegerneutral, Herdr ist nur „der gedachte Traeger"; herdr-Befehle im Skill machen Herdr zur Voraussetzung — optionaler Zweig mit manuellem Fallback oder bewusste Abhaengigkeit? (3) GUARD bleibt unveraendert noetig — die Automatisierung ersetzt #42 nicht, sie setzt darauf auf; (4) Aufraeumen — wann darf der Worktree weg: nach Merge sofort, oder erst nach dem Checkpoint des Orchestrators? Wer entfernt die Pane? (5) Kollisionsregel von dtb:worker — gilt sie unveraendert bei n parallelen Panes? Beruehrung: #42 (Fundament), #59 (Selektionsfrage „welche EINE Entscheidung fehlt"), #45 (Hook-Haertung), #62 (Ort der Notiz bei hartem Worker-Stopp — stellt sich hier je Pane neu). | Offen |
| 66  | 2026-08-10 | **Die Kontext-Zeile „Kit-Stand" in `WORKFLOW_STATUS.md` behauptet einen Projektzustand, misst aber den maschinenlokalen `~/.claude/dtb-lock.json`.** Belegt 2026-08-10: die Zeile meldete „Lock 46 Artefakte @ `a79c218`", auf der zweiten Maschine stand der Lock tatsaechlich auf 44 @ `3d15d0a` (2026-08-06) — drei Tage und 10 Artefakte Rueckstand, unsichtbar fuer jeden, der nur das Statusdokument liest. Beruehrung #63 (ortsrelative Bezeichnungen), hier eine **ortsrelative Zahl** im generierten Kontextblock. **Zu klaeren:** (1) Kennzahl streichen, um den Maschinenbezug ergaenzen („@ Hostname") oder in einen kit-sync-Check verschieben; (2) gilt dasselbe fuer weitere Kennzahlen im Kontextblock, die lokale Zustaende messen? | Offen |
| 65  | 2026-08-10 | **`dtb:archive` Schritt 4d sagt „Abschnitt *Aktive Features* bleibt unberuehrt" — archivierte Features stehen aber genau dort.** Der Abschnitt „Abgeschlossen" in BACKLOG.md ist seit 2026-08-07 leer (die Zeilen wurden damals als Karteileichen entfernt), archivierbare Features werden also aus „Aktive Features" heraus verschoben. Woertliche Befolgung von 4d laesst die Zeile stehen und erzeugt genau die Karteileiche, die am 2026-08-07 in sechs Faellen geraeumt werden musste. Belegt 2026-08-10 bei `autonome-schiene`: Zeile bewusst entgegen der Anweisung entfernt und die Abweichung gemeldet. **Zu klaeren:** (1) 4d umformulieren (archivierte Items aus JEDEM Abschnitt entfernen) vs. den „Abgeschlossen"-Abschnitt wieder befuellen (dann waere 4d wieder stimmig, aber die Karteileichen-Entscheidung von 2026-08-07 waere rueckgaengig); (2) dieselbe Frage fuer Schritt 4e (`WORKFLOW_STATUS.md`), dessen „nicht entfernen, sondern warnen"-Regel bei einem generierten Statusblock ins Leere greift. | Offen |
| 64  | 2026-08-10 | **`lessons.md` ist ungetrackt (#34) und fehlt auf der zweiten Maschine ab L15** — belegt 2026-08-10: nur L1-L14 vorhanden, L15-L21 (u.a. L18/L20/L21) nie angekommen. Erster konkreter Schadensbeleg zu #34: die vier Prior-Leser (`impl-plan`, `debug-plan`, `plan-review`, `impl-review`) und `dtb:no-loss-check` arbeiten dort mit veraltetem bzw. lueckenhaftem Bestand, ohne dass es auffaellt. | Offen |
| 63  | 2026-08-09 | **Maschinen-Bezeichnungen in Artefakten sind ortsrelativ und damit nicht selbsttragend.** Belegt 2026-08-09: die Status-Aufgabe „Sicherungs-Branch `backup/2026-08-03-pre-merge` auf Maschine 2 loeschen" und `gitattributes-eol` Schritt 6 („andere Maschine") meinen beide „die, auf der gerade nicht gearbeitet wird" — liest man sie dort, meinen sie „hier", ohne dass sich der Text aendert. In den Logs existieren nur „diese Maschine" (17x) und „andere Maschine"/„Maschine 2" (33x); eine Zuordnung Name→Rechner gibt es nirgends. Workaround im Task bereits angewandt: mechanische Identifikation per `git branch --list backup/2026-08-03-pre-merge` statt ueber den Namen. **Zu klaeren:** (1) feste Kennungen (Hostname) statt relativer Namen als Konvention in `skills/CLAUDE.md`; (2) Beruehrung #56 (Sitzungen ueber Mitternacht — dort haengt die Bedeutung an der Zeit, hier am Ort) und #42 (parallele Sessions). | Offen |
| 62  | 2026-08-09 | **Der Ort der Minimal-Notiz bei hartem Worker-Stopp ist unbestimmt** — `skills/dtb-worker/SKILL.md:196-197` sagt: bei harten Stopps (Task-Verwaltung, Kollisionsregel) bleibt der Diff ohne Report, „der koordinierende Haupt-Agent legt eine Minimal-Notiz an (Ausgang ‚gestoppt', Zeitpunkt)" — **wohin**, steht nicht da. Nach dem F1-Fix (Worker schreiben nur im eigenen Worktree, das Haupt-Repo erreichen Aenderungen erst mit der Abnahme) ist die Frage nicht mehr trivial: schreibt der Haupt-Agent nach `features/{slug}/worker-report.md` im **Haupt-Repo** (dann entsteht eine Datei fuer einen Lauf, dessen Arbeitsergebnis noch unabgenommen im Worktree liegt — die Trennung, die F1 gerade hergestellt hat, faellt auf der Report-Seite wieder) oder **in den Worktree** (dann liegt die Notiz genau dort, wo sie mangels Abnahme niemand liest)? **Belegt 2026-08-09** beim Randfall-Durchgang der Restabnahme `autonome-schiene` (Randfall „Kollision trotz Analyse"): der Fall ist definiert, sein Schreib-Ziel nicht. **Zu klaeren:** (1) welcher Ort — Haupt-Repo, Worktree oder Chat-only (Sammel-Zeile in Schritt 9 statt Datei); (2) Verhaeltnis zur `worker-report.md`-Konvention: ist die Minimal-Notiz derselbe Dateityp (dann status-neutral nach §1.1, aber ohne Versuchs-Tabelle) oder etwas anderes; (3) wer raeumt sie auf, wenn der gestoppte Eintrag spaeter erfolgreich neu laeuft — ueberschreiben oder anhaengen? **Schadenslage heute:** hypothetisch — beide realen Laeufe (2026-08-08, 2026-08-09) endeten selbst-beendet mit vollem Report, ein harter Stopp ist nie eingetreten. **Beruehrung:** F1/F3 der `impl-review`-Triage (Schreibgrenze bzw. Worktree-Lebenszyklus), #58 (zweiter Nebenbefund desselben Durchgangs). | Offen |
| 61  | 2026-08-09 | **`lessons.md` ist nicht durchgaengig sortiert** — die Nummern-Reihenfolge der Tabelle ist stellenweise verwuerfelt (Bestand 2026-08-09: 8 vor 7; 17 vor 16/15/14/13/12) — vermutlich durch Appends an wechselnden Stellen bzw. den Maschinen-Merge. Folge: „naechste laufende Nummer" und Duplikat-Check in `dtb:lesson` muessen die ganze Tabelle scannen statt der letzten Zeile, und Leser (Mensch wie Prior-Skills) erwarten chronologische Ordnung. Zu klaeren: (1) einmalige Neusortierung — vertraeglich mit der append-only-Regel (Kuratierung „geschieht bewusst" ist ja vorgesehen)? (2) Regel in `dtb:lesson` schaerfen: neue Zeile ans Tabellen**ende**, Nummer = max+1 ueber alle Zeilen; (3) Beruehrung #34 (lessons.md ungetrackt) — Sortierung sinnvollerweise vor dem Heben von L11-L18 nach `skills/CLAUDE.md`. Kontext: Verlustpruefung 2026-08-09, Fund 3. | Offen |
| 60  | 2026-08-09 | **`/dtb-idea` laedt sporadisch nicht** — am 2026-08-09 lud der Command den Skill zweimal nicht (beide Male mit sehr langem Freitext-Argument), waehrend vier andere Kit-Commands derselben Sitzung normal luden; beide Ideen wurden ersatzweise von Hand nach der gelesenen Skill-Mechanik erfasst. Zu klaeren: (1) Reproduzierbarkeit — haengt der Ausfall an der Argument-Laenge? (2) Ebene — Command-Loader des Harness vs. Skill-Datei selbst (im ersten Fall kein Kit-Fix moeglich, nur Doku/Workaround); (3) Workaround festhalten: Kurz-Argument absetzen und Text im Dialog nachreichen vs. Skill-Mechanik von Hand ausfuehren. Kontext: Verlustpruefung 2026-08-09, Fund 2 (Nebenbefund im Session-Log). | Offen |
| 59  | 2026-08-09 | **Welche EINE Entscheidung macht einen INBOX-Eintrag worker-tauglich?** — die Erkennungs-Sicht von `dtb:worker` sollte nicht nur urteilen, sondern den fehlenden Entscheid benennen. **Beleg aus zwei Laeufen:** 2026-08-06 waren 2 von 27 Eintraegen (teilweise) tauglich, 2026-08-09 waren es 2 von 26 — beide Male scheiterten die uebrigen NICHT am Aufwand, sondern daran, dass vor der Ausfuehrung eine Design-, Traeger- oder Zuschnitts-Frage liegt. Bei den zwei „teilweise"-Eintraegen liess sich die Luecke jeweils mit EINER kurzen Antwort schliessen (#46: „nur die zwei echten Kanten?" — danach lief der Worker gruen durch; #55: „Option (b) allein?"). **These:** fuer viele „nein"-Eintraege existiert ebenfalls genau eine solche Frage, sie wird heute nur nicht ausgewiesen — die Sicht sagt „nein, Scope offen", statt zu sagen „nein — es fehlt die Entscheidung X, danach waere es ein Worker-Auftrag". Das verwandelt eine Ablehnung in eine Handlungsanweisung fuer den Menschen und macht den Bestand messbar naeher an der Ausfuehrung. **Zu klaeren:** (1) Traeger — dritte Spalte „fehlender Entscheid" in der Worker-Sicht vs. eigenes Raster (Beruehrung #33, dessen Triage-Sicht dieselbe Liste scannt und sich nur im Bewertungsraster unterscheidet); (2) Abgrenzung zum bestehenden Feld „Vorab-Frage" — heute nur bei „teilweise" gefuellt; die Idee waere, es auch bei „nein" zu fuellen, sofern genau eine Frage die Luecke schliesst; (3) Ehrlichkeitsgrenze — bei Eintraegen mit mehreren offenen Punkten (#53 drei Optionen, #18 drei Punkte) waere eine erfundene Einzelfrage schlimmer als „nein", also braucht es ein Kriterium, wann die Spalte leer bleibt; (4) Nutzen belegen statt behaupten — der naechste Sicht-Lauf kann die Spalte probeweise fuellen und danach pruefen, wie viele Eintraege durch EINE Antwort tatsaechlich tauglich wurden. **Kontext:** Verlustpruefung 2026-08-08 (Fund 3, nie abgesetzt), bestaetigt durch den Sicht-Lauf 2026-08-09. | Offen |
| 58  | 2026-08-09 | **Zwei weitere echte halbseitige Pipeline-Kanten — der #46-Scan vom 2026-08-06 ist nicht mehr vollstaendig, und eine davon entstand NACH ihm.** Befund vom 2026-08-09 aus dem Gegenprobe-Vollscan des ersten `dtb:worker`-Laufs (Task `pipeline-kanten-reziprok`, Beleg: `features/pipeline-kanten-reziprok/worker-report.md`): (1) `dtb:opportunity-map` hat `next: [dtb:greenfield-prd]`, aber `dtb:greenfield-prd` steht auf `after: null` — der Greenfield-Ast bricht schon vor dem PRD ab, dieselbe Diagnose wie beim soeben geflickten `greenfield-roadmap` → `feature-discover`; (2) `dtb:worker` hat `next: [dtb:workflow-checkpoint]`, aber `dtb:workflow-checkpoint` steht auf `after: [dtb:impl-review, dtb:no-loss-check]` — der Worker-Rueckweg in den Checkpoint fehlt in der Gegenrichtung. **Fall (2) ist der eigentliche Befund:** `dtb:worker` entstand am 2026-08-08, und seine impl-review-Triage hat mit F5 GENAU diese Fehlerklasse behoben — allerdings nur auf der `dtb:task`-Seite; die Checkpoint-Seite blieb offen. Die Fehlerklasse reproduziert sich also weiterhin am Tag der Einfuehrung, obwohl sie im selben Lauf benannt wurde. Damit ist #46 (c) — „Traeger einer Pruefung: `project-health` vs. `pipeline-graph`" — nicht mehr nur eine Aufraeum-Frage, sondern die Frage, ob die Klasse ohne mechanische Pruefung ueberhaupt aufhoert: drei Nachweise in zehn Tagen (2026-07-30/31, 2026-08-06, 2026-08-08), jeder von Hand gefunden. **Zu klaeren:** (1) Bestandsbereinigung der beiden Faelle — bei (2) unstrittig, bei (1) haengt sie an der offenen Frage, ob `greenfield-prd.after: null` bewusst gesetzt ist (Einstiegspunkt des Greenfield-Astes) oder derselbe Nachtrag-Fehler; (2) gemeinsam mit #46 entscheiden oder als Beleg dort nachtragen — die Konventionsfragen #46 (a)-(c) sind unveraendert offen und wurden beim Teil-Routing 2026-08-08 bewusst nicht mitgeroutet; (3) taugt die Pruefung als Worker-Auftrag (Zielzustand ist ein Grep-Ergebnis, also nach dem Raster von `dtb:worker` tauglich) oder gehoert sie als Dauer-Check in `project-health`? **Beruehrung:** #46 (Ursprungs-Scan), #51 (Klasse-A-Nachlauf nach Fixes), #55 (Klassen-Tabelle statt Schaetzung). **Teil-Routing 2026-08-16:** Fall (2) → features/pipeline-kante-worker-checkpoint/task.md — **erledigt** (Commit `acb405e`, Worker-Session im Worktree, erster realer Herdr-Hand-off); Fall (1) und die Klaerungsfragen bleiben offen und wandern nicht mit. | Offen |
| 57  | 2026-08-08 | **Die Voll-Schiene nach Rueckfragen durchsuchen, die faktisch immer gleich beantwortet werden — und diese automatisieren.** Beobachtung aus der laufenden Praxis: an mehreren Stellen der Kette fragt ein Skill etwas ab, dessen Antwort in jedem bisherigen Lauf dieselbe war. Genannte Beispiele: (a) die **Namens-/Slug-Rueckfrage** fuer ein neues Feature (`dtb:feature-discover` Schritt 5, ebenso `feature-fast`) — der vorgeschlagene Slug wird uebernommen; (b) die Frage, ob ein **Task ins BACKLOG** geschrieben werden soll (`dtb:task`) — Antwort immer Ja. These: solche Rueckfragen kosten Zeit und Aufmerksamkeit, ohne Qualitaet zu erzeugen, weil sie keine echte Entscheidung tragen; sie koennten durch einen begruendeten Default mit Veto-Moeglichkeit ersetzt werden (das Muster, das `dtb:feature-fast` mit seiner vetobaren Zusammenfassungs-Vorlage bereits benutzt). **Auftrag der Idee ist die Erhebung, nicht die Einzelmassnahme:** die gesamte Voll-Schiene (`feature-discover` → `feature-plan`/`impl-plan` → `plan-review` → `feature-start` → `implement` → `impl-review` → `workflow-checkpoint`) systematisch nach weiteren solchen Stellen durchsuchen und je Fundstelle belegen, wie oft sie real abweichend beantwortet wurde. **Zu klaeren:** (1) **Abgrenzungskriterium** — was unterscheidet eine Schein-Rueckfrage (immer Ja, kein Informationsgewinn) von einem echten Entscheidungspunkt? Kandidat: traegt die Antwort eine Festlegung, die spaeter teuer zu revidieren ist? Ein Slug ist per `git mv` billig umkehrbar, ein Scope-Zuschnitt nicht; (2) **Ersetzungsform** — stiller Default vs. angezeigter Default mit Veto („Slug: `xy` — abweichen? [Enter = uebernehmen]") vs. Sammel-Bestaetigung am Ende; die Veto-Variante behaelt die Kontrolle bei nahezu null Kosten; (3) **Beleglage statt Bauchgefuehl** — die Behauptung „immer Ja" ist an den Session-Logs und Feature-Ordnern nachpruefbar (wurden je Slugs abgelehnt? je Tasks nicht ins Backlog geschrieben?); ohne diese Probe ist die Idee eine Vermutung; (4) **Kollision mit der Arbeitsweise** — dokumentierte Praeferenz ist, DTB-Entscheidungspunkte einzeln gemeinsam durchzugehen; die Idee zieht genau dort eine Grenze und muss sagen, wo sie verlaeuft. **Beruehrung #43** (autonome Schiene, Discovery laeuft seit 2026-08-08): dort geht es darum, ob eine Aufgabe **ganz ohne** Menschen laufen kann; hier darum, die volle, menschengefuehrte Schiene von Leerlauf zu befreien — dieselbe Achse, andere Radikalitaet. Beide sollten das Abgrenzungskriterium (1) teilen, sonst entstehen zwei widerspruechliche Definitionen von „braucht keine Entscheidung". **Beruehrung `dtb:feature-fast`**, das die Buendelung der drei Interviews bereits geloest hat — dessen Loesungsmuster (begruendete Default-Annahmen + eine vetobare Vorlage + Selbst-Eskalation bei >10 Annahmen) ist der naechstliegende Kandidat fuer die Uebertragung auf die Voll-Schiene. | Offen |
| 56  | 2026-08-07 | **Sitzungen ueber Mitternacht landen im Log des falschen Tages** — `dtb:workflow-checkpoint` bestimmt den Pfad des Session-Logs ueber das **heutige** Datum (`{changelog}/YYYY-MM/YYYY-MM-DD.md`). Laeuft eine Sitzung ueber Mitternacht, wird die Arbeit des Vortags in die Datei des Folgetags geschrieben, obwohl **alle** Belege das Vortagsdatum tragen: Commit-Zeitstempel, `ARCHIVE_LOG.md`-Zeilen, gesetzte Statusfelder, Lektions- und Ideen-Datumsspalten. **Belegt 2026-08-07/08:** der Archiv-Lauf (Commit `7d5e77f`, 11 Archiv-Log-Zeilen mit `2026-08-07`) haette nach `2026-08-08.md` geschrieben werden muessen; die Zuordnung wurde per Rueckfrage entschieden (Eintrag als Session 5 im 07er-Log) — **eine Ad-hoc-Entscheidung, die der Skill nicht kennt und die beim naechsten Mal wieder anfaellt.** Folge ohne Regel: Wer den Tag rekonstruiert, findet die Abschluss-Arbeit nicht dort, wo die Features abgeschlossen wurden; `dtb:session-summary` (liest Tages-Logs) meldet den Tag unvollstaendig. **Optionen:** (a) Kriterium „Datum des ersten Commits dieser Sitzung" — maschinell bestimmbar (`git log`), braucht aber einen Sitzungs-Anker; (b) Kriterium „Datei des letzten Session-Blocks, solange die Sitzung nicht unterbrochen wurde" — billig, aber unscharf; (c) Rueckfrage an den Menschen, wenn das heutige Datum vom Datum des letzten Session-Blocks abweicht (kleinster Eingriff, macht den Fall sichtbar statt ihn zu raten); (d) beides: Vollstaendiger Block im Vortags-Log + einzeilige Brueckenzeile im heutigen. **Zu klaeren:** (1) welche Option; (2) betrifft `dtb:session-summary` (Tages-Sicht), `dtb:lesson`/`dtb:idea` (Datumsspalten) und den `**Letzter Session-Log:**`-Verweis im Statusdokument gleichermassen? (3) Randfall Sitzung ueber mehrere Tage. | Offen |
| 55  | 2026-08-07 | **Die Klassenzugehoerigkeit geaenderter Dateien wird geschaetzt statt gegen die Klassen-Tabelle geprueft** — „reist diese Datei ueber `kit-sync` mit?" ist eine Frage mit einer eindeutigen Antwort (Klassen-Tabelle in `dtb:kit-sync`), wird aber regelmaessig aus dem Bauch beantwortet. **Belegt 2026-08-07** (Nachlauf zu #50): angekuendigt wurden „drei betroffene Klasse-A-Dateien", der sync fand **zwei** — `skills/CLAUDE.md` sieht wie ein Kit-Artefakt aus (liegt in `skills/`, ist Kit-Doku), trifft aber **kein** Klasse-A-Muster (`skills/dtb-*/SKILL.md` verlangt den Praefix-Ordner) und wird deshalb nicht verteilt. Schaden hier null, weil der sync-Lauf die Tabelle liest und nicht die Ankuendigung; die Fehleinschaetzung fuehrte aber zu einer falschen Aussage im Chat und haette in einer Planung (Nachlauf-Kriterien, Aufwandsschaetzung) direkt danebengelegen. **Optionen:** (a) winziger Lese-Modus `/dtb:kit-sync klass <pfad>` — beantwortet „Klasse A? wohin?" fuer einen oder mehrere Pfade, read-only, kein Netzwerk (die Klassen-Tabelle steht lokal im Skill); (b) Hinweiszeile in `skills/CLAUDE.md` selbst („diese Datei ist Repo-Doku, **kein** verteiltes Artefakt") — billigste Variante, faengt aber nur den einen Fall; (c) nichts tun, der sync korrigiert ohnehin. **Zu klaeren:** (1) welche Option; (2) bei (a): eigener Modus vs. Erweiterung des `check`-Reports um eine Klassen-Spalte; (3) gehoert dieselbe Frage in `dtb:impl-review` (dessen Triage-Abschluss laut #51 kuenftig auf den kit-sync-Nachlauf hinweisen soll — dafuer muss er die Klasse korrekt bestimmen, also haengt #51 faktisch hieran). **Beruehrung #31:** dort war `skills/CLAUDE.md` historisch der Streitfall (Lock-Eintrag ohne Klassenzuordnung, 2026-07-31 aufgeloest) — dieselbe Datei, andere Richtung: damals stand sie faelschlich IM Lock, heute wurde sie faelschlich als Klasse A angesehen. | Offen |
| 54  | 2026-08-07 | **Negativ-Kriterien auf Sprach-Muster koennen legitime Inhalte verbieten, nicht nur unerwuenschte — Vorschlag: verpflichtende Positiv-Gegenprobe.** **Belegt 2026-08-07** (Feature `feature-start-statusfeld`, impl-review-Finding F5): Das Checkpoint-Kriterium `sed -n '1,17p' … | grep -ciE 'updating|updates|setz|schreib'` = 0 sollte sicherstellen, dass das Frontmatter keine Schreib-Wirkung mehr **behauptet**. Es verbot damit aber auch jede **zutreffende** Offenlegung der verbliebenen Schreib-Wirkung: der Skill ruestet weiterhin `## Progress` nach (`produces: [features/*/plan.md]`), die `description` verschwieg das und endete mit „Status displays remain untouched" — ein Leser haette „schreibt nichts" verstanden. Behoben wurde es nur, weil eine **englische** Formulierung („may add a missing `## Progress` section") das deutsche Muster zufaellig nicht trifft; die Sprachgrenze war Glueck, nicht Design. **Muster-Abgrenzung:** Lektion 8 („Negativ-Kriterien auf die Wirkstelle ankern") deckt den **Ort** des Greps ab — hier war der Ort korrekt (Frontmatter-Fenster), das Problem lag im **Inhalt**: das Muster unterscheidet nicht zwischen „behauptet faelschlich" und „legt korrekt offen". **Vorschlag:** Zu jedem Negativ-Kriterium in `impl-plan` eine **Positiv-Gegenprobe** verlangen („was MUSS an der Wirkstelle weiterhin dastehen?") — dasselbe Feature hat das an anderer Stelle bereits richtig gemacht (Erhaltungs-Kriterium fuer die Auswahlregel-Zeile `keine "In Arbeit"`, und in Phase 2 fuer die fremde Aussage in Z. 104). Zu klaeren: (1) Traeger — Konvention in `skills/CLAUDE.md` („Mechanik-Regeln") vs. Pflichtfeld im `impl-plan`-Template; (2) gilt die Pflicht fuer **jedes** Negativ-Kriterium oder nur, wenn der verbotene Begriff auch legitime Verwendungen hat; (3) Beruehrung mit Lektion 2/8 (beide Grep-Ankerung) — als dritte Regel derselben Familie formulieren oder Lektion 8 erweitern? | Offen |
| 53  | 2026-08-07 | **Das `bug.md`-Statusfeld hat nach #50 keinen Pfleger mehr — und `dtb:bug-report` verspricht faelschlich einen** (dritter Fall der Familie „Zustandsaussage ohne Pfleger", nach #35 und #52). **Belegt 2026-08-07 im plan-review zu #50:** `dtb:workflow-checkpoint` fuehrt `bug.md` **weder** in `produces` (Z. 14: `WORKFLOW_STATUS.md, BACKLOG.md, features/*/spec.md, features/*/task.md, session-log, ROADMAP.md`) **noch** in seiner Sync-Anweisung (Z. 218: „`**Status:**`-Zeile in `features/<slug>/spec.md` bzw. `task.md`") — `spec.md` und `task.md` ja, `bug.md` nein. Einziger Schreiber des Felds ist heute `dtb:feature-start` (Z. 101, `Analysiert` → `In Arbeit`); genau diese Zeile entfernt Feature `feature-start-statusfeld`. Danach schreibt das Feld **niemand** mehr, waehrend es weiter gelesen wird (`backlog-status`, `project-health`, `archive`). **Verschaerfend — falsche Zusage im Bestand:** `dtb:bug-report` legt das Feld mit `**Status:** Offen` an (Z. 89) und behauptet Z. 134-135, die Status-Spalte werde „danach von `dtb:workflow-checkpoint` synchronisiert" — das stimmt schon heute nicht, unabhaengig von #50. **Optionen:** (a) `workflow-checkpoint` um `bug.md` erweitern (`produces` + Sync-Schritt) — macht die vorhandene Zusage nachtraeglich wahr, kleinste inhaltliche Aenderung; (b) das Anzeigefeld in `bug.md` abschaffen — der Status ist ohnehin aus `## Fix-Schritte` ableitbar (§1.5), dann muessen die drei Leser mit; (c) nur die falsche Zusage in `bug-report` streichen und die Luecke bewusst offen lassen. **Zu klaeren:** (1) welche Option; (2) gilt dieselbe Frage fuer `task.md` (dort synct der Checkpoint — also nur `bug.md` betroffen?); (3) gemeinsam mit **#52** entscheiden (dort Leser ohne Schreiber, hier Feld ohne Pfleger — dieselbe Familie) und mit **#44** (Zustand ohne Leser)? **Schadenslage heute:** null Bugs im Bestand, der Ausfall ist bis zum naechsten `bug-report`-Lauf hypothetisch. Zuschnitt-Entscheid 2026-08-07: bewusst NICHT Teil von #50 (haette den Fast-Track auf 3 Phasen gehoben). | Offen |
| 52  | 2026-08-07 | **Die Status-Sektion „Laufende Arbeit" wird von vier Skills adressiert, aber von keinem mehr erzeugt** — ein Leser-ohne-Schreiber-Defekt, Gegenstueck zu #44 (Zustand ohne Leser). **Belegt 2026-08-07** bei der Erhebung zu #50: `dtb:feature-start` weist an drei Stellen an, „Laufende Arbeit" in `WORKFLOW_STATUS.md` zu aktualisieren (Z. 97/103/109); gelesen wird die Sektion von `dtb:workflow-resume` (Z. 87 als Kontext-/Konfliktquelle, Z. 165 + Z. 200 als „1 Satz aus WORKFLOW_STATUS «Laufende Arbeit»" im Report-Template), von `dtb:workflow-status` (Z. 79 „Sektion «Laufende Arbeit» o.ae.") und von `dtb:project-health` (Z. 290 als Report-Zeile `✅/❌ Laufende Arbeit`). **Erzeugt wird sie nirgends:** weder das Status-Template in `dtb:workflow-checkpoint` noch die reale `WORKFLOW_STATUS.md` dieses Projekts enthaelt die Ueberschrift (Grep = 0). Die vier Leser greifen also seit der Template-Umstellung ins Leere — `workflow-resume` fuellt seine Zusammenfassung faktisch aus dem Kontextblock, `project-health` kann den Check nie gruen melden. Zu klaeren: (1) **Richtung** — Sektion abschaffen (vier Leser bereinigen, Zusammenfassung aus dem Kontextblock oder der Ableitung ziehen) ODER wieder einfuehren (Schreiber in `workflow-checkpoint` ergaenzen, generierter Block); (2) bei „abschaffen": woraus speist sich die 1-Satz-Zusammenfassung in `workflow-resume` kuenftig — aus dem manuellen Kontextblock oder aus der Ableitung selbst? (3) `project-health` Z. 290 — Check streichen oder auf eine existierende Sektion umhaengen? (4) Beruehrung **#35** (Zustandsaussagen ohne Pfleger — dieselbe Familie: hier fehlt der Pfleger ganz) und **#44** (dort Zustand ohne Leser, hier Leser ohne Zustand — gemeinsam entscheiden?). Zuschnitt-Entscheid 2026-08-07: bewusst NICHT Teil von #50 (dort nur die Schreib-Seite in `feature-start`), damit der Fast-Track bei 2 Phasen bleibt. | Offen |
| 51  | 2026-08-07 | **`dtb:impl-review` weist nach Triage-Fixes an Klasse-A-Artefakten nicht auf den faelligen `kit-sync` hin** — der Skill verweist am Ende nur auf `/dtb:workflow-checkpoint`; sind die gefixten Dateien Klasse-A-Artefakte (`skills/dtb-*/SKILL.md`, `agents/*.md`, `commands/dtb-*.md`, `output-styles/dtb-*.md`), bleiben die installierten Kopien unter `~/.claude/` auf dem alten Stand, waehrend das Repo die Fixes traegt. **Belegt 2026-08-07** (Feature `output-style-gezielt`): drei der neun Triage-Fixes lagen in Klasse-A-Dateien (`dtb-kit-sync`, `dtb-project-health`, `dtb-gezielt.md`) — der Nachlauf Commit → Push → sync fiel nur auf, weil er im Chat ausdruecklich benannt wurde; ohne diesen Hinweis waere der aktive Output-Style dauerhaft in der ungefixten Fassung weitergelaufen. Vorschlag: Triage-Abschluss prueft die gefixten Pfade gegen die Klassen-Muster und haengt bei Treffern eine Zeile „Nachlauf: Commit → Push → `/dtb:kit-sync sync`" an die Abschluss-Summary. Zu klaeren: (1) Traeger — `impl-review` allein oder auch `commit-and-push` (das denselben blinden Fleck hat, sobald Klasse-A-Dateien im Commit liegen); (2) Muster-Quelle — Klassen-Tabelle aus `kit-sync` lesen vs. im Skill duplizieren (Spiegel-Kopplung, #22-Muster); (3) nur melden vs. `kit-sync` direkt anstossen (Kit-Konvention: read-only = modellaufrufbar). Beruehrung: #31 (Lock-Zustaende ohne Klassenzuordnung), #22 (Skew sichtbar machen). | Offen |
| 48  | 2026-08-06 | **Die drei Capture-Skills des Grundbestands haben keinen Duplikat-Schutz** — `idea`, `task` und `bug-report` schreiben bedingungslos. **Vollscan 2026-08-06:** `dtb:lesson` (Schritt 3, Stichwort-`grep` mit weichem Hinweis + Bestaetigung, ausdruecklich „nicht hart blocken"), `dtb:open-question` (Schritt 4, exakter Textvergleich, „warnen und ueberspringen") und `dtb:meeting-dump` (mehrfach, §6.1 „je Meeting einer") pruefen vor dem Schreiben; `dtb:idea`, `dtb:task` und `dtb:bug-report` tun es nicht. **Das Muster ist kein Zufall:** die drei mit Schutz entstanden in den Features `lesson-loop` (2026-07-08), `fachfragen-erfassung` (2026-07-17) und `meeting-dump` (2026-07-21) — die drei ohne gehoeren zum Grundbestand und wurden nie nachgezogen. **Beleg 2026-08-06 (Beinahe-Fall):** Derselbe #35-Befund wurde erst als Nachtrag in den bestehenden INBOX-Eintrag #35 geschrieben und unmittelbar danach als `/dtb:idea` mit identischem Text erneut abgesetzt; verhindert hat die Dublette allein der Gespraechskontext — ein Aufruf in einer frischen Session haette kommentarlos einen zweiten Eintrag erzeugt. Schaden bei der INBOX konkret: eine Dublette verbraucht **zwei Nummern fuer einen Sachverhalt** (Beruehrung **#42**, wo die Nummernvergabe in einer geteilten Datei bereits als strukturell unsicher belegt ist) und blaeht die Triage-Liste (**#33**). **Vorlage vorhanden, kein neues Konzept noetig:** der `lesson`-Mechanismus (grep nach dem Kern der Aussage, Hinweis mit „trotzdem speichern? Ja/Abbrechen") passt 1:1 auf `idea`, weil beide Freitext erfassen. Zu klaeren: (1) **Traeger** — alle drei Skills einzeln haerten vs. eine gemeinsame Konvention in `skills/CLAUDE.md`, die fuer schreibende Capture-Skills einen Duplikat-Check vorschreibt (Letzteres verhindert, dass der naechste neue Capture-Skill die Luecke erneut erbt); (2) **Vergleichsschaerfe je Ziel** — exakt (`open-question`) vs. unscharf/Stichwort (`lesson`); fuer INBOX-Freitext ist exakt wertlos, weil dieselbe Idee selten woertlich gleich formuliert wird; (3) `task` und `bug-report` schreiben in `BACKLOG.md` bzw. `features/<slug>/bug.md` — dort ist der Kollisionsfall ein anderer (Slug-Kollision ist in `DERIVED_STATE_RULES` §4 bereits geregelt, Inhalts-Dublette nicht); (4) **Beruehrung mit #29:** dessen Randfall-Entscheidung „unscharfer Vergleich statt exaktem Textvergleich" ist dieselbe Frage an anderer Stelle — der No-Loss-Detektor muss erkennen, was schon erfasst ist, die Capture-Skills muessen erkennen, was sie gerade doppeln. Gemeinsam loesen oder getrennt? Kontext: `idea`-Aufruf 2026-08-06 nach dem `feature-discover`-Lauf zu #29. | Offen |
| 47  | 2026-08-06 | **`dtb:feature-discover` fragt zwei Dinge ab, bevor es die Grundlage dafuer hat** — zwei Auspraegungen desselben Reihenfolge-Problems, beide im Lauf zu #29 am 2026-08-06 aufgetreten: (1) **Schritt 2 (Codebase-Scan)** erhebt die Liste `## Betroffene Module` und laesst sie bestaetigen — welche Module betroffen sind, haengt aber am Scope, und der wird erst in **Schritt 3a** geklaert. Konkret: die Frage „faellt Rechner-Divergenz in den Zuschnitt" entschied darueber, ob `workflow-resume` in der Liste steht; sie musste aus 3a vorgezogen werden, um Schritt 2 ueberhaupt beantworten zu koennen. (2) **Schritt 3** speichert den Zwischenstand nach `features/<slug>/discovery.md` (Resume-Marker-Mechanik), aber der Slug wird erst in **Schritt 5** festgelegt — der Ordner muss provisorisch benannt und bei abweichender Namenswahl umbenannt werden. Zu klaeren: (a) Modulliste erst NACH 3a erheben, oder in Schritt 2 nur vorlaeufig erheben und nach 3a bestaetigen lassen? (b) Slug frueher festlegen (Schritt 5 vor Schritt 3) oder Zwischenstand woanders ablegen? (c) beruehrt `dtb:feature-fast`, das dieselben drei Erhebungen buendelt — hat es dasselbe Problem, oder loest die Buendelung es bereits? (d) Beruehrung mit **#32** (Aufnahmekriterium fuer `## Betroffene Module`) — beide betreffen denselben Abschnitt, #32 inhaltlich, dies zeitlich; getrennt halten oder zusammen angehen? Kontext: `feature-discover`-Lauf #29, 2026-08-06. | Offen |
| 46  | 2026-08-06 | **Pipeline-Kanten im Kit sind zur Haelfte einseitig notiert — kein Skill prueft das.** Das Kit notiert Kanten reziprok (`workflow-checkpoint.next: [dtb:workflow-resume]` + `workflow-resume.after: [dtb:workflow-checkpoint]`), aber ein Vollscan ueber alle 38 Skills am 2026-08-06 fand **sechs halbseitige Kanten**: (1) `feature-fast.next: [plan-review]` — `plan-review.after` nennt nur `impl-plan`; (2) `greenfield-roadmap.next: [feature-discover]` — `feature-discover.after` nennt nur `idea-review`; (3) `meeting-dump.next: [workflow-next]` — `workflow-next.after: null`; (4) `open-question.next: [workflow-next]` — dito; (5) `open-question.next: [meeting-agenda]` — `meeting-agenda.after: null`; (6) `meeting-dump.after: [meeting-agenda]` — `meeting-agenda.next: null`. Die Faelle zerfallen in zwei Gruppen: **vier** betreffen read-only Sichten (`workflow-next`, `meeting-agenda`), wo `after: null` bewusst sein koennte — eine jederzeit aufrufbare Sicht ist keine Pipeline-Station; **zwei** betreffen echte Stationen, wo die Gegenrichtung beim Bau nicht mitgezogen wurde (`feature-fast` ist vom 2026-08-02, `plan-review` wurde nicht nachgefuehrt — Spiegel-Kopplungs-Muster wie die Faelle vom 30./31.07.). Zu klaeren: (a) ist Reziprozitaet eine Regel oder eine Gewohnheit — und wo steht sie? `skills/CLAUDE.md` sagt dazu nichts; (b) sind read-only Sichten bewusst ausgenommen (dann als Konvention festhalten, sonst melden `project-health`/`pipeline-graph` kuenftig Rauschen); (c) Traeger einer Pruefung — `dtb:project-health` (Lint-Rolle) vs. `dtb:pipeline-graph` (liest die Kanten ohnehin); (d) Bestandsbereinigung der zwei echten Faelle. Nebenwirkung heute: `pipeline-graph` und `workflow-status` zeichnen einen Fluss, der in einer Richtung Kanten hat, die in der anderen fehlen. Kontext: `feature-discover` no-loss-gate (#29), Schritt 3e — dort wurde entschieden, drei Routing-Kanten bewusst NICHT zu setzen, was die Frage aufwarf. **Teil-Routing 2026-08-08:** nur die Bestandsbereinigung (d) — die zwei echten Kanten — ist als Aufgabe geroutet → features/pipeline-kanten-reziprok/task.md; die Konventionsfragen (a) Regel oder Gewohnheit, (b) read-only-Sichten ausgenommen? und (c) Traeger einer Pruefung bleiben **offen** und wandern nicht mit. | Offen |
| 45  | 2026-08-06 | Prüfen, **wo im cc-workflow-kit Claude-Code-Hooks passen und Sinn ergeben** würden. Zu klären: welche Hook-Events (SessionStart, PreToolUse/PostToolUse, Stop, SessionEnd, UserPromptSubmit) auf welche Kit-Stellen zahlen (z.B. Session-Lifecycle, Derived-State-Konsistenz, Commit-Disziplin), Abgrenzung Hook vs. Skill vs. Konvention, und Verteilungsweg (Klasse-A-Artefakt via `kit-sync` vs. `settings.json`-Seed via `project-init`). Berührung: **#35 (d)** — dort wurde ein SessionEnd-/Stop-Hook bereits begründet verworfen (Harness unterscheidet Session-Ende nicht von Pause), das ist als Vorbefund zu berücksichtigen; **#41** (TTS via Stop-Hook); **#29** (No-Loss-Gate als Guard-Moment). | Offen |
| 44  | 2026-08-04 | **INBOX-Status `Ausgearbeitet` hat keine Leseansicht** — ein entschiedener, ausfuehrungsbereiter Vorgang ist in keinem Lese-Skill sichtbar und bleibt dadurch liegen. **Belegter Fall #28** (`.gitattributes`, 10 EOL-Belege): steht seit 2026-08-02 auf `Ausgearbeitet` und taucht in **keiner** der drei Arbeits-Ansichten auf — `workflow-next` Schritt 2 liest aus der INBOX ausschliesslich `In Arbeit`; `idea-review` Schritt 1 filtert auf `Offen`; `backlog-status` liest BACKLOG.md, wo der Eintrag mangels `task.md` nicht steht (BACKLOG-Zeilen entstehen erst im `/dtb:task`-Lauf, `produces: [features/*/task.md, BACKLOG.md]`). Der Nutzer hat den Ausfall am 2026-08-04 selbst bemerkt („#28 kommt nicht in den Backlog"), nicht ein Skill. **Ursache ist eine bewusste Korrektur, deren Nebenwirkung nicht gesehen wurde:** bis `1eec2ea` (2026-08-02) setzte die Task-Lane der Dreier-Weiche `In Arbeit` — in diesem Zustand HAETTE `workflow-next` den Eintrag angezeigt. `impl-review` feature-fast F2 (S:Hoch × I:Hoch, blocking) verlangte die Umstellung auf `Ausgearbeitet`, weil ein dauerhaftes `In Arbeit` von argumentlosen `feature-fast`-/`feature-discover`-Laeufen automatisch geladen wird (Beleg dort ebenfalls #28). Der Befund war richtig; der Fix nahm den Eintrag aber aus dem einzigen Raster, das ihn sah, und legte ihn in kein anderes. Die im selben Fix ergaenzte INBOX-Rueckverlinkung (`dtb:task` Schritt 4b) greift erst NACH dem Task-Lauf und schliesst die Luecke davor nicht. **Optionen:** (a) **klein, bevorzugt** — `workflow-next` liest zusaetzlich INBOX-`Ausgearbeitet` und zeigt „Ausfuehrung ausstehend → `/dtb:task <N>`"; ein Lesepfad mehr, kein neues Artefakt, Derived State unangetastet; (b) zusaetzlich `backlog-status`/`workflow-status` (dort waere es eine eigene Queue-Zeile „Inbox (Ausgearbeitet)" — in `workflow-status` bereits faktisch vorhanden, weil dessen Schritt 1 alle vier INBOX-Status zaehlt, aber als „nicht pipeline-relevant" deklariert); (c) gross — der Zustand „entschieden, aber noch nicht ausgefuehrt" bekommt generell eine Ableitungsquelle (dieselbe Familie wie #35: Phase ohne Ableitungsquelle). **Offene Punkte:** (1) Traeger — nur `workflow-next` vs. alle drei Leser (Spiegel-Kopplung: dieselbe Konstellation, die am 30./31.07. zweimal Folgefehler erzeugt hat); (2) gilt dasselbe fuer `Verworfen`-Eintraege mit offenem Rest (heute still, vermutlich korrekt); (3) Abgrenzung zu #35 — dort fehlt die Ableitungsquelle NACH `Y/Y`, hier VOR dem ersten Artefakt; gleiches Muster (Zustand ohne Leser), verschiedene Pipeline-Enden: getrennt halten oder zusammenlegen? (4) `workflow-status` deklariert `Ausgearbeitet` ausdruecklich als nicht pipeline-relevant — ist das mit (a) noch konsistent oder muss die Zeile mit? Kontext: Nutzerfrage Session 2026-08-04 nach `/dtb:workflow-next` („warum kommt #28 nicht in den Backlog"), Historie per `git log -L` auf `1eec2ea` zurueckverfolgt. **Umnummeriert 2026-08-05:** urspruenglich als #39 erfasst (2026-08-04, diese Maschine); beim Zusammenfuehren kollidierte die Nummer mit dem parallel auf der anderen Maschine vergebenen #39 (Stitch) — praktischer Beleg fuer #42 (parallele Sessions). | Offen |
| 39  | 2026-08-05 | Tools wie **Stitch von Google** (https://stitch.withgoogle.com/ — KI-generierte UI-Designs/Frontend-Entwuerfe aus Text-Prompts) in den Workflow integrieren? Zu klaeren: wo im Kit ein UI-Entwurfs-Werkzeug andocken wuerde (Beruehrungspunkte: `project-design/`-Assets, `project-rules/UI.md`, Greenfield-Ast opportunity-map→PRD→Roadmap) und ob Integration = Skill, Konvention oder nur Doku-Verweis. | Offen |
| 41  | 2026-08-05 | Claude Code **zum Sprechen bringen** (Text-to-Speech fuer Ausgaben): Moeglichkeiten sichten — Open-Source-Projekte, Bibliotheken, ggf. Hook-basierte Ansaetze (z.B. Stop-Hook → TTS-Engine wie Piper/Coqui, OS-Bordmittel wie Windows SAPI/`say`). Zu klaeren: Was vorlesen (nur Zusammenfassungen vs. alles), Trigger (Hook vs. manuell), und ob das als Kit-Baustein taugt oder rein lokale Config bleibt. | Offen |
| 35  | 2026-08-01 | **Handoff-Block und WORKFLOW_STATUS haben keine Gueltigkeitsbedingung** — der „Naechster Befehl" ist eingefrorener Zustand ohne SHA-Anker und verfaellt unbemerkt. Belegter Fall 2026-07-31/08-01: Checkpoint `e55978a` (30.07.) schrieb Handoff „/dtb:impl-review plan-status-feld"; die Session vom 31.07. fuehrte genau diesen Lauf aus (10 Findings trianguliert, committet `5b29f81`), endete aber ohne Checkpoint. Der Resume am 01.08. schlug denselben, laengst erledigten Befehl vor. **Drei Ursachen, keine davon Disziplin:** (1) `workflow-checkpoint:159` fordert nur „Format stabil halten"; der Status-Kopf traegt `**Letztes Update:** YYYY-MM-DD` (Z. 94) — ein Datum misst weder Commits noch Arbeit und taugt nicht zur Invalidierung. (2) **Die Derived-State-Absicherung ist nach `Y/Y` blind:** `workflow-resume:219` laesst bei Widerspruch die Ableitung gewinnen, aber die Ableitung kennt nur `## Progress` — impl-review, Triage, kit-sync und Abnahme veraendern `## Progress` nicht (Feature stand vorher wie nachher 12/12), es gab formal keinen Widerspruch zu melden. Die gesamte Phase nach dem letzten Progress-Schritt hat keine Ableitungsquelle. (3) `workflow-resume:58-59` prueft Git nur gegen `@{u}`; HEAD war 0/0 zum Remote und trotzdem einen Commit vor dem Stand, den die Status-Datei beschrieb — diese Achse existiert im Skill nicht (die Drift wurde am 01.08. nur durch manuellen Vergleich mit dem Prosa-Satz „clean @ 8f3c3da" entdeckt, kein Skill-Schritt). **Zweiter Beleg desselben Musters:** `features/plan-status-feld/review.md:9` behauptet „Die Fixes liegen im Working Tree (noch nicht committet)" — seit `5b29f81` falsch, ohne Pfleger. Gemeinsamer Nenner: Zustandsaussagen in Prosa statt an einen SHA gebunden. Das Kit hat dieses Rezept zweimal erfolgreich angewandt (`## Progress`-SHA-Belege als Verifikationsevidenz; `dtb-lock.json` `sourceCommit` + Drei-Punkte-Vergleich) — der Handoff hat es nie bekommen. **Optionen:** (a) M1 `**Stand:** <short-sha>` im Status-Kopf, von `workflow-checkpoint` geschrieben; `resume` bekommt die dritte Vergleichsachse HEAD ↔ Checkpoint-Stand ↔ Remote (`git rev-list <sha>..HEAD`, Commits mit Betreff listen statt nur zaehlen — der Checkpoint-Commit selbst erzeugt immer +1 und ist als `chore(checkpoint)` erkennbar); Handoff wird nur bevorzugt, wenn der Stand passt, sonst als „ueberholt" gemeldet. (b) M3 Erinnerung am Choke-Point: `commit-and-push` Schritt 6 meldet, wenn fuer heute noch gar kein Tages-Log existiert (ein Read; faengt genau diesen Fall, aber keine Sessions ohne Commit; nur bei komplett fehlendem Tages-Log, sonst Rauschen mitten in der Session). (c) M4 inhaltliche Loesung: Post-Progress-Zustand ableitbar machen — `review.md` traegt bereits `Geprueft bis: <sha>` und eine Triage-Bilanz; Regel `Y/Y` ∧ `Geprueft bis` == HEAD ∧ 0 PENDING ⇒ naechster Schritt ist Abnahme, nicht Review. Haette am 01.08. nicht gewarnt, sondern richtig geantwortet; verlangt aber ein maschinenlesbares review.md-Kopfschema (dieselbe Behandlung, die `plan.md` gerade mit §7 bekommen hat) — eigener Feature-Zuschnitt. (d) verworfen: SessionEnd-/Stop-Hook — das Harness unterscheidet Session-Ende nicht von einer Pause, ein bei jedem Stop feuernder Hook wird ignoriert. **Offene Punkte:** (1) Zuschnitt — nur (a) als Fix vs. (a)+(b) vs. (c) als eigenes Feature; (2) Multi-Repo: ein Stand-SHA pro `config.repos`-Eintrag oder pro git-toplevel (Beruehrung mit #23); (3) Toleranzregel fuer den Checkpoint-Commit sauber definieren (Betreff-Muster vs. Datei-Scope vs. n<=1); (4) Kanonisierung — gehoert die Handoff-Gueltigkeit in `DERIVED_STATE_RULES.md` (neuer §, dann Seed-Skew nach #22) oder bleibt sie Skill-lokal? (5) Spiegel-Kopplung: betroffen sind `workflow-checkpoint`, `workflow-resume`, das Status-Template und ggf. die Regel-Datei — genau die Konstellation, die in den Laeufen vom 30./31.07. zweimal Folgefehler erzeugt hat (F2 „Fix verletzt eigene Kopplungsregel", F4 Kriterium falsch verankert); plan-review-Gate ist hier nicht optional. **Abgrenzung:** #29 (Session-Hygiene-Gate) sitzt als Detektor VOR dem Checkpoint und verhindert Verlust, wenn der Checkpoint laeuft — diese Idee behandelt den Fall, dass er gar nicht laeuft, und faengt ihn beim naechsten Resume auf (komplementaer, nicht ueberlappend). #22 (Seed-Skew melden) teilt das Muster „Skew sichtbar machen", betrifft aber Klasse-B-Seeds, nicht den Handoff. Kontext: Resume + Checkpoint-Session 2026-08-01, Analyse auf Nutzerfrage. **Nachgetragen 2026-08-06 — Praxistest von Option (a), sie reicht so NICHT:** Der Checkpoint S3 (2026-08-05) hatte die Klausel bereits von Hand eingebaut („ist HEAD beim Resume ein anderer, zuerst gegenpruefen") — sie griff nicht, weil HEAD identisch war und der Fehler im **Arbeitsbaum** sass: der Satz „Arbeitsbaum sauber" war im Moment seiner Niederschrift falsch, weil derselbe Checkpoint-Lauf zwei Dateien schmutzig machte (Log + Status, committet erst am Folgetag als `e04e4a9`). **Strukturelle Ursache:** `workflow-checkpoint` liest `git status --short` in Schritt 1 und schreibt in Schritt 3/4 — der Lesezeitpunkt liegt zwangslaeufig vor dem eigenen Schreiben, **jeder** Checkpoint hinterlaesst deterministisch einen schmutzigen Arbeitsbaum; eine „sauber"-Aussage im Handoff ist damit immer falsch, ausser es folgt noch ein Commit. Beide Fehlerrichtungen jetzt belegt: S4 (2026-08-04) sagte „drei Dateien uncommittet", obwohl `71b6404` sie mitgenommen hatte (zu pessimistisch); S3 sagte „sauber", obwohl zwei offen waren (zu optimistisch — verschweigt Arbeit, der schlechtere Fall). **Konsequenz fuer (a):** zwei Achsen noetig (`HEAD == <sha>` UND Arbeitsbaum-Zustand), und der Checkpoint muss seinen eigenen Schreibvorgang antizipieren statt den vorgefundenen Zustand zu protokollieren. Vierter Beleg des Musters in Folge (2026-08-04, 2026-08-05 S1, 2026-08-05 S2, Resume 2026-08-05/06). | Offen |
| 34  | 2026-07-30 | **Entscheidung „`lessons.md` bleibt ungetrackt" (2026-07-18, S5) bewusst neu aufmachen** — dies ist die **legitime Wiederaufnahme** trotz Wiedervorlage-Schutz (WORKFLOW_STATUS Kontext-Zeile „Entschieden" + `.gitignore:10-13` „Nicht erneut als offene Frage aufmachen"): beide Sperren muessten bei einer Neuentscheidung mit angefasst werden, der Schutz gilt gegen *stilles* Wiederaufmachen, nicht gegen eine begruendete Revision. **Neues Faktum als Anlass** (war 2026-07-18 Theorie, seit 2026-07-30 Befund): nachgewiesene Zwei-Maschinen-Divergenz — die andere Maschine haelt L1–L11, diese L1–L7, dieselbe Lektion waere dort L12 und hier L7; die Bestaende wissen nichts voneinander, die Nummerierung ist kollidierend auseinandergelaufen. Real verloren gehen dabei nur die **projektspezifischen** Lektionen (nicht `Applies-to: alle`) — fuer Verallgemeinerbares existiert der Hebe-Weg nach `skills/CLAUDE.md` (versioniert, reist mit; L8/L10/L11 gehoben `4bb2b15`, L7 wartet). Optionen fuer die Neuentscheidung: (a) Status quo bestaetigen + Hebe-Disziplin staerken (Nullvariante, dann Schutz-Zeile um das neue Faktum ergaenzen); (b) `lessons.md` im Kit-Repo tracken (Konflikt mit der Rollen-Trennung Laufzeit-Artefakt vs. Deliverable — Kernargument der Alt-Entscheidung); (c) maschinen-lokale Dateien mit Sync-Sicht (z.B. `lessons.<host>.md` getrackt + merge-freie Aggregation); (d) Nummerierung global eindeutig machen (datums-/hashbasiert statt fortlaufend) als Minimal-Fix nur gegen die Kollision, Lokalitaet bleibt. Offene Punkte: (1) Scope — nur Kit-Repo oder kitweit (Zielprojekte fuehren `lessons.md` ebenfalls als unversioniertes Laufzeit-Artefakt, `dtb:lesson`/`project-init` waeren betroffen); (2) bei (b)/(c): Auswirkung auf `.gitignore`-Muster und kit-sync (lessons sind bewusst KEINE Klasse A); (3) Migrationspfad fuer die bestehende Divergenz (L-Nummern beider Maschinen zusammenfuehren ohne Beleg-Verlust); (4) Konsumenten pruefen — impl-plan/debug-plan/plan-review/impl-review lesen lessons.md als Prior, aendert Tracking deren Verhalten? Kontext: Nutzerfrage Session 2026-07-30 („zwei lokale Lektionen, die nicht voneinander wissen"). | Offen |
| 33  | 2026-07-30 | **Aufwand×Nutzen-Triage-Sicht als eigener Skill** (z.B. `/dtb:idea-triage`) oder als Modus von `dtb:idea-review`: sichtet alle offenen INBOX-Ideen und liefert eine priorisierte Triage-**Ansicht** statt Einzelentscheidungen — Gruppierung in vier Toepfe: **Quick Wins** (kleiner Aufwand, sofort nuetzlich), **strategisch wertvoll** (Mittel-Aufwand, lohnt Planung), **wartend/blockiert** (mit benannter Vorbedingung bzw. Blocker-Idee), **braucht eigenen Fokus** (nicht nebenbei); je Idee Aufwand- und Nutzen-Einschaetzung mit 1-Satz-Begruendung, Abhaengigkeiten zwischen Ideen explizit ausgewiesen (Muster „#32 zwingend vor #27"), empfohlene Bearbeitungs-Reihenfolge als Fazit. Anlass/Beleg: am 2026-07-30 wurde ein angesetztes `/dtb:idea-review` **dreimal** auf Nutzerwunsch in genau diese Triage umgelenkt (2x S1 lt. Log, 1x Folge-Session) — wiederkehrendes Muster, gehoert formalisiert. Abgrenzung: `idea-review` = schreibender Entscheidungs-Workflow pro Idee (Status-Aenderungen in der INBOX); Triage = rein lesende Priorisierungs-Sicht (INBOX unveraendert, keine Statusentscheidung, `produces: []`, kein zweiter Speicherort — Muster der read-only Skills `workflow-next`/`backlog-status`). Offene Punkte: (1) Traeger — eigener Skill vs. Argument-Modus `/dtb:idea-review triage` (ein Einstiegspunkt weniger vs. Vermischung read-only/schreibend in einem Skill); (2) Persistenz — nur Chat-Report vs. Ablage als Datei (Triage veraltet schnell, Drift-Risiko spricht fuer Chat-only; bisherige Praxis: Ergebnis landet komprimiert im Session-Log); (3) Bewertungsquellen — nur INBOX-Text vs. zusaetzlich Changelog-/WORKFLOW_STATUS-Querbelege (Praxisbelege wie bei #28/#31 fliessen heute manuell ein); (4) Verhaeltnis zur letzten dokumentierten Triage — jedes Mal neu bewerten vs. fortschreiben und nur Deltas begruenden; (5) Uebergabe an `idea-review` — Triage als optionale Vorstufe, deren Reihenfolge der Review uebernimmt. Kontext: idea-review-Sessions 2026-07-30. | Offen |
| 32  | 2026-07-28 | `dtb:feature-discover` Schritt 2 nennt **kein Aufnahmekriterium** fuer `## Betroffene Module` — externe Lesequellen/Vorbilder landen dadurch in einer Liste, die `dtb:impl-plan` als **projekt-relative Aenderungsziele** parst (Wartungs-Hinweis im Skill: „jede Zeile mit fuehrendem Pfad-Token" loest dort den Codebase-Research aus). Drei Ursachen im Skill-Text: der Wartungs-Hinweis warnt ausschliesslich vor **Format**-Aenderungen („Aenderst du hier das Format der Zeilen…"), nicht vor falschem Inhalt; Spaltenname „Relevanz" + Ueberschrift „potenziell betroffene Dateien/Module" lesen sich als „thematisch relevant" statt „wird geaendert"; die CLAUDE.md-Konvention „File paths … relative to the target project root" wird an der Stelle nicht wiederholt. **Bestandspraxis belegt den Systemfehler — 4 von 20 `discovery.md` tragen Fremdpfade:** `features/commit-and-push/` (`10x-devs-3.0/.claude/commands/10x-commit-and-push.md`), `archive/impl-review/` (`oma-personas/persona-{principled,torvalds}-reviewer/COGNITIVE.md`), `archive/lesson-loop/` (`frameworks/claude-code-memory-framework/templates/pitfalls.md`) und `archive/greenfield-autoren-skills/`, das den Bedarf am deutlichsten zeigt: dort wurde ad hoc ein Praefix **„Extern:"** erfunden, weil der Abschnitt keinen Platz fuer Vorbilder hat. Zusaetzlich `features/meeting-dump/` mit `features/*/{discovery,spec}.md (Zielprojekte)` — Glob plus Klammer-Kommentar statt Pfad, gleiche Parser-Frage. Ausloeser: Discovery-Lauf #15 am 2026-07-28, dort standen Vorbild + `references/health-check-schema.md` in der Tabelle; vom Nutzer bemerkt und vor dem Schreiben von `discovery.md` korrigiert (kein Artefakt entstanden). Wiederholungsrisiko konkret: #27 (`grill-me`) ist die naechste Discovery mit externem Vorbild. Offene Punkte: (1) Aufnahmekriterium in Schritt 2 ergaenzen — nur projekt-interne Aenderungsziele, relativ zum Projekt-Root; (2) legitimer Platz fuer Vorbilder/Lesequellen — eigener Abschnitt `## Referenzquellen` im Schritt-6-Template vs. Standardisierung des vorhandenen „Extern:"-Praefixes (Ersteres parser-sicher, Letzteres bestandskompatibel); (3) soll `impl-plan` nicht-existente Pfade melden statt still zu recherchieren (Robustheit unabhaengig von der Erfassungs-Disziplin); (4) Bestandsbereinigung der belegten Dateien — nur aktive Features (`commit-and-push`, `meeting-dump`) oder Archiv mit? Kontext: feature-discover #15, 2026-07-28. | Offen |
| 31  | 2026-07-23 | `kit-sync` hat **keinen definierten Zustand fuer Lock-Eintraege ausserhalb der Klassen-Tabelle** — sie sind ein blinder Fleck: weder geprueft noch aktualisiert noch als verwaist erkannt. **Konkreter Ausloeser — seit 2026-07-31 ERLEDIGT (Aufloesung unten), der generische Teil bleibt der Kern der Idee:** `skills/CLAUDE.md` stand im Lock (`mode: synced`, `target: ~/.claude/skills/CLAUDE.md`, `installedAt: 2026-07-18`) und passte auf keine Zeile der Klassen-Tabelle (`skills/dtb-*/SKILL.md`, `agents/*.md`, `commands/dtb-*.md`). Folge im check-Modus damals: Schritt 3 erfasst die Datei nicht (kein Klasse-A-Treffer), Schritt 4 klassifiziert sie daher nie, und Schritt 5 greift auch nicht — „verwaist" verlangt, dass der Repo-Pfad in `$SRC` **nicht** existiert, er existiert hier aber. Der Eintrag fiel durch jedes Raster. Praktische Auswirkung damals belegt: der sync-Lauf 2026-07-23 hat `skills/CLAUDE.md` nicht angefasst, obwohl die Repo-Version im selben Lauf geaendert wurde (toter INBOX-#8-Verweis bereinigt, `7c3272a`) — die installierte Kopie trug weiter den alten Text, ohne dass ein check das gemeldet haette. **Aufloesung des konkreten Falls (Variante „Lock-Eintrag streichen, Guide gehoert ins Repo"):** 2026-07-29 war der Eintrag nicht mehr im Lock, Lock- und Repo-Menge deckungsgleich (41/41, `2026-07/2026-07-29.md:141`); 2026-07-30 auf der anderen Maschine Eintrag gestrichen **und** die veraltete Kopie entfernt (`2026-07-30.md:20-24`); 2026-07-31 dieselbe Kopie auf dieser Maschine entfernt (Hash `a312b500…` vs. Repo `44a09675…`) — damit **auf beiden Maschinen vollzogen** (`2026-07-31.md:17-19`). Nebenertrag: der Bestand traegt jetzt **keinen** Nicht-Klasse-A-Schluessel mehr (41/41, 0 verwaist). **Verbleibender Zuschnitt (Stand 2026-08-04):** (1) **der Kern** — ein definierter Zustand „unklassifiziert" fuer Lock-Eintraege ohne Klassen-Zuordnung, damit derselbe Fall nicht erneut stumm bleibt; **frischer Praxisbeleg 2026-07-30:** das check-Hilfsskript `kitcheck.sh` musste diesen Zustand ad hoc selbst melden, weil `kit-sync` ihn nicht kennt (`2026-07/2026-07-30.md:29`). (2) Herkunft klaeren — wie kam der Eintrag 2026-07-18 ins Lock, obwohl auch der install-Modus „pro Klasse-A-Datei" arbeitet? Ggf. weicht die tatsaechliche Erfassung dort von der Tabelle ab; unabhaengig vom erledigten Einzelfall weiter offen, denn der Erfassungspfad ist unveraendert. **Erledigt und gestrichen:** alter Punkt (1) (Aufloesung des Einzelfalls — entschieden, s.o.) und alter Punkt (4) (weitere Nicht-Klasse-A-Schluessel — Antwort: es war genau der eine von 42, jetzt keiner). Kontext: `/dtb:kit-sync sync`-Lauf 2026-07-23, waehrend der Personas-Entfernung entdeckt; **Eintrag 2026-08-04 korrigiert** — er behauptete bis dahin im Praesens, `skills/CLAUDE.md` stehe im Lock (seit 2026-07-29 widerlegt; die Aufgabe „auf den generischen Teil reduzieren" stand 7x unerledigt in den Naechsten Schritten). | Offen |
| 27  | 2026-07-17 | `grill-me`-Skill von Matt Pocock (https://github.com/mattpocock/skills/tree/main/skills/productivity/grill-me) evaluieren: in den Greenfield-Stack aufnehmen — würde es überhaupt Sinn machen, und wo? Zu klären: was der Skill leistet, und Abgrenzung/Ergänzung zu den bestehenden Vorfeld-/Greenfield-Skills (opportunity-map, greenfield-prd/-roadmap, mom-test #16 — inzwischen Verworfen und archiviert 2026-07-30, als Abgrenzungsziel entfallen —, feature-discover). **Nachgetragen 2026-08-05, zwei Befunde aus den Logs:** (1) `grill-me` UND `grill-with-docs` sind laengst global unter `~/.claude/skills/` **installiert**, aber nie gegen den Greenfield-Stack abgegrenzt worden (Nebenbefund 2026-07-28, `2026-07/2026-07-28.md:37-39`) — die Evaluation ist damit **ohne Bau entscheidbar** (`2026-07-28.md:165`): lesen, abgrenzen, entscheiden; der Zuschnitt ist real kleiner als der Eintrag suggeriert. (2) Abhaengigkeit aus der Aufwand×Nutzen-Triage 2026-07-30: **#32 zwingend VOR #27** — grill-me waere die naechste Discovery mit externem Vorbild, exakt das #32-Risiko (Fremdpfade/Vorbilder landen in `## Betroffene Module`; `2026-07/2026-07-30.md:47-48`). | Offen |
| 23  | 2026-07-16 | Multi-Repo-Begriff kitweit auf Git-toplevel vereinheitlichen: `dtb:implement` + `DERIVED_STATE_RULES.md` §2 Regel 5 definieren „Multi-Repo" ueber die **Anzahl `config.repos`-Eintraege** (löst `repo-name@SHA`-Praefix in `## Progress` aus). `dtb:commit-and-push` (Feature 2026-07-16) definiert Repo-Identitaet dagegen ueber `git rev-parse --show-toplevel` — robuster, weil config-unabhaengig. Divergenz sichtbar bei pkp (Monorepo): `config.repos` hat 2 Eintraege (`pkp-frontend`/`pkp-backend`) → implement schreibt `pkp-frontend@sha`-Praefixe, obwohl es faktisch **ein** Repo (ein `.git`) ist → die SHA-Belege suggerieren zwei Repos. Idee: §2 Regel 5 + implement auf toplevel-Gruppierung umstellen (Multi-Repo = mehr als ein eindeutiger toplevel, nicht mehr als ein config.repos-Eintrag). Offen: Rueckwaertskompatibilitaet bestehender `## Progress`-SHAs mit Praefix; Auswirkung auf `repo-sync`/`build-check`. Kontext: plan-review commit-and-push 2026-07-16 (Architekt-Befund Multi-Repo-Divergenz). **Reproduktionsbeleg nachgetragen 2026-08-05 (Lauf vom 2026-07-20, `2026-07/2026-07-20.md:15-16,30-31`):** bei der verifikations-gate-Abnahme in pkp **hart reproduziert** — `config.repos`=2 fuehrte zum Praefix `pkp-frontend@f39af15` in `## Progress`, obwohl der Commit physisch in EINEM `.git` lag (enthielt auch `dtb-project/`); die Divergenz ist seitdem nicht mehr Theorie. Der damalige Log setzte „#23 ausarbeiten (jetzt hart belegt): `/dtb:feature-discover 23`" bereits als naechsten Schritt — seit 2026-07-20 unerledigt. | Offen |
| 22  | 2026-07-15 | Seed-Skew aktiv melden: Seeds (`DERIVED_STATE_RULES.md`, `settings.json`) sind bewusst „copied once, never drift-checked" (Klasse B) — mit der §2-Härtung (Feature verifikations-gate) entsteht aber realer Skew: global verteilte Klasse-A-Skills (z.B. `dtb:implement`) setzen das gehärtete §2 voraus, Bestandsprojekte tragen den alten Seed. Idee: `kit-sync check` oder `project-health` meldet veraltete Seeds als eigenen Hinweis-Zustand (informativ, KEIN Auto-Update — Seeds bleiben projektgehörig, Design-Entscheidung Klasse B bleibt unangetastet). Offen: Träger (kit-sync vs. project-health), Erkennungsmechanik (Seed-Hash im Lock vs. Versions-Marker in der Datei), Abgrenzung zur bewussten Projekt-Anpassung des Seeds. Kontext: plan-review verifikations-gate 2026-07-15 (Architekt-Frage Version-Skew). Belegt als **zweiter Skew-Fall** (2026-07-30, Feature plan-status-feld): `DERIVED_STATE_RULES.md` bekam §7 (Kopf-Statusfeld: Definitionsfenster, Pfleger-Regel, Lese-Toleranz) — die global gesyncten Klasse-A-Skills (`plan-review`, `workflow-next`, `workflow-status`, `impl-plan`) verweisen ab sofort auf diesen §, Bestandsprojekte tragen aber weiter den Seed ohne ihn. Der Schaden blieb hier klein, weil die Leser-Regeln bewusst rueckwaertskompatibel tolerant gebaut wurden (fehlendes Feld/Altwerte werden still behandelt) — genau diese Milderung ist aber Handarbeit pro Feature und ersetzt keine Skew-Meldung. Damit waren es drei dokumentierte Faelle (impl-review `review.md`, fachfragen-erfassung §6, jetzt §7). **Zaehler nachgezogen 2026-08-05 — es sind SECHS Faelle, alle nur in der Fusszeile der Regel-Datei vermerkt** (`DERIVED_STATE_RULES.md:407-419`): (1) `review.md` status-neutral (impl-review, 2026-07-16), (2) §6 Fach-Frage-Konvention (fachfragen-erfassung, 2026-07-17), (3) §6.1 `→ Zwischenstand:` + §6.3 schreibender Konsument (meeting-dump, 2026-07-21 — fehlte bereits in der damaligen Drei-Zaehlung), (4) §7 Kopf-Statusfeld (plan-status-feld, 2026-07-30), (5) `fast-draft.md` status-neutral (feature-fast, 2026-08-02, Triage-Fix F8 — `2026-08/2026-08-02.md:15`), (6) §6.3 lesender Konsument benannt (meeting-agenda, 2026-08-02 — `2026-08-02.md:96`). Frequenz als Nutzen-Argument: sechs Faelle in 17 Tagen (2026-07-16 bis 2026-08-02), davon zwei an einem Tag — der Skew waechst mit jedem Feature, das den Kanon anfasst, und die Fusszeile ist der einzige Ort, der ihn festhaelt. Offene Punkte unveraendert (Traeger, Erkennungsmechanik, Abgrenzung zur bewussten Projekt-Anpassung). | Offen |
| 18  | 2026-07-14 | `feature-discover` optionalen ROADMAP-Slice-Lookup geben (Roadmap→Discovery-Brücke schließen): Aktuell ist der Hand-off `greenfield-roadmap` → `/dtb:feature-discover <change-id>` nur konventionsbasiert — `feature-discover` liest die `ROADMAP.md` NICHT (`consumes: [INBOX.md, workflow.config.yaml]`), sein Schritt 1 sucht das Argument in der INBOX; ein Roadmap-Slice steht dort nicht → String dient nur als Slug-Seed, Slice-Kontext (Outcome + PRD-Bezug) muss manuell mitgebracht werden. Idee: Wenn das Argument einer Change-ID in `project-strategy/ROADMAP.md` entspricht, den Slice-Block (Outcome, Prerequisites, PRD-Bezug) als Discovery-Startkontext einlesen; `consumes` um ROADMAP.md erweitern. Offen: (1) Reihenfolge INBOX- vs. ROADMAP-Lookup (Slug direkt vs. Nummer/Stichwort); (2) Drift-Vermeidung — nur lesen, kein zweiter Speicherort; (3) Verhalten wenn Slug in beiden (INBOX + ROADMAP) vorkommt. Anschluss an #11 (Greenfield-Autoren-Skills), schließt die dort bewusst offen gelassene Rough Edge | Offen |
| 15  | 2026-07-14 | `10x-health-check`-Pendant bauen: Audit eines bestehenden (Brownfield-)Projekts — Dependency-Audit, Security-Scan, Test-Runner-/CI-/Missing-Config-Analyse, priorisierte Fixes + Agent-Readiness-Verdikt (Vorbild `10x-devs-3.0/.claude/skills/10x-health-check`, schreibt `context/foundation/health-check.md`). VOR dem Bau: gegen den bestehenden `dtb:project-health` prüfen und die Unterschiede herausarbeiten (project-health = Kit-/Workflow-Lint: Frontmatter, Derived-State, Drift, UPPER_SNAKE_CASE etc.; 10x-health-check = Projekt-Substanz: Abhängigkeiten/Security/CI). Offen: eigener Skill vs. project-health erweitern; Überschneidung/Abgrenzung; Brownfield-Ast war bei #11 bewusst ausgeklammert — hier ggf. Wiedereinstieg. **Vorpruefung gegen `dtb:project-health` ist ERLEDIGT (2026-07-27) — nicht wiederholen.** Ergebnis: Ueberschneidung praktisch null (project-health prueft ausschliesslich DTB-Artefakte, das Vorbild ausschliesslich Projekt-Substanz; drei harmlose Beruehrungen: CLAUDE.md-Zeilenzahl, Git-Status, build-check-Zustandsfrage). Verdikt: 1:1-Pendant nicht bauen (Kategorie-B-Logik des Originals ist Kurs-Ballast, acht Sprachfamilien zu teuer, schwacher Nutzungsfrequenz-Test); Gegenvorschlag schlanker Brownfield-Check auf Kategorie A. Details: Log `2026-07/2026-07-27.md`. **Zweiter Befund (2026-07-28, Discovery bewusst abgebrochen — braucht zusammenhaengenden Fokus, Status zurueck auf `Offen`):** Das Vorbild ist **kein alleinstehender Skill, sondern das letzte Glied einer Brownfield-Kette** — `10x-shape → 10x-prd → 10x-stack-assess → 10x-health-check` (steht so im Vorbild-SKILL.md). Sein Step 3a liest `context/foundation/stack-assessment.md`, erzeugt von `10x-stack-assess` (18 KB; bewertet den Stack gegen vier Gates — typed, convention-based, popular in training data, well-documented — und liefert **Kompensations-Bloecke fuer CLAUDE.md/AGENTS.md** statt Stack-Austausch; Verdikt u.a. `ready-with-compensation`). Das Kit hat dafuer **kein Pendant**: der Greenfield-Ast existiert (`opportunity-map` → `greenfield-prd` → `greenfield-roadmap`/`TECH-STACK.md`), der **Brownfield-Ast fehlt komplett** (bei #11 bewusst ausgeklammert). Folge: ein Kit-`health-check` ohne Vorstufe erbt einen Schritt, der ins Leere greift, und der praktisch wertvollste Teil des Vorbilds (Kompensationspfad, muendet in CLAUDE.md) saesse im fehlenden Nachbar-Skill. Nebenbefund: im Kurs-Repo existiert `stack-assessment.md` gar nicht — dessen Demo lief greenfield (`tech-stack.md` vorhanden), der Brownfield-Zweig wurde dort nie durchgespielt. **Entscheidungen fuer die spaetere Discovery:** (1) **Reihenfolge** — ist #15 ueberhaupt das richtige Einstiegsglied, oder zuerst ein `stack-assess`-Pendant? (2) **Zuschnitt** — nur Kategorie A (Lockfile, CVE-Audit, veraltete Deps, Test-Runner-Dry-Run, Config-Luecken: `.gitignore`/`tsconfig strict` high, Linter/Formatter medium) vs. Teile von Kategorie B (CI-Erkennung) vs. eingefalteter Gate-Teil statt Fremd-Datei; (3) **Traeger** — eigener Skill vs. `project-init` (dessen Schritt 1 erkennt den Stack bereits) vs. `build-check` (Letzteres waere **Wiederbelebung** eines nachweislich ungenutzten Skills, s. Log 2026-07-27); (4) **Naming** — `dtb:project-health` belegt den Namen „health"; (5) **Output** — Chat-Report vs. Datei (wohin im `dtb-project/`-Baum?) vs. Rueckschreiben erkannter Werte in `workflow.config.yaml` (`test_command` ist dort heute leer); (6) Sprachfamilien auf real genutzte begrenzen (TS/Node, Python)? Step 3a des Vorbilds ist mangels DTB-Aequivalent nicht uebertragbar. | Offen |
